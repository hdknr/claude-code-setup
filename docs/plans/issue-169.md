# #169 — 1 周の経過時間がどこで使われたかを測る（プロファイル）

## 周の在り処

- ブランチ: `issue/169-elapsed-profile`
- worktree: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-169`
- 起点: `e94521e`（Merge pull request #172）
- base: 参照 `origin/main`（`refs/remotes/origin/HEAD` から解決）→ `e94521ebe12799544f895ca3c6199172c916919b`
- 入場: `prepare-worktree.sh` で作成 → `EnterWorktree(path)`。**`ExitWorktree` は畳まない**（path で入ったため）

## 0. 範囲の決定（着手前に人間に訊いた）

Issue には切り離せる塊が 3 つある。**AskUserQuestion で「(a) だけ」が選ばれた。**

- **(a) 周の経過時間プロファイル** — この周でやる（Issue 本文の問い）
- (b) #168 から送られた `summarize-subagent.py` の復活と未解決 10 件 — **別 Issue に起票する**
- (c) 2 つ目のコメントの「遅延中は再実行せず待つ」規範 — **別 Issue に起票する**
  （本文の「やらないこと」＝まず測る、とぶつかり、根拠も 1 件だけ）

**(b) を外したので、Issue 本文の「#168 の集計スクリプトと式を共有する」は、この周では該当しない**
（共有する相手がリポジトリに無い）。

### 着手前に実物で確かめたこと（#168 の親セッション `cea81733-…` 1 本）

| 観測 | 結果 |
| --- | --- |
| 周の起動形 | `/dev-loop 168` が**素のテキスト**で打たれ、`Skill` の tool_use（`input.args: "168"`）で起動した。**`<command-args>` が無いので、既存の `issue_number` の経路では Issue 番号が取れない**（`--per-issue` から落ちる） |
| Verifier の起動と報告 | `Agent`（`subagent_type: dev-loop:dev-loop-verifier`・`name: verifier-168-p1`）→ 結果は `status: teammate_spawned`。**報告は user 行の `Another Claude session sent a message: <teammate-message teammate_id="verifier-168-p1" …>`** で届く。同じ id の `{"type":"idle_notification"…}` が後に続く |
| `/code-review` の起動と報告 | `Skill`（`skill: code-review`）→ 結果は `status: forked, background: true`。**報告は user 行の `<task-notification>` で、`<tool-use-id>` が起動した tool_use の id と一致する** |
| 親の動き | **関門を 2 つとも裏で走らせたまま、親は作業を続ける**（04:39:59 起動 → 04:40:07 end_turn → 04:47:45 通知）。**関門の区間は親の区間と重なる** |
| 人間の応答 | `AskUserQuestion` の tool_use → tool_result の間、または end_turn → 素のテキストの user 行 |

## 1. 変更範囲

触る:

- `scripts/token-metrics.py` — `--elapsed` を足す。**周の検出（`scan` の `dev_loop_sessions` と Issue 番号）は共有する**。Issue 番号の経路に `Skill` の `args` を足す
- `scripts/test-token-metrics.py` — 経過時間の回帰テストと変異テスト、`Skill` の `args` の経路
- `CLAUDE.md` — 「トークン使用量を測る」のコマンド例に 1 行
- `docs/plans/issue-169.md` — この計画

触らない:

- 手順（1〜8）単位の推定——Issue 本文のとおり、transcript に境界が無い。**この周は関門（tool 呼び出し）単位まで**
- `plugins/`（SKILL.md に「手順 N に入った」の印を書かせる案は Issue 本文が不採用）。**よって version bump なし**
- `summarize-subagent.py`（(b)）、遅延の規範（(c)）
- 経過時間を短くする規範（Issue の「やらないこと」）

## 2. 乗るデプロイ経路

`scripts/` と `CLAUDE.md` と `docs/plans/` だけ。**`mkdocs.yml` に `docs/plans/` の除外は無い**ので、
マージすると `docs/plans/issue-169.md` は **nav に無いページとして GitHub Pages に載る**（反映経路あり）。
スクリプト本体は CI から呼ばれない（テストは `docs.yml` で回る）。

## 3. verify の受入基準

### 3.1 不変条件

- **A. 分割は壁時計と一致する。** セッションごとに、区分（モデル／道具／人間待ち／通知待ち／その他）の合計が、そのセッションの最初の行から最後の行までの時間と**等しい**（欠けも重複もない）
- **B. 人間待ちを作業に入れない。** `AskUserQuestion` の tool_use → tool_result、および end_turn → 人間の発言の区間は「人間待ち」に入る
- **C. 通知待ちを人間待ちと混ぜない。** end_turn → 通知（task-notification / teammate-message）の区間は「通知待ち」に入る
- **D. 関門の区間は、起動から最初の報告まで。** Verifier（`Agent` で `subagent_type` に `dev-loop-verifier`）と `/code-review`（`Skill` で `code-review`）について、終わりは次のどれか——前景の tool_result ／ `<tool-use-id>` が一致する task-notification ／ `Agent` の `name` と `teammate_id` が一致する teammate-message（**`result` を持たない** idle 通知は除く）。**終わりが見つからない関門は落とさず、0 分にもせず、件数を別に出す**
- **E. 関門の時間を壁時計に足さない。** 関門は親の区間と重なるので、出力は合計に混ぜず、そう明記する
- **F. 周の数え方は 1 箇所。** `--elapsed` は `scan` が出す周と Issue 番号をそのまま使う（別の検出を書かない）。**`Skill` の `args` から Issue 番号を取る経路は共有側に足す**（`--per-issue` にも効く）
- **G. 人間の発言の判定。** tool_result・通知・teammate-message・`isMeta` の行（スキル本文の注入など）は人間の発言として数えない
- **H. 既存の出力を黙って変えない。** `--per-issue` 以外の既存出力は変わらない。`--per-issue` は `Skill` の `args` で束ねられる周が増える（F の意図した変化）
- **I. 読めない・壊れた行は既存の件数報告に乗ったまま**

### 3.2 これを破りうる経路

| 経路 | 関わる不変条件 | どう守るか |
| --- | --- | --- |
| `/clear` で割った周（1 Issue に複数セッション） | A・F | セッションごとに分割し、Issue で合計。セッション間の空白は「セッション外」として別に出す |
| タイムスタンプが順不同の行（実物: 先頭の 2 行が逆順） | A | 時刻で並べ替えてから区間を作る |
| タイムスタンプの無い行・attachment・system 行 | A・G | 境界にしない |
| `--since` が周の途中に落ちる | A | 既存の起点と同じく「先頭が範囲外」として出す（近い値で代用しない） |
| `--repo` / `--merge-worktrees` | F | `scan` と同じ粒度で絞る |
| サブエージェントの transcript | A・E | 親の分割には入れない（関門は親の側の起動と報告で測る） |
| 同じ tool-use-id / teammate の通知が 2 回来る | D | 最初の報告を終わりにする |
| 前景の `Agent`（同期） | D | tool_result を終わりにする |
| 報告が来なかった関門 | D | 「終わりの記録なし」の件数に入れる |
| `AskUserQuestion` の結果 | B | 直前が end_turn でなくても人間待ち |
| 起動形が素のテキスト＋`Skill`（#168 の実物） | F | `Skill` の `args` の数字を Issue 番号として使う（`<command-args>` が優先） |
| **（1 パス目で追加）** `--since` で `/clear` 前のセッションが丸ごと範囲外 | A | 丸ごと範囲外のセッションの Issue を覚え、残った周に「先頭が範囲外」の印を付ける |
| **（1 パス目で追加）** `end_turn` 以外で止まった応答（`stop_sequence`・なし）→ 人間 | B | 「止まった」を `stop_reason != "tool_use"` で判定する |
| **（1 パス目で追加）** 人間 → 人間 | B | 人間待ちにする |
| **（1 パス目で追加）** `<tool-use-id>` の無い通知（attachment の経路） | C | 通知の形なら境界にする（関門の終わりには当てない） |
| **（1 パス目で追加）** `isMeta` で届くサブエージェントの引き渡し | C・G | `<agent-message ` を含む `isMeta` の行だけ通知にする。他の `isMeta` は従来どおり境界にしない |
| **（2 パス目で追加）** 引き渡しが attachment（`queued_command`）で届く | C | `<agent-message ` で始まる prompt を通知にする |
| **（2 パス目で追加）** 関門の最初の報告が引き渡し（`<agent-message from="<name>">`） | D | `from=` を報告した teammate として数え、`Agent` の `name` と一致したら終わりにする。`/code-review` には当てない（`from="code-review"` は並行するレビューを見分けられない） |
| **（2 パス目で追加）** 1 つの応答が複数行に分かれ、先の行が `end_turn` を持つ | A（区分の正しさ） | 次の行が同じ `message.id` の assistant なら止まったと見なさない（モデル） |

### 3.3 この環境では証明できないもの（未証明）

- **通知の行の時刻が、親に届いた時刻と一致するか。** transcript が書く時刻しか見えない。
  **ずれは時間単位になりうる**——`c73a0b7a…` の idle 通知は、行が 23:47:15・本文の `timestamp` が
  12:14:30 だった（2 パス目の `/code-review` が指摘）。**行の時刻を採る**（親が受け取った時刻）
- **teammate-message の最初の非 idle の発言が「報告」か。** 途中経過を送る Verifier がいれば、終わりが早く出る。**実物（#168 の 8 体）では全部が報告だった**が、形として保証されない
- **分類の精度そのもの。** 境界の手がかり（end_turn → 次の行）で分けているので、**end_turn の直後に人間ではなく自動の入力が来た場合**（hook など）の誤分類は、実物に当てて数えるしかない
- **「報告なし」6 件のうち 5 件が本当に報告の無い関門か**（1 件だけ実物で見た）
- **通知でない `queued_command`**（人間の入力の待ち行列など、実物に 562 件）を境界にしていない。
  **その行は無かったことになり、区間は次の境界で分類される**——end_turn の後に来て、
  次が assistant なら**「その他」に入る**ので、**人間待ちが「その他」の側に過小に出る**
  （1 パス目の `/code-review` の実測で 651 件・2,178 分。**親は測り直していない**）。
  **最初「前後の行の区分に入る」と書いていたのは誤りで、`/code-review` 1 パス目に反証された**

### 3.4 BLOCKED

なし（入力の実物は手元にある）。

### 3.5 生成物の鮮度

**生成物なし。** 図（`diagrams/`）と `SKILL.md` の測定値（`skill-metrics.py`）は、どちらもこの周で触るファイルに依存しない。**`check-all.py` が両方の `--check` を回すので、それで確かめる。**

### 3.6 必須要件の掛け算

Verifier × 1・2 パス目、`/code-review` × 1・2 パス目 の 4 つの受け渡しすべてに、§3 と §6 を渡す。

### 3.7 実データでの陽性対照

#168 の親セッション（`cea81733-…`）に当てて、**上の「着手前に確かめたこと」の時刻と一致するか**を見る——`/code-review` 1 パス目は 04:39:59 起動 → 04:47:45 通知（7 分 46 秒）、Verifier 1 パス目は 04:39:33 起動 → 05:12:42 報告（33 分 9 秒）。

## 4. 未解決の判断

- (b)・(c) の起票（手順 8 で行う）
- **既存の `<command-args>` の経路が拾う番号**に怪しいものがある（`--elapsed` の実データに `#1737` が出た。
  `Skill` の `args` の経路ではないことは確かめた——`dev-loop` の `Skill` 起動 43 件のうち 41 件が数字だけ、
  残る 2 件も先頭の数字が正しい番号だった）。**この周では触らない**（範囲外。起票するかは手順 8 で決める）

## 5. 関門の進捗（再開点）

- 実装: **済み**（`2c24d53`）
- 手順 4 の割り目: **AskUserQuestion で訊いた → 人間が「ここで割る」を選んだ**。
  **次は手順 5 の Verifier 1 パス目から**（この worktree の中で `/dev-loop 169`）
- 歯止め: `python3 scripts/check-all.py` → **28/28 本（失敗 0 / 飛ばし 0）**
- Verifier 1 パス目（`sonnet`・`dev-loop:dev-loop-verifier`）: **反証 0 件**。
  **ただし報告の冒頭で「`Edit`／`Write`／`Agent` を持つ」と申告した**——同梱の定義
  （`Read, Grep, Glob, Bash`）と食い違う。**密閉できていない側に倒す**。
  起動後に作業ツリーが変わっていない（`status --porcelain` が空）ことは親が確かめた
- `/code-review` 1 パス目（high）: **指摘 4 件**（下の採否の表）。**4 件とも直した**
- **手順 4 への差し戻し: 1 回**（`/code-review` の指摘 1〜3 が不変条件 A の経路・B・C に当たるため）
- 残り: **手順 5 の当て直し（Verifier 2 パス目）** と **`/code-review` 2 パス目**の両方
- 指摘対応のコミット後、`check-all.py` → **28/28 本（失敗 0 / 飛ばし 0）**
- 2 パス目の前の割り目: **AskUserQuestion で訊いた → 人間が「ここで割る」を選んだ**。
  **次は手順 5 の当て直し（Verifier 2 パス目）と `/code-review` 2 パス目**
  （この worktree の中で `/dev-loop 169`）。**base は変えない**（`e94521e…`）
- **再開した周（2 パス目の前の割り目のあと）**: Verifier 2 パス目 **反証 0 件**・
  `/code-review` 2 パス目 **指摘 10 件**（下の「2 パス目の指摘の採否」）。**直す 3・TODO 6・採らない 2**
  （V2 を含めて 11 行）。**手順 4 への差し戻し: 2 回目**（P2-1・P2-2 が不変条件 D・C に当たる）
- 2 パス目の指摘対応のあと、`check-all.py` → **28/28 本（失敗 0 / 飛ばし 0）**。
  実データで HEAD と比べた: 区分の合計 **124,734 分で一致**（A）、Verifier の関門の合計 5,542 → 4,844 分
  （`c73a0b7a…` の 699.3 分が消え、`verify-87-pass2` が 12.2 → 7.6 分）。モデル +93・道具 −21・その他 −73 分
  （分かれた応答の分）。**新しい変異 2 件の陽性対照**: `s5` を抜くと 2 件とも生き残る（親がリポジトリの外の複製で確かめた）
- 残り: **手順 5 の当て直し（Verifier 3 パス目）** と **`/code-review` 3 パス目**。
  **3 パス目が指摘 0 件でなければ、止まって人間に相談する**（手順 6）
- 交絡: **実験あり**。下の「実装中に実データで見つけたこと」の 3 件は、どれも**陽性対照と母集団で当てた**
  （#168 の関門の時刻が着手前の手計算と一致すること・重なり／報告なしの件数が直したあと減ること）

### 実装中に実データで見つけたこと（どれも直した）

| 見つけたこと | 実物 | 直し方 |
| --- | --- | --- |
| **fork / resume でコピーされた行を 2 度数えていた** | 同じ Issue の 2 セッションが **933 行の `uuid` を共有**し、セッションの長さの合計が壁時計の約 2 倍（344: 111 分に対し 220 分）。**コピーは時刻も同じ** | `uuid` で 1 度だけ数える。**直したあと、重なりは `uuid` を共有しない 2 組（本当に並行したセッション）だけ**になった → 「重なり N」と出す |
| **idle 通知を一律に捨てていた** | 「報告なし」40 件を調べたら、**報告の本文が idle 通知の `result` にしか無い**形（#92 の周） | `result` が空でない idle 通知は報告として数える → **40 件 → 6 件**。残る 6 件のうち 1 件（`verifier-122-pass2`）を実物で見た: 起動 27 秒後に `result` の無い idle 通知が来ただけで、そのあと `verifier-122-retry` が立っている。**残り 5 件は見ていない** |
| **attachment の通知を `json.dumps` してから当てていた** | 試料で落ちた。`teammate_id="…"` の引用符がエスケープされる | `prompt`（実物は全件が文字列）に直接当てる。**実物で `queued_command` に載るのは task-notification だけで、teammate は 0 件** |

### 陽性対照の結果（§3.7）

#168 の親セッションに当てて、`/code-review` 1 パス目 **7.76 分**（手計算 7 分 46 秒）、Verifier 1 パス目 **33.14 分**（手計算 33 分 9 秒）。**一致した。**

## 6. 既知の限界・決着済みの論点

| 論点 | 決着 | 理由 |
| --- | --- | --- |
| 手順単位で出すか | 採らない | transcript に境界が無い。Issue 本文も「推定の精度も測る必要がある」とし、今回は関門単位まで（人間が (a) を選んだ範囲） |
| `summarize-subagent.py` と式を共有するか | 該当しない | (b) を別 Issue に送ったので、共有する相手がリポジトリに無い |

### 1 パス目の指摘の採否

| 指摘 | 実物で失敗を示せるか | 採否 | 対応・理由 |
| --- | --- | --- | --- |
| R1: `--since` が `/clear` で割った周の前のセッションを丸ごと落とし、周を完結して見せる | 示せる（§3.2 の `--since` の行・不変条件 A）。`--elapsed --merge-worktrees --since 2026-09-25` で #169 が 2 セッション・35 分の周として出た | 直す | 丸ごと範囲外のセッションの Issue を記録し、その周に「先頭が範囲外」の印を付ける。**直したあと同じ実行で #169 は出ず「先頭が範囲外の周 1 件」**（親が実行） |
| R2: `<tool-use-id>` を持たない attachment の通知と、`isMeta` の引き渡し（`<agent-message …>`）が境界にならない | 示せる（不変条件 C）。`6c4131ab…` の 04:00:08 end_turn → 05:11:22 が「その他」。親が行を見て、`isMeta` の引き渡しと id の無い task-notification を確かめた | 直す | 通知の形なら attachment の経路でも境界にする。`isMeta` の行のうち `<agent-message ` を含むものは通知として数える（**関門の終わりには当てない**） |
| R3: 人間待ちを end_turn の直後にしか数えない（`stop_sequence` の応答・人間 → 人間が「その他」） | 示せる（不変条件 B）。親が実物で、人間の発言の直前の assistant 行の `stop_reason` を数えた: `end_turn` 4153・`stop_sequence` 85・なし 17・`tool_use` 2 | 直す | 「止まった」を `stop_reason != "tool_use"` にする。人間 → 人間も人間待ち |
| R4: `queued_command` の限界の記述が誤り（区間は前後ではなく「その他」に入る） | 示せる（`token-metrics.py` のコメントと §3.3 の記述が、コードの挙動と食い違う） | 直す | コメントと §3.3 を実際の挙動に合わせた |
| V1（Verifier の報告事項）: 列ごとに丸めるので、表を足すと壁時計と 1 分ずれる行がある | 示せる（#96 の 2026-09-16 の行: 41+6+1120+23+3=1193 に対し 1192） | 直す | 出力の注記に「列ごとに丸めるので足しても一致しないことがある」を足した（計算は変えない） |
| C1（親が当て直しで見つけた）: `isMeta` の `<cross-session-message …>`（別セッションからの発言）も境界にならず、end_turn → assistant で「その他」に入る | 示せる（`37c209eb…` の 2026-09-11 11:19:35 → 翌 05:42:09 の 1,102 分） | TODO | **通知（裏の仕事の報告）とも人間の発言とも言えない**ので、どの区分に入れるかがこの周の不変条件から決まらない。PR の TODO に書く |

### 2 パス目の指摘の採否

Verifier 2 パス目（`sonnet`・`dev-loop:dev-loop-verifier`・`verifier-169-p2`）: **反証 0 件**。
**ただし再び「`Edit`／`Write`／`Agent` を持つ」と申告した**（1 パス目と同じ食い違い。密閉できていない側に倒す）。
報告の到着後に親が `git status --porcelain` を見て空を確かめた。
`/code-review` 2 パス目（high）: **指摘 10 件**。

| 指摘 | 実物で失敗を示せるか | 採否 | 対応・理由 |
| --- | --- | --- | --- |
| P2-1: 引き渡し（`<agent-message from="…">`）が関門の終わりにならない | 示せる（不変条件 D）。`c73a0b7a…` の `verify-89-pass4` は 12:07:59 起動・12:14:20 引き渡しなのに、23:47:15 の idle 通知で終わり 699 分と出る。親が 5317〜5329 行目で引き渡しの行を見た | 直す | `from=` の名前を報告した teammate として数え、`Agent` の `name` と一致したら終わりにする（Verifier の経路と同じ）。**`/code-review` の引き渡しは `from="code-review"` で、並行する複数のレビューを見分けられないので終わりに当てない** |
| P2-2: attachment（`queued_command`）で届く `<agent-message` が境界にならない（R2 の直し残し） | 示せる（不変条件 C）。同じ行（5329 行目）が attachment で、`elapsed_event` が `None` を返す | 直す | attachment の経路でも `<agent-message ` で始まる prompt を通知にする |
| P2-3: `uuid` で重複を除くとき、先に並んだファイル（パス順）が行を取るので、resume の隙間の区分が UUID の並びで変わる | 示せない（実データで該当する組を示していない） | TODO | PR の TODO に書く |
| P2-4: `result` を持つ idle 通知は、行の時刻ではなく本文の `timestamp` を使うべき | 示せる（同じセッションで行は 23:47:15・本文は 12:14:30） | 採らない | **行の時刻は親が受け取った時刻**で、関門は親が待った区間を測る。本文の時刻は送った時刻。どちらを採るかは §3.3 の「通知の行の時刻が、親に届いた時刻と一致するか」そのもの。**この例は P2-1 の修正で引き渡し（12:14:20）が先に終わりになるので、結果に効かない**。§3.3 に実例として足す |
| P2-5: `_notified` は task-notification を部分一致で当てるので、通知を引用した人間の発言が通知になる | 示せない（実物で該当する発言を示していない） | TODO | PR の TODO に書く |
| P2-6: 1 つの応答が複数行に分かれると、各行が最後の `stop_reason` を持つので、`end_turn` → 同じ応答の次の行が「その他」に入る | 示せる。親が `c73a0b7a…` で同じ `message.id` の連続で先の行が `tool_use` でないものを数えた: **19 組**（例: 06:09:41 の thinking 行 `end_turn` → 06:10:19） | 直す | 次の行が同じ `message.id` の assistant なら「止まった」と見なさない（モデルの時間に入れる） |
| P2-7: 範囲外の記録を (repo, Issue) で持つので、同じ Issue の古い周が範囲外だと、新しい完結した周まで「先頭が範囲外」になる | 示せない（仮定の例のみ） | TODO | PR の TODO に書く |
| P2-8: 関門の検出が `args["skill"]` しか見ず、`scan` が受ける `args["command"]` を見ない | 示せない（実データで 0 件） | TODO | PR の TODO に書く |
| P2-9: `id` の無い tool_use の関門が黙って落ちる | 示せない（仮定の例のみ） | TODO | PR の TODO に書く |
| P2-10: `--elapsed` が全リポジトリの境界を保持し、関門ごとに全行を走査する（性能） | 示せない（遅さで困った記録が無い） | 採らない | 実問題が起きてから直す（全走査は 11〜13 秒で済んでいる） |
| V2（Verifier の観察）: 「その他」の大きな塊は C1 以外にもある——401 のあと翌日に `system` 行経由で再開したセッションなど（`042a01df…` 926 分） | 示せる（ただし不変条件は破らない。`system` 行を境界にしないのは §3.2 の意図した設計） | TODO | C1 と合わせて PR の TODO に書く（「その他」は既知の穴の寄せ集めで、C1 の 1 件より多い） |

**直したあとの実データでの当て直し**（親が実行。番号で束ねた dev-loop の周、`--merge-worktrees`）:
区分の合計は HEAD・修正後とも **124,371 分で一致**（不変条件 A）。人間待ち 83,625 → 90,975、
その他 10,332 → 4,952、モデル 11,269 → 9,136。**モデルが減ったのは `stop_sequence` → assistant の
19 件・2,128 分がモデルから「その他」に移ったため**（API エラーの後の空白で、モデルの時間ではない）。
関門の分（レビュー 3,662・Verifier 5,528）と「報告なし 6」は変わらない。
**新しい変異 6 件の陽性対照**: 新しい試料（`s4`・`early1`・`early2`）を抜くと 5 件とも生き残る
（`isMeta の行を人間の発言と数える` は既存の変異の書き換えで、s1 の試料が殺す）。
