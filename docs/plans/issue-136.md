# #136 段 1・段 2 — 再開の探索をスクリプトにし、本文と委譲文を外に出す

**Issue**: [#136](https://github.com/hdknr/claude-code-setup/issues/136)（**4 段に割ってある。
この周は段 1 だけ**。残りは別の周で回す）

**新規の周である。** `docs/plans` / `.claude/plans` を再帰で探索、`git worktree list`、
`git branch --all --list '*136*'`、`git log --all --diff-filter=A -- '*issue-136*'`、
`git log --all --grep '#136'` の **5 本すべてが空振り**した。

## 周の在り処

- **ブランチ**: `issue/136-lazy-resume`
- **worktree**: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-136`
- **起点コミット**: `97cd52e`（`Merge pull request #139 from hdknr/issue/122-bake-back`）

## 0. 着手前に潰した前提（B が成立するか）

**`references/` に出した内容が、本当に起点から外れるのかを測った**（2026-09-20）。
**外れなければ、この周は 1 文字も減らさない**——むしろファイルが増えるぶん悪化する。

**測り方**: 小さな `SKILL.md` と、**印（`MARKER-…`）を 400 回並べた大きな
`references/big.md`** を持つ probe スキルを作り、`claude -p` で起動して
**トランスクリプトに印が現れるか**を数えた。**「ファイルを読むな」と指示しているので、
現れたとすればハーネスが載せたことになる。**

| 経路 | 参照ファイル | 印の出現 | 本文の出現 |
| --- | --- | --- | --- |
| **プロジェクト配下の素のスキル**（`.claude/skills/`） | 25,619 バイト | **0 回** | 1 回 |
| **プラグイン**（`--plugin-dir`） | 29,619 バイト | **0 回** | 1 回 |

**両経路とも、参照は載らない。** **`dev-loop` はプラグインとして配布され、
`link-skills.sh` で素のスキルとしても使われる**ので、**両方を測る必要があった。**

**確かめていないこと**: **これはハーネスの現在の挙動であって、約束ではない。**
**将来変わったら、この周の設計は黙って効かなくなる**（**検出手段は無い**——
CI は `claude` を起こせない）。**その旨を `CLAUDE.md` に残す。**

## 1. 変更範囲

触る:

- `docs/plans/issue-136.md` — この計画ファイル
- `plugins/dev-loop/skills/dev-loop/scripts/find-cycle.py` — 再開の周の探索。
  **1 本目の PR では `scripts/` に置いてしまい、マージ後に誤りと分かった**
  ——**`dev-loop` はどのリポジトリでも使うスキル**なので、対象リポジトリの `scripts/` に
  在ることを当てにできない。**スキルと一緒に配られる場所へ移す。**
- `scripts/test-find-cycle.py` — 上の回帰テスト（変異テストを含む）
- `scripts/check-all.py` — 上のテストを収録リストに足す
- `plugins/dev-loop/skills/dev-loop/references/resume.md` — 「途中から再開する周」の移設先
- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 当該節を薄い指し先に置き換える。版を上げる
- `plugins/dev-loop/.claude-plugin/plugin.json` — 版を揃える
- `.claude-plugin/marketplace.json` — 版を揃える
- `docs/plugins/dev-loop-design.md` — `skill-metrics.py` の生成ブロック（**手で書かない**）
- `CLAUDE.md` — `find-cycle.py` を一覧に載せ、遅延読み込みの前提と限界を書く
- `scripts/check-norm-markers.py` — **範囲を広げた**（下記 1）
- `scripts/test-check-norm-markers.py` — 同上
- `plugins/dev-loop/skills/dev-loop/prompts/verifier.md` — **段 2**。手順 5 の委譲文
- `plugins/dev-loop/skills/dev-loop/prompts/review-handoff.md` — **段 2**。手順 6 の委譲文
- `plugins/dev-loop/skills/dev-loop/scripts/prepare-worktree.sh` — **段 3**。新規の周の入場
- `plugins/dev-loop/skills/dev-loop/scripts/check-pr-preconditions.sh` — **段 3**。PR 前の前提条件
- `scripts/test-worktree-scripts.py` — **段 3**。上 2 本の回帰テスト（変異 8 件）
- `.github/workflows/plugins.yml` — **段 3**。CI に登録
- `plugins/dev-loop/skills/dev-loop/references/guard-rejections.md` — **段 4**。拒否されたときの対処
- `plugins/dev-loop/skills/dev-loop/references/untrusted-input.md` — **段 4**。疑う形と委譲先の数え方
- `scripts/test-skill-metrics.py` — **関門の反証 A**。`references/` / `prompts/` の正常系と変異
- `scripts/test-check-all.py` — **関門の反証 B・範囲を広げた**（下記）
- `scripts/check-skill-pointers.py` — **`/code-review` の指摘 10**。スキルの中の指し先を見る
- `scripts/test-check-skill-pointers.py` — 上の回帰テスト（変異 4 件）
- `plugins/dev-loop/skills/dev-loop/scripts/*.sh` — **`/code-review` の指摘 1・2 の実バグ修正**
- `plugins/dev-loop/skills/dev-loop/references/*.md` — **指摘 5・6・7**（指し先とリスト構造）
- `scripts/check-duplicate-counts.py` / `scripts/check-norm-markers.py` — **指摘 11・12**
- `docs/plans/issue-136-stage3.md` — **段 3** の計画（**この周から、段ごとに 1 本にする**）
- `scripts/skill-metrics.py` — **範囲を広げた**（下記 2）
- `scripts/test-skill-metrics.py` — 同上

**範囲を広げた 2 件と、その理由**（どちらも**移設に必然的に伴うもの**で、任意の改善ではない）:

1. **`check-norm-markers.py` の原本が 1 ファイル決め打ちだった。**
   `ORIGIN = "plugins/dev-loop/skills/dev-loop/SKILL.md"` なので、
   **`（必須）` を含む節を `references/` に移すと、この歯止めが「原本の外に漏れた」と誤って落ちる**
   （移す節は `（必須）` を **25 個**持っている）。
   **原本を「そのスキルのディレクトリ（`SKILL.md` ＋ `references/*.md`）」に一般化する。**
   **除外にするのではない**——除外にすると**移設先が無検査になり、歯止めが盲目になる。**
   なお**原本の中の重複は元から見ていない**（docstring の「守らない」にそう書いてある）ので、
   **一般化しても守備範囲は狭まらない。**

2. **`skill-metrics.py` が `SKILL.md` しか測らない。**
   このままだと、**節を移しただけで「178 行減った」と生成ブロックに出る**——
   **内容は 1 行も減っていないのに。** 設計 §8.2 は**この数字だけで「分割しない」を
   正当化している節**なので、**測るものが変わったことに気づけないまま論証が崩れる。**
   **`SKILL.md` と `references/` を分けて測り、「起点に載る分」と「遅延分」を区別する。**
   **これは #136 全体の前提（圧縮できたかを測る）そのものでもある。**

触らない:

- `diagrams/` — 図は 1 件も触らない
- `.github/workflows/` — CI の定義は変えない

**段 2〜4（委譲文の外出し・入場のスクリプト化・重複の集約）はこの周でやらない。**
**#122 が大きすぎて 3 パス目を回せなかったので、同じことを繰り返さない。**

### 段 1 は、さらに 2 本の PR に割る

**探索のスクリプトと、本文の移設は、独立にマージできる。** 割る理由は同じ
——**#122 は 1 本に詰めて 3 パス目を回せなかった。**

| PR | 含むもの | 含まないもの |
| --- | --- | --- |
| **1 本目**（[PR #140](https://github.com/hdknr/claude-code-setup/pull/140)・マージ済み） | `find-cycle.py` ＋ テスト ＋ `check-all.py` への登録 ＋ この計画ファイル | **`SKILL.md` を 1 文字も触らない**（版も上げない） |
| **2 本目**（この PR） | **スクリプトの置き場所の訂正**・`references/resume.md` への移設・`SKILL.md` の指し先化・`check-norm-markers.py` と `skill-metrics.py` の一般化・`CLAUDE.md` | — |

**1 本目には誤りがあった。** `find-cycle.py` を**このリポジトリの `scripts/` に置いた**が、
**`dev-loop` はどのリポジトリでも使うスキル**（`SKILL.md` 30 行目が「どのプロジェクトでも
使えるよう汎用化してある」と明記）なので、**他のリポジトリにそのパスは存在しない。**
**2 本目で `plugins/dev-loop/skills/dev-loop/scripts/` に移す。**

**1 本目は、手順 5 の Verifier も `/code-review` も回さずにマージした**（人間の判断）。
**「関門を通った」とは書けない。** **この置き場所の誤りは、関門を回していれば
見つかった可能性がある**——**が、それは確かめられない**（回していないので）。

**順序には理由がある**——**スクリプトが先でないと循環する**（§3.2 の P3）。
**2 本目の `SKILL.md` は「`find-cycle.py` を走らせろ」と書く**ので、
**1 本目が入っていない状態でその指示だけ届くと、存在しないスクリプトを指すことになる。**

## 2. 乗るデプロイ経路

`docs/` を含むので **GitHub Pages への反映経路を持つ**（`docs.yml`）。
`plugins/` を触るので `plugins.yml` のカタログ整合チェックにも掛かる。

## 3. verify の受入基準

### 3.1 不変条件

| # | 不変条件 |
| --- | --- |
| **I1** | **規範が 1 つも消えていない**——移設の前後で、`（必須）` の数と規範の一覧が一致する |
| **I2** | **起点が実際に減っている**——`SKILL.md` の文字数が減り、**参照は起点に載らない**（上記 0 で測った前提） |
| **I3** | **再開する周が、移設後も再開できる**——`SKILL.md` だけを読んだ状態から `references/resume.md` に到達できる |
| **I4** | `find-cycle.py` が、**5 本の探索経路すべて**を実行し、**当たったものを区別して出す** |
| **I5** | `find-cycle.py` が**「新規の周」と「見つからなかった」を区別する**——空振りを「新規」と即断しない |

### 3.2 これを破りうる経路（入口一覧から数える）

| # | 経路 | 守り方 |
| --- | --- | --- |
| **P1** | **移設のときに規範が落ちる**（「軽くするついでに落とす」） | **`（必須）` の数と規範の一覧を前後で機械照合する。** 目視しない |
| **P2** | **指し先に到達できない**——`SKILL.md` が `references/resume.md` を指しても、**読む条件が書かれていない** | **`find-cycle.py` の出力が読む先を決める**形にする。「再開かどうか」を人間の記憶に頼らせない |
| **P3** | **循環**——「自分が再開しているか」を知る前に、それを知る方法を読む必要がある | **スクリプトが先、遅延読み込みが後。** `SKILL.md` には**スクリプトを走らせる指示だけ**を残す |
| **P4** | **`find-cycle.py` が `-name` で探して `issue-136/plan.md` を取り逃す** | **`-path` の部分一致で探す。** `SKILL.md` が名指ししている既知の失敗（#96） |
| **P5** | **部分一致が広すぎる**——`*96*` が `issue-960` にも当たる | **当たったものを全部出し、選ばせる。** スクリプトが 1 つに決め打ちしない |
| **P6** | **リポジトリの外で走る**——相対パスなので cwd 依存 | **リポジトリの root を自分で解決する。** テストで別 cwd から走らせる |
| **P7** | **`references/` が将来ハーネスに載るようになる** | **検出手段が無い。** `CLAUDE.md` に前提として明記し、**約束ではないと書く** |
| **P8** | **移設した本文が、公開サイトのリンク検査から外れる** | **確かめた——入る。** `SKIP_DIRS` / `SKIP_PATHS` のどちらにも当たらず、`.md` は走査対象 |
| **P9** | **歯止めが移設先を見なくなる**（`check-norm-markers.py` の原本が 1 ファイル決め打ち） | **原本をスキルのディレクトリに一般化する。** 除外にしない——除外は歯止めを盲目にする |
| **P10** | **測定が「移しただけ」を「減った」と報告する**（`skill-metrics.py` が `SKILL.md` しか測らない） | **`SKILL.md` と `references/` を分けて測る。** 設計 §8.2 はこの数字だけで判断を正当化している |

### 3.3 この環境では証明できないもの（未証明）

| 項目 | なぜ原理的に証明できないか |
| --- | --- |
| **将来もハーネスが参照を載せないこと** | **現在の挙動しか測れない。** 約束されていない |
| **移設で「読まれなくなる」ことが起きないか** | **モデルが指し先を読むかどうかは実行時の判断**で、静的には決まらない |

### 3.4 BLOCKED（手段を用意すれば消える）

| 項目 | 何が無いか |
| --- | --- |
| **起点の実測（前後比較）** | **同じ Issue の周を移設前後で 1 本ずつ回さないと比べられない。** この周だけでは片側しか取れない |

**「軽くなった」を自己申告しない**（#92 の前例）。**取れるのは `SKILL.md` の文字数と、
参照が載らないという 0 節の実測まで**で、**周あたりの起点の前後比較は次の周に回す。**

### 3.5 生成物の鮮度

**該当あり。** `SKILL.md` を編集するので `skill-metrics.py` の生成ブロックが古くなる。
**再生成して `--check` が緑になること**を受入基準に含める。
図は触らないが、`check-all.py` で機械的に確かめる。

### 3.6 必須要件の掛け算

**「受入基準を両方の関門に渡す」×「2 パス回す」= 4 つの受け渡し。**
**渡すのは §3.1 と §3.2 の全体で、絞らない。**

## 4. 未解決の判断・確認待ち

1. ~~`check-norm-markers.py` の扱い~~ — **決めた。** **原本を「スキルのディレクトリ」に
   一般化する**（§1 の「範囲を広げた 1」）。**移す節は `（必須）` を 25 個持つ**ので、
   放置すると歯止めが誤って落ちる。
2. ~~`check-site-links.py` の対象に `references/` が入るか~~ — **入る。**
   `SKIP_DIRS` / `SKIP_PATHS` のどちらにも当たらず、拡張子 `.md` は走査対象である。**P8 は解消。**
3. **`skill-metrics.py` の測る対象**（§1 の「範囲を広げた 2」）。**決めた**が、
   **生成ブロックの表をどう見せるか**（1 表に混ぜるか、分けるか）は実装時に決める。

## 5. 関門の進捗（再開点）

**周の大きさ**: この周が触る規範は **1 箇所**（`SKILL.md` の「途中から再開する周」）。
**移設であって書き換えではない**ので、複製は増えない。**分ける理由は無い。**

**交絡を潰した手順**: 0 節のとおり。**probe を 2 経路で取り、印の出現数で判定した。**
**「載らない」という否定的な結果なので、本文が 1 回出ていることを陽性対照にしている**
——**本文も出なければ、スキル自体が起動していなかったことになる。**

| 関門 | 状態 |
| --- | --- |
| 手順 4 の実装 | **未着手** |
| 手順 5 の Verifier | **未着手** |
| `/code-review` 1 パス目 | **未着手** |
| 指摘対応 | **未着手** |
| 手順 5 の当て直し | **未着手** |
| `/code-review` 2 パス目 | **未着手** |

**移設前の実測**: `SKILL.md` **1,745 行 / 84,847 文字 / `（必須）` 168 個**。

**移設後の実測**（2026-09-20）:

| | 移設前 | 移設後 |
| --- | --- | --- |
| **起点に載る分**（`SKILL.md`） | 84,847 文字 / 1,745 行 | **76,632 文字 / 1,601 行**（**−8,215 文字 / −9.7%**） |
| 遅延分（`references/resume.md`） | — | 9,563 文字 / 188 行 |
| `（必須）` | 168 | **148 ＋ 25 = 173**（**指し先に新しい必須を足したので増えた**） |

**規範が落ちていないことは機械で確かめた**——**移設した本体が `references/resume.md` に
バイト一致で入っている**ことをアサートした。**目視していない。**

## 段 2 — 委譲文を `prompts/` に出す

### 見積もりが外れた（訂正）

**#136 には「A で手順 5・6 の散文が縮む」と書いたが、実測は支持しなかった。**

手順 5 の 307 行を塊ごとに分類すると、**委譲文の中身**に当たるのは約 **48 行**だけで、
残りは**著者の判断**である——`subagent_type` の選び方（49 行）・BLOCKED と未証明の
分類（50 行）・交絡（25 行）・生出力を積まない（約 40 行）・報告が返らなかったときの
扱い（13 行）。**これらはテンプレートに移せない。**

**実測: `SKILL.md` は 76,632 → 77,043 文字で、411 文字*増えた*。**
**段 2 は圧縮ではない。** 価値は別の 2 つである:

1. **1 周あたりの出力が減る**——実測で、**1 周のうちに同じ趣旨の 2,300 文字級の委譲文を
   6 回手書き**した（約 9,000 トークン）。**起点ではなく毎周の出力コスト**である。
2. **「渡したか」を機械で見る足場**——いまは散文の必須で、**#101 で「15 箇所に
   書いてあるのに発火 0 件」を踏んだのと同じ形**である。

**「軽くなった」と報告しない。**

### `prompts/` では必須マーカーを使わない

**あれは dev-loop の著者が守る規範の印**で、**`prompts/` はサブエージェントへの指示文**である。
混ぜると「どちらに向けた必須か」が読めなくなり、`check-norm-markers.py` の意味も濁る。
**一度テンプレートに 4 個書いて、歯止めが落ちたので外した。**

### 段 2 の実測

| | 段 1 後 | 段 2 後 |
| --- | --- | --- |
| 起点に載る分（`SKILL.md`） | 76,632 文字 | **77,043 文字**（**+411**） |
| 起点に載らない分 | 1 本・188 行 | **3 本・339 行** |
| `（必須）`（原本の合計） | 173 | 174 |

**歯止めの数え方が 2 件ずれていたのを直した**——`skill-metrics.py` はフェンスしか
剥がさないので、**テンプレート内でバッククォート付きに言及した印を数えていた**。
**言及のほうを書き換えて揃えた**（174 個で `check-norm-markers.py` と一致）。


## 段 3 — 入場をスクリプトにし、条件つきの塊を `references/` へ

**詳細は `docs/plans/issue-136-stage3.md` を正とする**（測定・受入基準・経路・実測）。
**ここには変更範囲だけを足してある**——`check-plan-scope.py` が拾うのは
`issue-<番号>.md` の側だからである。

**この食い違いで CI を 1 度割った**（PR #144）。**別名の計画ファイルを作ったら、
番号どおりの名前のほうに変更範囲を足すこと。**

**そして、`check-plan-scope.py` はコミットしてから走らせること**——
**コミット前に走らせると `origin/main...HEAD` に差分が無く、「判定していない」で
空振りする。** **空振りを緑と読まない。**


## 段 4 — 内容を入れ替えた（測って決めた）

**D（重複した規範を 1 箇所へ）はやめた。** 測ると割に合わない:

- `原則` は **3,645 文字（当時の 5.5%）**しかなく、**大半が既に 1〜4 行で指し先付き**。
- 得られるのは **2〜3%** の一方、**規範の書き換えという最も危険な操作**になる。
- **#122 はまさにこの型で 3 パス目を回せなかった。**

**#138 の歯止め（件数の食い違い）は入れた**ので、**D の目的だった「直し残しに気づけない」は
部分的に埋まっている。**

**代わりに、条件つきの塊がまだ 2 つ残っていた**（段 1・3 と同じ手が当たる）:

| 移したもの | 行 | 文字 | 読む条件 |
| --- | --- | --- | --- |
| 手順 6「走らせる形は、素の 1 行に保つ」→ `references/guard-rejections.md` | 101 | 5,106 | **ガードに拒否されたときだけ** |
| `原則`「信頼できない入力」→ `references/untrusted-input.md` | 39 | 1,880 | **外部入力を読むときと、委譲するとき** |

### 通算の実測（段 1 の前 → 段 4 後）

| | 段 1 の前 | 段 4 後 |
| --- | --- | --- |
| **起点に載る分**（`SKILL.md`） | 84,847 文字 / 1,745 行 | **60,045 文字 / 1,305 行**（**−24,802 / −29.2%**） |
| 起点に載らない分 | 0 | **5 本・31,857 文字** |
| `（必須）` | 168 | **118 ＋ 参照 62 = 180** |

**一度ここに「119 ＋ 62 = 181」と書いて、関門に反証された。**
**素の `grep` で数えており、`SKILL.md` のフェンス内にあるコード例の
`（必須）` を 1 個余計に数えていた**——**機械（`check-norm-markers.py` と
`skill-metrics.py`）はどちらも 180 で一致していた。** **自己申告のほうが誤っていた。**

**規範は 1 つも落ちていない**——**各段で、移した本体が移設先に 1 行残らず入っていることを
機械でアサートした**（見つからない 1 行は、毎回**節見出しに変換した元の行そのもの**）。
**`（必須）` が増えているのは、指し先に新しい必須を足したからである。**

### 残していること

- **C（経緯の外出し）と F（検査へ移す）はやっていない。** **C は設計 §2 が
  「なぜを消すと規範は守られない」を実測しており、最も危険**である。
  **F は `SKILL.md` に移せる分がほとんど無い**（該当する規範は `CLAUDE.md` 側にある）。
- **周あたりの起点の前後比較は BLOCKED のまま**（同じ Issue の周を前後で 1 本ずつ回さないと
  比べられない）。**「軽くなった」を自己申告しない。**


## 関門（全段まとめて掛けた）

**PR #140〜#146 は、どれも手順 5 / 手順 6 を回さずにマージした**（人間の判断で、
**全段のあとにまとめて掛ける**ことにした）。**だから「関門を通った差分」は 1 行も無かった。**

**対象**: `git diff cb76f01 origin/main`（25 ファイル / +2,144 −549、コミット 11 本）。
**受入基準は 3 つの計画ファイルから 1 つにまとめて渡した**（不変条件 7 件・経路 18 件）。

### 手順 5 の Verifier — 反証 2 件＋数値誤り 1 件

**A（重大）: `skill-metrics.py` の新ロジックが無テストだった。**
`LAZY_DIRS` / `lazy_files()`（約 58 行）は **P10「測定が『移しただけ』を『減った』と
報告する」を防ぐための機構**なのに、**`test-skill-metrics.py` は 1 バイトも変わっておらず**、
偽リポジトリが `references/` も `prompts/` も作らないので
**常に空リストを返す経路しかテストされていなかった。**
**同じ #136 で `check-norm-markers.py` の同種の一般化には変異テストを足していた**
——**片方だけ判別力を持たせていた。**

**B: CI に登録し忘れたテストが 2 本あった。**
`test-find-cycle.py`（#136 段 1）と `test-collect-guard-rejections.py`（#122）は
`check-all.py` には在るが、**`.github/workflows/` のどちらにも個別ステップが無く、
GitHub Actions では 1 度も走っていなかった。**
**`check-all.py` は CI から呼ばない設計**（`CLAUDE.md` のとおり）なので、
**収録と CI 登録は別の作業**であり、**片方だけ済ませると手元でしか走らない。**

**C（軽微）: 計画の自己申告が 1 個ずれていた。** 「`（必須）` 119 ＋ 62 = 181」と
書いていたが、**機械（`check-norm-markers.py` と `skill-metrics.py`）はどちらも 180**。
**素の `grep` でフェンス内のコード例を 1 個余計に数えていた。**

### 直した内容

| 反証 | 直したもの |
| --- | --- |
| A | `test-skill-metrics.py` に**正常系 6 件と変異 3 件**を足した（参照を `SKILL.md` と混ぜる／`prompts` を数えない／参照のフェンスを剥がさない）。**試料に `references/` と `prompts/` を作り、フェンスも入れた**——**入れないと「フェンスを剥がさない」変異が殺せない** |
| B | CI に 2 本を登録。**あわせて `test-check-all.py` に「`check-all.py` に在るのに CI に無い」の検査を足した**——**今回の盲点そのもの。1 本外して落ちることまで確かめた** |
| C | 表の数値を **118 ＋ 62 = 180** に直し、**誤った理由（素の `grep`）も書いた** |

### 範囲を広げた 1 件（`test-check-all.py`）と、その理由

**反証 B は「CI に 2 本を登録する」だけでも閉じる。** だが**それは同じ穴を残す**
——**次に足すテストでまた漏れる。** **実際、2 本のうち 1 本は #122 の周から
漏れていた**（気づかれないまま **6 本の PR** を跨いだ）。

**既存の収録漏れ検査は逆向きしか見ていない**（`scripts/` に在るのに `check-all.py` に
無い）。**「`check-all.py` に在るのに CI に無い」は、この周が初めて踏んだ形**である。

**受入基準も広げた**——**I8: 収録したものは CI でも走る**。
**検証は、1 本を CI から外すと実際に落ちることまで確かめた**（目視していない）。

**これは手順 4 の「範囲を広げたら受入基準も広げる」に従っている**——
**そして `check-plan-scope.py` が、書く前にコミットしようとしたのを捕まえた。**


### `/code-review` — 指摘 13 件（全件実在）

**HIGH 2 件は、出荷済みスクリプトの実バグだった。** どちらも**再現して直した**:

1. **`check-pr-preconditions.sh` が、メインの作業ツリーのサブディレクトリから rc=0 を返す。**
   `--git-dir` は絶対、`--git-common-dir` は**相対**（`../../.git`）で返るため、
   文字列比較が必ず不一致になる。**この関門がいちばん止めたい状況で素通りしていた。**
   **`Bash` の cwd は呼び出し間で持続する**ので、**直前の `cd docs` だけで起きる。**
2. **`prepare-worktree.sh` が、リポジトリの外に worktree を作ろうとする。**
   相対の `--git-common-dir` を root 起点で解決していたため、**サブディレクトリから
   走らせると `MAIN` がリポジトリの*親*になる**（実リポジトリなら `~/Projects/hdknr/`）。

**どちらも `--path-format=absolute` で直した。**

**なぜ出荷できたか**——**テストがトップレベルでしか走らせていなかった。**
**サブディレクトリからの観測を足した**（変異 11 件に増えた）。

**残る 11 件も全件直した**——指し先の迷子（5・6）、リスト構造の崩れ（7）、
`CLAUDE.md` の自己矛盾（8・9）、免除がフェンスを剥がしていない（11）、
歯止めと測定の範囲の食い違い（12）、終了コードの記載漏れ（13）、
そして**指し先を見る歯止めが無い**（10）。

**10 に対しては `check-skill-pointers.py` を置いた**——**置いた瞬間に実物を 1 件捕まえた。**

### この回で分かった、検証の限界

**`/code-review` が明示してくれた**: **「移した本体が 1 行残らず在る」は*行の集合*の
検査なので、*構造や親子関係*の欠落は検出できない。** 指摘 7 がまさにそれで、
**すべての行が生き残ったのに、リストの入れ子が壊れていた。**
**「1 行残らず在る」は真だが、「規範が 1 つも消えていない」の完全な証明ではない。**
