# Issue #112 — worktree 隔離セッションのガードの境界を特徴づける

## 0. この周が答える問い

Issue #112 の「進め方の提案」が先に置いた問いに従う:

> **境界を当てにいく前に、当てなくて済む運用で足りているかを先に見る。**
> #110 で残した最小形が実際に回るなら、**特徴づけは要らない**かもしれない。**要ると分かってから測る。**

したがってこの周の成果は、**境界の特徴づけそのものではない**。
**(a) 最小形で足りているかの判定**と、**(b) 足りていない箇所の修正**である。

**#112 が 5 度壊れた型（測った形の集合から境界の全体を述べる）を、この周で再現しない。**
数値には必ず**観測窓**（件数・期間・環境）を添え、
**「〇〇は N 種類である」ではなく「N 件の観測で N 種類だった」**と書く。

## 1. 変更範囲

**触る:**

- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 手順 6「PR 作成の前提条件」の
  **拒否されたときの対処**の記述（**規範の原本**）
- `plugins/dev-loop/.claude-plugin/plugin.json` — `version`
- `.claude-plugin/marketplace.json` — `dev-loop` の `version`
- `docs/plugins/dev-loop-design.md` — **§8.2 の生成ブロックのみ**（`skill-metrics.py` が書く。手で触らない）
- `docs/plans/issue-112.md` — この計画ファイル
- Issue #112 — 棚卸しの結果をコメントで投稿（**リポジトリ外の「正」**）

**触らないと決めた範囲:**

- `plugins/dev-loop/README.md` / `docs/plugins/index.md` / `docs/plugins/dev-loop-design.md` の本文 /
  `CLAUDE.md` — **ガードの話はここに複製されていない**（grep で確認済み）。
  **列挙を増やさない**ので、今回も書かない。
- `scripts/` — **棚卸し用のスクリプトをリポジトリに足さない**。
  このリポジトリの慣習では 1 本足すごとに `test-*.py` と `CLAUDE.md` の一覧が付いてくる（複製が増える）。
  棚卸しは 1 回性の測定なので、**再現用のスクリプトは #112 のコメントに本文ごと残す**。
  常設が要ると分かったら別 Issue に起票する（手順 8）。
- `diagrams/` — 図は触らない。

**周の大きさ（同じことを述べている箇所の数）: 1。**
ガードの対処を述べているのは `SKILL.md` の 1 箇所だけで、観測・経緯は #112 を正としている。
**複製が 1 なので分けない。**

## 2. 乗るデプロイ経路

- `SKILL.md` / `plugin.json` / `marketplace.json` → **プラグインのカタログ**。
  main へのマージで配布物が変わる（利用者側は `claude plugin update` が要る）。
- `docs/` → **main への push で GitHub Pages が自動デプロイ**。
  **`docs/` の変更はそれ自体がデプロイである**（手順 7 で「反映経路なし」と書いてはならない）。

## 3. verify の受入基準

### 3-a. 検証で証明できるもの

**I1 — 運用指示が境界に依存しない。**
未知の拒否理由が出ても成り立つ形で書かれていること。
特定の対処（「割る」）を**唯一の手として名指ししない**。

| 経路（この不変条件を破りうる入口） | どう守るか |
| --- | --- |
| P1 手順 6「拒否されたら…」の項（原本） | 対処は「**メッセージ末尾の対処文が指示するとおりにする**」と書く |
| P2 手順 6「守る形 `（必須）`」の項 | 予防側は残す（**特定の拒否理由に依存していない**ことを確認する） |
| P3 「途中から再開する周」の `git -C` の項 | **個別の観測**としては正しい。境界の断定に読めないか確認する |
| P4 手順 4 内のガード言及 | 同上（grep で全数を当たる） |

**I2 — 規範の原本は `SKILL.md` だけ、観測と経緯は #112。**

| 経路 | どう守るか |
| --- | --- |
| P5 `plugins/dev-loop/README.md` | 列挙を足さない（現状も持っていない） |
| P6 `docs/plugins/index.md` | 同上 |
| P7 `docs/plugins/dev-loop-design.md` 本文 | 同上 |
| P8 `CLAUDE.md` | 同上 |
| P9 `SKILL.md` | **理由の一覧・対処の一覧・件数を書かない**。#112 を正とする |
| P10 この計画ファイル | 実績として書いてよいが、**規範として書かない** |
| P11 `scripts/check-norm-markers.py` | `（必須）`が原本の外に出ていないこと（非ゼロ終了しない） |

**I3 — 版が 3 箇所で一致する。**

| 経路 | どう守るか |
| --- | --- |
| P12 `.claude-plugin/marketplace.json` | `1.17.0` → `1.18.0` |
| P13 `plugins/dev-loop/.claude-plugin/plugin.json` | 同じ値 |
| P14 `SKILL.md` の 2 行 1 組（コメント印＋可視テキスト） | 同じ値。**両方**直す |
| P15 `scripts/check-plugin-versions.py` | 緑 |
| P16 `scripts/check-version-bump.py origin/main` | 緑（中身を変えて版を据え置いていない） |

刻み方: **minor**。当初は patch と書いたが、**`/code-review` 1 パス目の指摘で直した**——
この周は**記述を直しただけでなく、新しい `（必須）` の項を足している**
（「別の道具に移せば通る、ではない」「メッセージの形が 1 つだと思わない」
「ファイル編集が拒否される形は原因が別」）。
`CLAUDE.md` の表は**能力・手順の追加を minor** としているので、patch は低すぎる。

**I4 — 生成物の鮮度。**

**該当判定の結果: 該当あり（1 件）／該当なし（1 件）。**

| 経路 | 判定 | どう守るか |
| --- | --- | --- |
| P17 `docs/plugins/dev-loop-design.md` §8.2 の生成ブロック | **該当あり** — `SKILL.md` を編集すると黙って古くなる | `python3 scripts/skill-metrics.py` で作り直し、`--check` が緑 |
| P18 `diagrams/` の書き出し | **該当なし** — drawio を 1 件も触らない | `scripts/check-diagram-freshness.py` が緑のままであることを確認 |

**I5 — 述べる主張が境界の断定を含まない。**

| 経路 | どう守るか |
| --- | --- |
| P19 `SKILL.md` に足す文 | 件数・一覧を書かない。「末尾の対処文に従う」だけ |
| P20 #112 へのコメント | 全数値に**観測窓**（件数・期間・環境・1 回性）を添える |
| P21 この計画ファイル | 同上 |
| P22 PR 本文・PR コメント | 同上。**未証明と BLOCKED を別の見出しで書く** |

**I6 — `description` の同期。**

| 経路 | 判定 |
| --- | --- |
| P23 `marketplace.json` / `plugin.json` / `SKILL.md` frontmatter の `description` | **今回は変えない**（ガードの対処は要約に載っていない）。`check-description-sync.py` が緑 |

**I7 — 公開サイトへの絶対リンクが解決する。**

| 経路 | どう守るか |
| --- | --- |
| P24 追記に入れるリンク | `scripts/check-site-links.py` が緑 |

**I8 — 棚卸しの数字が再現する。**

| 経路 | どう守るか |
| --- | --- |
| P25 抽出（何を「拒否イベント」と数えるか） | **引用を読んだだけの `tool_result` を除く判別条件**を持つこと（下記の交絡） |
| P26 集計（理由・規則・対処の分解） | 規則文が全件で一致していることを確認してから分解する |
| P27 復帰コスト | 陽性側（長い連続）が**出うる**測り方であることを示す |

### 3-b. この環境では証明できないもの（未証明 — 手順 6 で人間レビューに回す）

- **U1: 観測した対処文が「全部」かは、原理的に確かめられない。**
  理由節と同じく、対処文も生成文である可能性がある。
  **だから `SKILL.md` に件数も一覧も書かない**——この未証明を、**書かないことで回避する**設計にする。
- **U2: 他の harness 版・他の OS では測っていない。** 手元のトランスクリプトは 1 環境のもの。
  #112 の未解決 3 はそのまま残る。
- **U3: 「次の `Bash` が通った」は「目的を達成した」ではない。**
  連続拒否の長さが測るのは**拒否が連続した回数**で、
  **諦めて別のことをした**場合と区別できない。**復帰の成否そのものは測れていない。**
- **U4: 拒否率の分母。** 全セッションを分母にすると**ガードが効かないセッションも入る**（下限寄り）。
  拒否のあったセッションだけに絞ると**拒否があることで選んでいる**（上限寄り）。
  **2 つの見積もりを両方出し、真値を 1 つに書かない。**

### 3-c. BLOCKED（手段が無くて回せないもの）

**現時点で無し。** U1〜U4 はいずれも**環境が整えば消える**類ではなく、
**原理的な限界（未証明）**なので、BLOCKED には分類しない。

## 3-d. 周の途中で広げた範囲（黙って広げない）

**広げた: `SKILL.md` に「対処が、対処になっていないことがある」の項を足した。**

- **理由**: Issue にコメントを投稿しようとして拒否され、**対処文が
  `Run the plain command from <PATH>` だったが、それは既に素のコマンドだった**
  （2026-09-19 の実測）。**言われたとおりにしようがない形が実在する**ので、
  「対処文のとおりにする」だけでは**読者がそこで詰まる**。
- **触るファイル**: `plugins/dev-loop/skills/dev-loop/SKILL.md`（同じ節の中。**新しいファイルは増えない**）。
- **受入基準の追加**: **I1（境界に依存しない）に経路 P28 を足す**——
  「対処が実行不能なときの逃げ道が、**特定の拒否理由に依存していない**こと」。
  守り方: **逃げ道を「そのコマンドを走らせるのをやめる」という一般形で書き、
  どの理由のときにそうなるかを書かない。**
- **既存の項目は消していない。**

## 4. 未解決の判断・確認待ち

- **#112 を閉じるか、開けたままにするか。** この周が答えるのは
  「**運用には特徴づけが要らない**」までで、**境界そのものは依然として未特徴づけ**である。
  → **人間の判断を仰ぐ**（手順 6 の PR コメント）。既定は「開けたまま、判定の結果をコメントで残す」。
- **棚卸しスクリプトを常設するか。** 今回は足さない（上記「触らない」）。

## 5. 関門の進捗（再開点）

- **周の在り処**
    - ブランチ: `issue/112-guard-remedy`
    - worktree: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-112`
    - 起点コミット: `165dd48`
- **どの関門を、どのパスまで通したか**
    - 手順 4（実装）: **完了**。**検査の結果は、その時点の tip に対するもの**なので、
      **コミットを固定して書かない**——**最後に走らせたのは指摘対応後**であり、
      下の「指摘対応」の行を正とする。
      **途中で `check-norm-markers` が 1 件落ちた**——この計画ファイルが
      `（必須）` を原本の外に漏らしていた。バッククォートで囲んで直し、再実行で緑。
    - 手順 5 の Verifier: **起動済み・報告待ち**（`sonnet`・別ティア）。**済んでいない。**
    - `/code-review` 1 パス目: **通過。反証 11 件**（HIGH 4・MEDIUM 4・LOW 3）。
    - 指摘対応: **完了。11 件すべてに対応**（下記の差し戻しを参照）。
      対応後に機械的な検査を**走らせ直して全件緑**——
      `check-plugin-versions` / `check-norm-markers` / `check-site-links` /
      `skill-metrics --check`。
      **`check-version-bump` / `check-description-sync` / `check-diagram-freshness` と
      `test-*.py` 9 本は、1 パス目の前に緑を確認したのが最後**
      （**当て直しでここを走らせる**）。
    - 手順 5 の当て直し: **未着手**
    - `/code-review` 2 パス目: **未着手**
- **手順 5 で挙がった反証と、手順 4 への差し戻しの履歴**
    - **`/code-review` 1 パス目 → 手順 4 へ差し戻し（2026-09-19）。**
      **もっとも重い 4 件は、この周の測り方そのものへの反証**だった:
      **`tool_result` が `<tool_use_error>` に包まれる場合を落としており**、
      そのせいで**ガードの第 2 のメッセージ形（`Edit` / `Write` の拒否）が
      母集団に入っていなかった**。
      その結果、**`SKILL.md` に「ファイル編集ツールが拒否された観測は無い」と
      事実に反することを書いていた**——**反例はこのリポジトリ自身の
      issue-110 の worktree のトランスクリプトにあった。**
      **著者は指摘を写さず、自分で再現してから直した**（#112 の「関門の報告も自分で再現する」）。
    - 直した内容: 抽出条件（包みを剥がす）／`SKILL.md` の**ファイル編集ツールの項**／
      **メッセージの形が 1 つだと思わない**の項の追加／**対処文に無い `cd` の例を削除**／
      **`-C` を「外を指したから」と読ませる記述**（`SKILL.md` の再開の節）／
      **禁止形を指し先形に**／**観測窓を持たない因果の断定を削除**／
      **版を patch から minor に**／**計画の再開点をコミットに固定しない**／
      **陽性対照が安定した土台ではないことの明記**。
- **手順 4 の割り目**: 人間に `AskUserQuestion` で提示済み。**選択は下に書き戻す。**
    - 選択: **「このまま続ける」**（2026-09-19）。割らずに手順 5 へ進んだ。
- **交絡を潰した手順**: **実験あり。** 下に記録する。

### 交絡を潰した手順（手順 5 の規定に対する実績）

**候補 1（実際に踏んだ・1 度目は潰しきれなかった）: 何を「拒否イベント」と数えるか。**

- **1 度目**: `Refusing to run it …` を含む `tool_result` を数えていた。
  この文は **Issue #112 の本文と `SKILL.md` が引用している**ので、
  **それを読んだ `gh issue view` / `Read` の結果まで拒否イベントに入っていた**
  （**主張が偽でも同じ出力が出る配置**）。
  **本文が `This session is isolated in the worktree ` で始まる**ことを判別条件にして、
  850 → 841 件にした。
- **これは偽陽性しか潰していなかった。`/code-review` 1 パス目が反証した。**
  **`tool_result` は `<tool_use_error>` で包まれることがあり**、包まれたものは
  **「始まる」の判定に落ちる**。**包みを外すと 850 件になり、増えた 5 件は
  `Edit` 4・`Write` 1 だった**——**引用ではなく、本物の拒否**である。
  **除いた 9 件を「すべて引用を読んだもの」と書いたのは誤りだった。**
- **潰し方（2 度目）**: `<tool_use_error>` を剥がしてから前方一致を見る。
  **結果**: **850 件**（`Bash` 819・`Monitor` 26・`Edit` 4・`Write` 1）。

**候補 2: 復帰コストの測り方が、構造的に「1」に貼り付いていないか。**
**潰し方（陽性対照）**: 同じ測り方で**長さ 2・長さ 3 が出た**。
**測り方は長い連続を出しうる**ので、「大半が 1 回」は測り方の産物ではない。

**候補 3（潰せていなかった）: 規則文で分解してよいか。**
理由節と対処文は「規則文の前後」で切り出している。
**1 度目は「規則文が 841/841 で逐語一致」を根拠にしたが、これは循環していた**
——**選別条件が同じテンプレートの前方一致**なので、
**別のテンプレートの拒否は、そもそも母集団に入ってこない**。
**100% は、規則文が不変であることではなく、選別が 1 つのテンプレートを切り出したことを見ていた。**
**実際、包みを外して現れた `Edit` / `Write` の 5 件は規則文を持たない**
（本文は `Edit the worktree copy of this file instead of the shared-checkout path.` の 1 文）。
**訂正後**: 規則文を持つのは **845/850**。
**「規則文は全件で同一」が言えるのは、規則文を持つ族の中だけ**である。

**潰せなかったもの**: U3（復帰の成否そのもの）・U4（分母）・**U5（下記）**。

**U5: 測っている対象が、測っている最中に増える。**
**この周自身の拒否がトランスクリプトに入る**（実際に `Bash` の件数が周の途中で増えた）。
**件数は「いつ数えたか」に依存する**ので、**日付を添えずに件数を書かない。**

### 周中の観測（この周自身がガードに当たった）

**この計画ファイルを書く `mkdir -p … && cat > … <<'EOF'` が拒否された**（2026-09-19）。
理由節は `is too complex to verify that it stays inside the worktree.`、
対処文は `Split it into plain, separate commands and run them from <PATH>`。
実際の書き込みは、**長いヒアドキュメントを送り直す文脈コストを避けて**
`SKILL.md` の既存の規定（ファイル編集がガードに当たるならファイル編集ツールで行う）に従った。

**そのうえで、対処文が正しかったかを 1 次元だけ振って確かめた**（2026-09-19・各形 1 回・この 1 環境）:

| 形 | 結果 |
| --- | --- |
| `cat > <リテラルなパス> <<'EOF'`（本文に `git` を複数回）+ `echo` | **通る**（陽性対照） |
| **同じ本文**に `mkdir -p docs/plans && ` を**前置しただけ** | **拒否**（理由節・対処文とも上と同一） |

**振った次元は複合の有無 1 つだけ**なので、**この対では**対処文（割る）が正しかったと言える。

**ただしこれを「複合が効いている」と読んではならない。**
#110 の実測では**複合単独（`git` あり・なし）は通っている**。
**この対が示すのは、複合とヒアドキュメントの組み合わせで拒否が出たこと**までで、
**どちらが効いたかは判別していない**。**境界の主張として書かない。**

**さらに、陽性側は安定した土台ではない（`/code-review` 1 パス目の指摘）。**
**複合でも `git` を含むのでもないヒアドキュメントが、別の場面で拒否された**
（レビュー側の実測、および**この周でも 2 度目の書き込みで拒否された**）。
**2 つの対は書き込み先も本文も違う**ので、**上の 1 次元の比較は対の中でだけ成り立つ**。
**「この形は通る」と読んではならない。**
