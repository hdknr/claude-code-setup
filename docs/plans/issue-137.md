# #137 — 「拒否 870 件」が再現しない理由

**Issue**: [#137](https://github.com/hdknr/claude-code-setup/issues/137)

**新規の周である。** `find-cycle.py` の 5 本（計画ファイル・worktree・ブランチ・
計画ファイルを足したコミット・`#137` を含むコミットメッセージ）が**すべて空振り**した
（exit=0）。

## 周の在り処

- **ブランチ**: `issue/137-sidechain-discrepancy`
- **worktree**: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-137`
- **起点コミット**: `5c90ca9`（`Merge pull request #154 from hdknr/issue/122-close`）
- **切った元の参照**: `origin/main`（`refs/remotes/origin/HEAD` から解決。取り直し済み）
- **base（関門に渡す SHA）**: `5c90ca92f5585663dad595b52164fe2c638741d8`

## 0. 手順 2 で原因を特定した

**先に結論**——**食い違いの主因は母集団の違いである。**
**PR #133 が数えたのは main-chain のレコードだけで、
`collect-guard-rejections.py` はサブエージェントのトランスクリプト（`isSidechain: true`）も
数えている。** **#133 がどの手段で落としたかは特定していない**（§3.3）——
**言えるのは「落ちていたのは sidechain の側である」までである。**

### 再現

母集団は `~/.claude/projects`（2026-09-21 時点で 6,003 ファイル）。

| 数え方 | 件数 |
| --- | --- |
| 現行の定義（全件） | 1,496 |
| 現行の定義（`timestamp < 2026-09-20T04:00Z`） | 1,386 |
| **`isSidechain` を除く（同じ時刻カット）** | **872** |
| `isSidechain` のみ | 514 |
| **main-chain を時刻順に先頭 870 件**（最新が `2026-09-20T03:49:33`） | **870** |

**PR #133 のマージは `2026-09-20T04:17:01Z`** なので、**03:49 までの main-chain 870 件**は
**#133 を書いていた時点の値として整合する。**

### 交絡を潰した手順 — 陽性対照と陰性対照

**件数が合うだけでは偶然と区別できない。** **#133 が本文に載せた理由節の表**
（件数と、その理由節が出た版の数）を、**同じ 870 件に対して数え直した**:

| #133 が報告した理由節 | #133 の件数 / 版数 | main-chain 870 件 | 全件（`< 04:00Z`・1,386 件） |
| --- | --- | --- | --- |
| `too complex to verify that it stays inside the worktree` | 324 / 22 | **324 / 22** ✓ | 553 / 23 ✗ |
| `… ; break it into plain, separate commands` | 167 / 7 | **167 / 7** ✓ | 178 / 7 ✗ |
| `names git in a form too complex …` | 138 / 16 | **138 / 16** ✓ | 232 / 16 ✗ |
| `feeds python text naming git in a plain command …` | 21 / 6 | **21 / 6** ✓ | 32 / 7 ✗ |
| `redirects git to the shared checkout via -C` | 10 / 7 | **10 / 7** ✓ | 16 / 7 ✗ |

**陰性対照の列は、上の表と同じカット（`< 2026-09-20T04:00Z`）で数えている。**
**一度ここに 1 行目を `551 / 23` と書いていたが、それは別のカット**——
**main-chain 870 件目の時刻（`03:49:33.205Z`）までで切った 1,384 件**——**の値だった**
（2 パス目のレビューと Verifier が両方とも指摘し、**カットを振って再現を確かめた**:
`< 04:00Z` で **553**、`≤ 03:49:33.205Z` で **551**。**残る 4 行はどちらでも同じ**）。
**1 行だけが黙って別の母集団から来ていた**——**#137 が直そうとしている食い違いと同じ形**である。
**結論は動かない**（553 でも 551 でも 324 とは一致しない）。

**5 行すべてが、件数も版数も完全に一致した。** **陰性対照（全件）はどの行も一致しない。**
**版の種類も 31 種（`2.1.226` 〜 `2.1.278`）で #133 の記述と一致する**——
**ただしこれは判別力を持たない**（全件でも 31 種になる）ので、**単独では根拠にならない。**

**観測窓**: `~/.claude/projects` 配下の `*.jsonl` 全件（6,003 ファイル）。
**`cwd` は全件 macOS**（`isSidechain` 別に見ても main 954 / sidechain 542 で、
どちらも macOS のみ）。**Linux（コンテナ）の記録はこの置き場所には無い**
——`measure-guard-os.sh` は出力先ディレクトリに書くため。

### 特定できなかった残差 — 理由節の種類

**#133 は「パスを伏せてなお 106 通り、うち 62 通りは 1 件」としているが、これは再現しない。**

| 切り方 | 種類 / 1 件のみ |
| --- | --- |
| 現行の `reason_clause`（`". "` で切る）・main-chain 870 件 | 134 / 97 |
| **文末を `"."`（ピリオド単独）で切る・同じ 870 件** | **108 / 67** |
| #133 が報告した値 | 106 / 62 |

**探索した範囲**——バッククォート正規化・引用符正規化・数字正規化・末尾記号の除去の
**4 次元 16 通り**、および**パス正規表現の 4 変種**、さらに**母集団サイズ 700〜954 の全域**。
**どの組み合わせでも (106, 62) にならなかった。**

**だから残差は「特定できない」と確定させる。**
**#133 の測定コードは残っていない**（Issue #137 の記述のとおり周の scratchpad は消えている）
ので、**これ以上は復元できない。**

### どちらが「正しい」か

**どちらも誤りではない。別の母集団を数えている。**

- **870** = **main-chain のみ**。#123 が 40 件を無作為抽出したのもこの母集団である。
- **1,496**（現行） = **サブエージェントを含む全件**。

**既定は現行のまま（全件）を採る。** ガードは**サブエージェントのコマンドにも発火する**ので、
**「ガードが何回発火したか」を数えるなら sidechain を落としてはならない。**
**#122 の OS 比較もこの定義で取っている**ので、**既定を変えると過去の測定と比較できなくなる。**

## 1. 変更範囲

触る:

- `docs/plans/issue-137.md` — この計画ファイル
- `scripts/collect-guard-rejections.py` — docstring の訂正と、main/sidechain の内訳の出力
- `scripts/test-collect-guard-rejections.py` — 回帰テスト（変異テストを含む）
- `plugins/dev-loop/skills/dev-loop/references/guard-rejections.md` — 「理由は特定できていない」の訂正
- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 版のバナー（2 行 1 組）
- `plugins/dev-loop/.claude-plugin/plugin.json` — version
- `.claude-plugin/marketplace.json` — version
- `docs/plugins/dev-loop-design.md` — `skill-metrics.py` の生成ブロック（**手で書かない**）

触らない:

- `docs/plans/issue-122.md` — **過去の周の記録である。** 当時そう書いたことは事実なので
  遡って書き換えない
- `diagrams/` — 図は触らない
- `scripts/measure-guard-os*.sh` — 測り直さない（この周は数え方の話で、測定はしない）

## 2. 乗るデプロイ経路

`docs/` を含むので **GitHub Pages への反映経路を持つ**（`docs.yml`）。
`plugins/` を触るので **version bump が要る**（`marketplace.json` / `plugin.json` /
`SKILL.md` の 3 箇所）。刻みは **patch**——**記述の訂正**であって、能力も手順も増えない。

## 3. verify の受入基準

### 3.1 不変条件

| # | 不変条件 |
| --- | --- |
| **I1** | **870 の再現が、機械で当て直せる形で残っている**——散文だけにしない。出力に main/sidechain の内訳が出る |
| **I2** | **docstring が、特定できた主因と、特定できなかった残差を分けて書いている**——混ぜない |
| **I3** | **「理由は特定できていない」と書いている箇所が、リポジトリ中に 1 つも残っていない**（`docs/plans/issue-122.md` を除く。過去の記録） |
| **I4** | **既定の件数が変わらない**——`isSidechain` を既定で落とさない。落とすと #122 の測定と比較できなくなる |
| **I5** | **「870 が誤り」「1,388 が誤り」と書いていない**——どちらも別の母集団の正しい数である |
| **I6** | **残差（106 / 62）を「特定した」に混ぜていない**——探索した範囲まで書く |
| **I7** | **版が 3 箇所で揃っている**（`marketplace.json` / `plugin.json` / `SKILL.md`） |
| **I8** | **テストが実環境を対象にしない**——既存のアサートと同じ形で担保する |

### 3.2 これを破りうる経路

**入口は「この主張を読む／数える場所」の一覧から数える**（差分からではない）:

| # | 経路 | 守り方 |
| --- | --- | --- |
| **P1** | **docstring だけ直して `references/guard-rejections.md` の「特定できていない」が残る** | **I3 を全文 grep で確かめる。** #137 の食い違いは **2 箇所**に書かれている |
| **P2** | **内訳を出すつもりで既定の母集団を変える** | **I4。** 既存テストの件数の期待値が動かないことで見る |
| **P3** | **テストが `~/.claude/projects` を掴む** | **I8。** 既存の `assert_not_real_home` を新しい試料にも通す |
| **P4** | **変異テストがこの経路に 1 件も当たっていない**（#155 と同じ型） | **`MUTANTS` に sidechain 判定を壊す変異を足す。** 既定の観測で殺せることまで見る |
| **P5** | **version bump 漏れ（3 箇所のどれか）** | `check-plugin-versions.py` / `check-version-bump.py` |
| **P6** | **`skill-metrics.py` の生成ブロックが古くなる**——`references/*.md` を触るので動く | `skill-metrics.py --check`。**生成ブロックを手で書かない** |
| **P7** | **「どちらが正しいか」を一方に倒して書く** | **I5。** 両方の母集団を名指しして、既定の選択理由を書く |
| **P8** | **残差を主因に混ぜて「全部説明できた」と読める** | **I6。** 探索した次元と範囲を docstring に書く |
| **P9** | **`description` の同期漏れ**——`references/` を触るが frontmatter は変えない | `check-description-sync.py`。変えないなら trailer は不要（共変の判定は差分で見る） |
| **P10** | **件数の写しが増える** | `check-duplicate-counts.py`。**この周は数字を大量に書くので、いちばん当たりやすい** |

### 3.3 この環境では証明できないもの（未証明）

| 項目 | なぜ |
| --- | --- |
| **#133 が*どの手段で* sidechain を落としたか** | **測定コードが残っていない。** 示したのは**結果の一致**であって意図ではない。**同じ集合を作る別の手段が実在する**——**sidechain の拒否は main-chain と同じファイルに同居しない**（拒否を含む 535 ファイルのうち main のみ 251・sidechain のみ 284・**混在 0**）ので、**ファイル単位で絞っても同じ 870 件になる。** **言えるのは「落ちていたのは sidechain の側である」まで** |
| **理由節 106 / 62 の残差** | **上記のとおり探索して当たらなかった。** 復元手段が無い |
| **#122 の OS 比較が sidechain の構成で交絡していないか** | **Linux 側の記録がこの置き場所に無い**ので、ここからは数えられない。**この周では測らない**（手順 8 で起票する） |

### 3.4 BLOCKED

**無い。** 上の 3 件はいずれも**手段を用意すれば消える欠落ではない**
（測定コードは失われており、Linux の記録はこの置き場所に存在しない）。
3 件目は**受入基準ではない**ので、手順 8 の未解決事項として起票する
（[#119](https://github.com/hdknr/claude-code-setup/issues/119) の区分）。

### 3.5 生成物の鮮度

**該当あり。** `references/guard-rejections.md` を触るので、`skill-metrics.py` が測る
**参照側の節構造が動く**。**`skill-metrics.py` で生成ブロックを書き直し、`--check` が
緑になることを確かめる。** 図は触らないので `check-diagram-freshness.py` は動かない。

### 3.6 必須要件の掛け算

**「受入基準を両方の関門に渡す」×「2 パス回す」= 4 つの受け渡し。**
**渡すのは §3.1〜§3.3 の全体で、絞らない。**
**あわせて「実験の交絡を判別しているか」も両関門・両パスに渡す**——
**この周の成果物は実験の結論そのもの**なので、§0 の陽性対照・陰性対照が
本当に判別しているかが最大の争点になる。

## 4. 未解決の判断

**無い。** 既定を全件のままにする判断は §0 で理由つきで決めた。

## 5. 関門の進捗（再開点）

**周の大きさ**: この周が触る**同じ主張の複製は 2 箇所**
（`collect-guard-rejections.py` の docstring と `references/guard-rejections.md`）。
**docstring が正**で、参照側は既に「docstring を正とする」と書いている
——**訂正が要るのは、参照側が持っている「理由は特定できていない」という*独自の断定*だけ**である。
**version の 3 箇所は機械が見る**ので、手で数える複製には数えない。
**2 箇所なので分けない。**

**交絡を潰した手順**: §0 のとおり。**陽性対照（main-chain が #133 の表を 5 行とも再現）と
陰性対照（全件はどの行も再現しない）を両方取った。** **件数の一致だけでは偶然と
区別できない**ので、**独立に報告されていた内訳（件数 × 版数）を判別に使った。**
**版の種類（31 種）は一致するが判別力を持たない**（全件でも 31 種）ので、**根拠に数えない。**

| 関門 | 状態 |
| --- | --- |
| 手順 4 の実装 | **完了**（`b35942a`）。歯止め **26/26 本が緑**（失敗 0 / 飛ばし 0）、`check-plan-scope.py` も緑（差分 8 ファイルすべて範囲内） |
| 手順 5 の Verifier | **完了**（1 パス目）。**反証 0 件**。ただし**付帯で 1 件の不一致を報告**（陰性対照 1 行目 551 vs 553）——**原因の説明は誤っていた**（下記） |
| `/code-review` 1 パス目 | **完了**。**指摘 4 件**（MEDIUM 2・LOW 2） |
| 指摘対応 | **完了**（下の採否表）。歯止め **26/26 本が緑**、`check-plan-scope.py` も緑 |
| 手順 5 の当て直し | **完了**——**母集団（`~/.claude/projects` 6,011 ファイル）に当て直して 553 を確認**し、**新しいアサート 3 本すべてに陽性対照を取った**（下記） |
| `/code-review` 2 パス目 | **未着手** |

### 1 パス目の指摘と採否（1 件ずつ。会話にしか無いものを写す）

| # | 出所 | 指摘 | 採否 |
| --- | --- | --- | --- |
| **F1** | `/code-review` | 陰性対照 1 行目 **551/23** は、docstring が名指しする母集団（`< 04:00Z`・1,386 件）から再現せず **553/23** になる。551 は**別のカット**（`≤ ts870`・1,384 件）の値 | **採る。** docstring を 553 に直し、**カットを明示**して、**取り違えの経緯も残した** |
| **F2** | `/code-review` | 計画ファイル §0 の同じ数字・同じ欠陥。列見出し「全件（同じ時刻まで）」が曖昧 | **採る。** 見出しを `< 04:00Z`・1,386 件に変え、**551 と 553 の両方を再現して**経緯を書いた |
| **F3** | `/code-review` | 「内訳を**必ず**出力する」が **0 件の枝で偽**（早期 return が内訳ブロックより前）。かつ「出力の内訳で 870 は当て直せる」は**標準出力を名指ししている点で誤り**（あれは時刻で切らないので 870 は出ない） | **採る。** **内訳を 0 件の枝より前に移して主張を真にし**、870 の当て直しは **`--json` を正とする**ように直した（§6 の決着理由も同じ） |
| **F4** | `/code-review` | 新しいアサート `main + sidechain == 件数` は**恒真**（相補な分割の和）。`sidechain: False` 変異でも緑。`isSidechain` 欠落のケースも同じ変異で緑 | **採る。** 恒真式を**「どのレコードが sidechain か」**（uuid 比較）に置き換え、欠落のケースには**印を持つレコードを隣に置いて**判別できるようにした |
| **F5** | Verifier | 同じ 551 / 553 の不一致。**原因は「計測後の自然なデータ増減」** | **不一致は採る。原因の説明は採らない**——**実験で否定した**（下記） |

**F5 の説明を採らなかった理由（機構を実験で確かめた）。** **母集団の合計は今日も一致する**
（`< 04:00Z` で全件 **1,386**・main **872**、870 件目の時刻も `03:49:33.205Z` で同一）。
**自然増なら、合計が動かないまま 1 行目だけが 2 件ずれることは説明できない。**
カットを振ると**`< 04:00Z` → 553、`≤ 03:49:33.205Z` → 551** で、**残る 4 行はどちらでも同じ**
——**551 は別のカットの値**という `/code-review` 側の説明のほうが再現した。
**自分に不利でない報告（「0 件」）に付いてきた付帯の説明を、そのまま採らない。**

### 新しいアサートの陽性対照（3 本とも、赤くなることを確かめた）

**リポジトリと `~/.claude/` の外**（scratchpad）に本体とテストを 1 部ずつ複製して当てた。

| アサート | 陽性対照 | 結果 |
| --- | --- | --- |
| `sidechain と数えたのは、その印を持つレコードである` | `"sidechain": bool(...)` → `False` | **FAIL**（恒真式だった旧アサートは緑のままだった） |
| `フィールドが無ければ main、印があれば sidechain` | 同上 | **FAIL**（旧アサートは緑のままだった） |
| `0 件でも母集団の内訳を出す` | 内訳ブロックを 0 件の枝より**後ろ**に戻す | **FAIL** |

**手順 6 の割り目（2 パス目の前）**: `AskUserQuestion` で訊き、
**人間は「このまま続ける」を選んだ**（2026-09-22）。**割らずに 2 パス目へ進む。**
**選択のとおり文脈は持ったまま**だが、**1 件ずつの採否は上の表に書き出してある**
——会話にしか無い状態では、割らない周でも圧縮で失われる。

**手順 4 の割り目**: `AskUserQuestion` で訊き、**人間は「ここで割る」を選んだ**（2026-09-21）。
**再開する場所**: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-137`
の中で `/clear` してから `/dev-loop 137`。**手順 5 から**再開する。

**再開した側がまずやること**:

1. `find-cycle.py 137` を走らせる（当たるので `references/resume.md` を読む）。
2. **手順 5 の Verifier を起動する**——`dev-loop:dev-loop-verifier`、`model` は **`sonnet`**
   （実装は Opus 5 なので別ティア）。**`Agent` の使用許可は取得済み**（上記）。
   委譲文は `prompts/verifier.md` の `{{ }}` を置き換える。
   **base に渡す SHA は `5c90ca92f5585663dad595b52164fe2c638741d8`。**
3. **`/code-review` にも §3.1〜§3.3 と §6 をまるごと渡す**（`prompts/review-handoff.md`）。

**渡す観点で、この周に固有のもの**——**成果物が実験の結論そのもの**なので、
**§0 の陽性対照（main-chain が #133 の表を 5 行とも再現）と陰性対照（全件は 1 行も一致しない）が
本当に判別しているか**が最大の争点になる。**「件数が合った」だけを根拠にしていないかを見させる。**

## 6. 既知の限界・決着済みの論点

**両方の関門に、毎パス渡す。**

| 論点 | 決着 | 理由 |
| --- | --- | --- |
| **既定を main-chain に変えるべきか** | **変えない** | ガードはサブエージェントにも発火する。**#122 の OS 比較が現行の定義で取られている**ので、既定を変えると過去の測定と比較できなくなる |
| **`docs/plans/issue-122.md` の 870 を直すべきか** | **直さない** | **過去の周の記録**である。当時そう書いたことは事実なので遡って書き換えない |
| **理由節の残差を追い続けるか** | **追わない** | 16 通り × パス正規表現 4 変種 × 母集団サイズ 700〜954 の全域で当たらず、**測定コードが残っていない**ので復元手段が無い |
| **`--main-chain-only` フラグを足すか** | **足さない** | **`--json` が `sidechain` と `timestamp` を持つので、870 は書き出しの側で当て直せる**（**標準出力の内訳ではない**——あれは時刻で切っていないので 870 は出てこない。2 パス目のレビューが指摘した）。フラグは母集団を切り替える新しい意味論を増やし、**`--cwd-prefix` との組み合わせが積になる**。#137 が求めているのは原因の特定であって、母集団の切り替え手段ではない |
| **版の刻み** | **patch** | 記述の訂正のみ。能力も手順も増えない |
| **陰性対照 1 行目が 551 だった原因** | **別のカットの値だった**（自然増ではない） | **実験で判別した**——合計（1,386 / 872 / `03:49:33.205Z`）は今日も一致し、`< 04:00Z` → **553**、`≤ ts870` → **551**、**残る 4 行はどちらでも同じ**。自然増では「合計が動かないまま 1 行目だけ 2 件ずれる」を説明できない |
| **`--main-chain-only` を足さない決着は、F3 で理由が変わったか** | **決着は変わらない。理由の名指しだけ直した** | **当て直しの手段は `--json`**（`sidechain` と `timestamp` を持つ）。**標準出力の内訳ではない**——時刻で切らないので 870 は出ない。**「当て直せる」という決着の根拠は残っている** |

**決着の*理由*が誤っていると判断したなら、それは新しい反証である。**
