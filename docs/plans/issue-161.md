# issue #161 — `observe_leak` の漏れの計数が累積し、直っている経路まで赤にする

PR #160（#157）の 4 パス目の `/code-review` が挙げた 6 件を、別の周として直す。

## 1. 変更範囲

触る:

- `scripts/test-check-mutation-claims.py` — 計数の累積・`parent` の歯止め・偽の散文
- `docs/plans/issue-157.md` — §5「2 パス目 #5」の測定値の訂正
- `docs/plans/issue-161.md` — この計画ファイル

触らない:

- `scripts/check-mutation-claims.py`（**本体**。6 件はすべて*テスト側*の話で、本体の欠陥ではない）
- `plugins/` 配下（**版は上げない**——プラグインの中身を変えない）
- `CLAUDE.md`（**この周の学びは `scripts/` の docstring に収まる**。手順 8 で判定し直す）

## 2. 乗るデプロイ経路

**`docs/` を触るので、GitHub Pages への反映経路を持つ**（`.github/workflows/docs.yml`、main への push で自動デプロイ）。
`scripts/` は CI の検査対象で、本番に出るものではない。

## 3. verify の受入基準

### 不変条件 A — 観測は、その probe が残したものだけを数える

**経路（`observe_leak` の probe を、入口一覧として数える）:**

| 経路 | 数える先 | いまの守り |
| --- | --- | --- |
| P1（退避先が原本の内側） | `leak_root` | **他の probe と場所を共有しない**ので累積しない |
| P2（`.git` が残る） | `parent` | **掃除は外側の `finally` で 1 度きり**——累積の起点 |
| P5（中断） | `parent` | 同上。**P2 の残骸を拾う** |
| P10（`.resolve()` が落ちる） | `parent` | 同上。**P2・P5 の残骸を拾う** |
| 正常系 | `parent`（`box` を自分で畳む） | 累積の**下流**にいる |

**守り方**: `left()` を**数えたら掃く**形にする（数えた分を消してから返す）。
差分で数える案も等価だが、**掃くほうが「次の probe が素の状態から始まる」ことを構造で保証する**。

**ただし「差分で数える案も等価」は、*観測*の側の話だった（2 パス目の `/code-review`）。**
**E2 の検査の側では等価ではない**——**絶対値で見ると、掃きが正常でも本体の後始末が
変わっただけで赤くなる**（下の E2）。**掃きは `left()` に、差分は E2 の検査に、
という使い分けになった。**

**証明できること（陽性対照つき）**:

- **直す前**: `except BaseException:` → `except Exception:` に狭めると **FAILED 4 件**
  （うち観測は「中断でも退避先が残らない」と「`.resolve()` が落ちても退避先が残らない」の 2 件。
  **後者が誤り**）。**2026-09-22 に実測して再現済み。**
- **直したあと**: 同じ操作で**観測の赤は `中断` の 1 件だけ**になること。
  **2026-09-22 に、2 パス目の指摘対応のあとで当て直して満たしている**
  （`FAILED 3 件` ＝ `中断` ＋ **当て先が古くなった分 2 件**）。
- **陰性だけの対照にしない**——**素の状態が緑であること**を毎回先に見る。

**「観測の赤」は `leak[...]` の観測の検査を数える。** **E2 の検査（`掃かれ`）は
観測ではなく歯止めなので、この数に入れない。** **2 パス目の `/code-review` はここを
混ぜて「受入基準が成り立っていない」と報告したが、混ぜずに数えれば直す前から 1 件だった**
——**とはいえ、当の E2 の検査が偽の赤を出していたこと自体は真で、それが下の E2 の指摘である。**
**数え方の食い違いと、検査の欠陥は別の話だった。**

### 不変条件 B — 試料の外を対象にしない

**経路**: `observe_leak` が受け取る 2 つのパス。

| 経路 | いまの守り |
| --- | --- |
| `leak_root` | `assert_not_real_repo(leak_root)` が当たっている |
| `parent` | **当たっていない**。しかも `tempfile.tempdir` に据えられ、`shutil.rmtree` される |

**守り方**: `parent` にも `assert_not_real_repo` を当てる。**両方とも、何かを作る前に当てる。**

**証明できること**: **歯止めを外すと赤になること。**
`assert_not_real_repo` を記録する体に差し替えて `observe_leak` を呼び、
**`leak_root` と `parent` の両方に当たったこと**を見る。
**実リポジトリを渡して落ちるかを見る形は採らない**——
**歯止めを外した状態でその検査を走らせると、実リポジトリに書いてしまう**
（このリポジトリの規範「禁止は文でなく構造で」が、検査の側にも掛かる）。

### 不変条件 C — 散文の主張が、実物と合っている

**経路（偽の記述の入口を、ファイルの中から数える）:**

| # | 場所 | 何が偽か | 採る手 |
| --- | --- | --- | --- |
| C1 | ファイル冒頭 docstring | 指し先が `observe_leak()` を覆っていない | **指し先に足す** |
| C2 | ファイル冒頭 docstring | 「ただし 1 つだけ、木ではないので `build*()` から読めないものがある」 | **削る** |
| C3 | `observe_leak` docstring | 「（それは別の検査が端から端まで見る）」 | **削る** |
| C4 | `observe_leak` docstring | 「（上の 3 つはどれも `RuntimeError` ＝ `Exception` で落ちる）」 | **削る** |
| C5 | `observe_leak` docstring | 「（上の 4 つはどれも `.resolve()` の *後* で落ちる）」 | **削る** |
| C6 | P10 の inline コメント | 「全部落とすと『どこで落ちたか』が判別できない」 | **削る** |

**6 つのうち 5 つは削る。** #157 の 3 パス目で
**「追加は毎回指摘を生み、削除は 0 件」**と実測されている型なので、**直すのではなく消す。**
**C1 だけは指し先そのものなので、消すと指す先が無くなる**——**足す。**

**「言い換えは削除ではない」**（#110）。**削ると決めた 5 つは、文字を消すだけにする。**

**証明できること**: 削った語句がファイルに 1 つも残っていないこと（`grep`）＋テストが緑。

### 不変条件 E — 新しい歯止めは、守ると言っている当のものを判別する

**1 パス目の `/code-review` が挙げた 2 件を受けて足した。**
**どちらも「散文だけで、実行されない主張」だった箇所**である。

| # | 守ると言っていたもの | 直す前の実測（1 パス目） | 足した検査 |
| --- | --- | --- | --- |
| E1 | 歯止めは**何かを作る前に**当たる（不変条件 B の後半） | **歯止めを末尾へ動かしても緑のまま**。当たった*先*しか見ていなかった | `歯止めは、何かを作る前に 2 つとも当たっている` |
| E2 | `left()` は**数えたら掃く**（不変条件 A の守り方そのもの） | **掃きを外しても緑のまま**。累積は**変異 2 つ同時**でしか見えない | `数えた退避先は掃かれ、次の経路に持ち越されない` |

**E2 の検査は、2 パス目の `/code-review` に「守ると言っている当のものを判別していない」と
反証された**——**`(swept[...]) == (1, 0, 0)` と絶対値で見ていたので、
掃きが正常でも本体が畳まなくなっただけで赤くなる**。**2 通りで再現した**（下の §6）。
**それは「掃き」という名前で別の原因を指す偽の赤**で、**この周が `.resolve()` の偽の赤を
消したのとまったく同じ害**である。**直した形は「植える前との差を取る」**
——**差にすれば、本体が何を畳まなくなっても同じ分が両側に乗って消える。**

**E2 は `mutation-claim:` では書けない**——`MUTATIONS` は**1 度に 1 箇所しか壊さない**ので、
**2 ファイル（2 箇所）同時の変異を表現する書式が無い**。**だから直接の probe にした**
（退避先の親に的を 1 つ植え、**次の経路に持ち越されない**ことを見る）。
**規定の形と違うものを、同じ名前で報告しない。**

**証明できること（陽性対照つき）**:

| 当てるもの | 期待 |
| --- | --- |
| **素の状態**（先に見る） | **rc=0・FAILED 0** |
| 歯止めを `parent.mkdir()` の**あと**へ動かす | **E1 の検査だけが落ちる** |
| `left()` を「数えるだけ」に戻す | **E2 の検査だけが落ちる** |
| **`except BaseException:` → `except Exception:`** | **E2 の検査は落ちない**（掃きは正常だから）。**偽の赤が出ないこと** |
| **`.resolve()` を `try` の外へ戻す** | **E2 の検査は落ちない。** 落ちるのは `.resolve()` の観測だけ |

**「どちらかが落ちる」では足りない**——**落ちた検査の名前まで見る**
（1 件赤になっただけでは、当の検査が効いたのか別の検査が拾ったのか判別できない）。

### 不変条件 D — 既に main に入っている測定値が、いま偽のまま残らない

| 経路 | 状態 | 採る手 |
| --- | --- | --- |
| `docs/plans/issue-157.md` §5「2 パス目 #5」 | **偽のまま main に入っている** | **訂正する** |
| PR #160 のコメント | **4 パス目に訂正済み**（「当時は正しく、いまは偽」と明記済み） | **この周の PR へのリンクを足す** |

**訂正の形**: **測定値を消さない。**「**`a16f180` 時点では正しく、P10 の probe を足した
3 パス目以降は偽**」と、**いつまで正しかったか**が読める形にする。

### この環境では証明できないもの

**無い。** 6 件はすべて**このリポジトリの中で当てて確かめられる**
（変異の当て直しと `grep`）。**指摘 1・5 は着手前に実測で再現済み。**

### 生成物の鮮度

**該当なし。** 触るのは `scripts/test-*.py` と `docs/plans/` だけで、
**drawio の書き出しにも `SKILL.md` の測定値にも掛からない**
（前者は `diagrams/`、後者は `plugins/*/skills/*/SKILL.md` に依存する）。
**それでも `check-all.py` は全部回す**——該当判定は自己申告なので、歯止めの側で見る。

### 必須要件の掛け算

| 掛け算 | 満たし方 |
| --- | --- |
| 受入基準 × 2 つの関門 | Verifier と `/code-review` の両方に、この §3 をまるごと渡す |
| 受入基準 × 2 パス | 2 パス目にも §3 と §6 を渡す |
| 2 つの関門 × 2 パス | **4 つの受け渡し**。どれも省かない |
| 「実行されない主張」× 2 つの関門 | **この周は散文の主張を削る周**なので、**残した主張が当たるか**を両方に当てさせる |
| 陽性対照 × 2 つの関門 | 「直す前 2 件 → 直したあと 1 件」を、**両方に再現させる** |

## 4. 未解決の判断・確認待ち

- **`left()` を「数えたら掃く」に変えると、外側の `finally` の掃除が二重になる。**
  **残す**（正常系の `box` と、例外で抜けた分の受け皿）。**消すと守る対象が減る。**
- **指摘 2 の「変異テストで押さえる」の形。** `MUTATIONS` は**本体**を壊す仕組みで、
  **テスト自身の `assert` は対象外**。だから**記録する体に差し替える検査**で代替する
  ——**この差は報告に書く**（規定の形と違うものを、同じ名前で報告しない）。

## 5. 関門の進捗（再開点）

### 周の在り処

| 項目 | 値 |
| --- | --- |
| ブランチ | `issue/161-observe-leak-accumulates` |
| worktree | `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-161` |
| 起点コミット | `2fac653` Merge pull request #160 from hdknr/issue/157-sandbox-cleanup |
| 切った元の参照 | `refs/remotes/origin/HEAD` → `origin/main`（取り直した） |
| **base（関門に渡す SHA）** | `2fac653bee39880e3fbe7eb5a16b4c7e9406f947` |

**base は HEAD の祖先であることを確認済み**（`git merge-base --is-ancestor`）。

### 通した関門

| 関門 | 状態 |
| --- | --- |
| 手順 4（実装） | **完了。**`10004cb` にコミット済み |
| 歯止め（`check-all.py`） | **`9813605` 時点の申告は偽だった**——**同じコミットで足した行が `check-duplicate-counts.py` を割っていた**（手順 5 の Verifier が反証。下記）。**免除を足して回し直し、いまは全数緑**（`28/28 本を回した（失敗 0 / 飛ばし 0）`。**2026-09-22 に、書く前に回した**）。**`/code-review` の指摘対応のあとにも回し直して同じ**（**回してから書いている**）。**2 パス目の指摘対応では、いったん `check-norm-markers.py` を割った**——**この計画ファイルに足した 2 行が `（必須）` を原本の外に持ち出していた**（**規範の複製を論じる段落で、著者が同じ編集の中で複製を作った**）。**マーカーを外して指し先の形に直し、28 本を全部回し直して緑**（2026-09-22） |
| `check-plan-scope.py`（base に対して） | **完了。差分 3 ファイルはすべて変更範囲の中** |
| 手順 5 の Verifier（1 パス目） | **完了。反証 1 件**（V1。採用・手当て済み） |
| `/code-review`（1 パス目） | **完了。指摘 4 件**（R1〜R4。**4 件とも採用**） |
| 手順 5 の当て直し（2 パス目分） | **完了。**素の状態 → 変異 2 つを当て、**落ちた検査の名前まで**見た（不変条件 E） |
| `/code-review`（2 パス目） | **完了。指摘 5 件**（P1〜P5。**3 件採用・2 件却下**。下記） |
| 手順 5 の当て直し（3 パス目分） | **完了。**素の状態 → **変異 6 通り**を当て、**落ちた検査の名前まで**見た（§3 E の表のとおり。2026-09-22） |
| `/code-review`（3 パス目） | **未** |

**再開するなら**: この worktree
（`/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-161`）
の中で `/clear` してから `/dev-loop 161`。
**着地先は `/code-review` の 3 パス目だけ**——**手順 5 の当て直し（2 パス目の指摘対応分）は済んでいる**（上の表のとおり）。
**3 パス目には §3（不変条件 A〜E）と §6 をまるごと渡す**——**「初回に渡したから 3 回目は要らない」は成り立たない。**
**対象の差分は絞らない**——**P1 は E2 の検査（既存）を*改変*しており、足した分だけでは相互作用が見えない。**

**3 パス目以降の判定は `dev-loop` の手順 6 を正とする**（「指摘 0 件」でなければ止まって相談する）。
**この周に当てはまる事実だけ書く**——**パスごとの指摘数は 4 → 5 で減っていない**ので、
**相談に入るなら「削る」が選択肢に入る**（#110 の実測——**削除は 0 件、追加は毎回指摘を生む**）。

**手順 4 の割り目で `AskUserQuestion` を出した結果**: **ここで割る**（人間が選択。2026-09-22）。

**手順 6 の割り目（2 パス目の前）で `AskUserQuestion` を出した結果**: **ここで割る**（人間が選択。2026-09-22）。

**手順 6 の割り目（3 パス目の前）で `AskUserQuestion` を出した結果**: **ここで割る**（人間が選択。2026-09-22）。
**併せて「3 パス目の指摘が 0 件でなかったときの扱い」も訊いた**——
**「0 件でなければ止めて相談（規定どおり）」を人間が選択**（2026-09-22）。
**つまり 3 パス目で指摘が出たら、その場で直しに入らず、指摘数と型と選択肢を添えて相談する。**

### 実装で当てた実測（1 パス目の前に取ったもの）

| 当てたもの | 結果 |
| --- | --- |
| **陽性対照**（素の状態） | **rc=0・すべて ok**（**当時 52 件。R1・R2 で検査が増え、いまは 54 件**） |
| **`except BaseException:` → `except Exception:`（直す前）** | **FAILED 4 件**。観測は **2 件**（`中断` と `.resolve()`）。**後者が誤り** |
| **同じ操作（直したあと）** | **FAILED 3 件**。観測は **1 件**（`中断` だけ）。**残る 2 件は当て先が古くなった分** |
| **`assert_not_real_repo(parent)` を外す** | **当時 FAILED 1 件**（`退避先の親にも歯止めが当たっている`）。**R1 を足したいまは 2 件**（`歯止めは、何かを作る前に 2 つとも当たっている` も落ちる）——**どちらも同じ原因を指しており、偽の赤ではない**（2026-09-22 に再実測） |
| **`if calls["n"] == 1:` → `if True:`** | **出力に 1 バイトの差も無い**（指摘 5 の理由が偽であることの実測） |
| **削った語句の残り** | **0 件**（`grep`）。**`上の 3 つ` の残り 1 件は `snapshot` の `observe` 3 つを指しており、真** |

**この表の `観測は 2 件` / `観測は 1 件` は、`check-duplicate-counts.py` の免除を使っている。**
**別々の状態の実測だからである**——前者は**直す前**、後者は**直したあと**で、**どちらも真**。
**語句を変えて検査をすり抜ける言い換えは採らない**（意味上の重複は残るのに黙るだけになる）。

dup-counts-ok: 観測

### 手順 5 で挙がった反証と、手順 4 への差し戻し

**差し戻しは 2 回**——**V1（Verifier の反証）と、`/code-review` の指摘 4 件への対応。**
**後者の当て直しは済んでいる**（素の状態 → 2 つの変異を当てて、落ちた検査の名前まで見た）。

**1 パス目（Verifier・別ティア `sonnet`）: 反証 1 件。**

| # | 反証 | 採否 | 手当て |
| --- | --- | --- | --- |
| V1 | **§5 の「歯止め: 28/28 本 OK（失敗 0 / 飛ばし 0）」が偽。** `check-duplicate-counts.py` が `docs/plans/issue-161.md` の 165・166 行（`観測は 2 件` / `観測は 1 件`）で発火する。**その 2 行と当の申告は同じコミット（`9813605`）で足されており、足した内容を含めて回し直していなかった** | **採る** | **`dup-counts-ok: 観測` を足す**（下記の理由つき）＋**申告を訂正**（上の表）＋**回し直す** |

**この反証は、親（実装した側）が自分で再現した**——`python3 scripts/check-duplicate-counts.py` が
`rc=1` で同じ 2 行を名指しした。**報告を検証の代わりにしていない。**

**型としては #117 と同じ**（申告と実測が食い違う）。**CLAUDE.md が名指しで警告している形を、
その警告を読んでいる周が踏んだ**——**歯止めは毎回*全部*回す**が、
**「書き終えたあとに回す」まで含めて初めて満たされる。**

**手順 4 への差し戻し**: 1 回（計画ファイルのみ。**実装（`scripts/`）には触っていない**）。

### 交絡を潰した手順

**この周は実験をする**（変異を当てて赤になる件数を数える）。潰した手順:

- **陽性対照**——**変異を当てる前に緑であること**を毎回先に見る（`FAILED` が出ないこと）。
  **2026-09-22 に素の状態で 1 度取った**（rc=0・すべて合格）。
- **「赤が減った」が、変異が届かなくなったせいで出ていないか**——
  **当て先（`MUTATIONS` の needle）が生きていること**を、
  **`変異を当てる先が 1 箇所ある` が FAIL に出ないこと**で見る。
- **観測窓**——見たのは `test-check-mutation-claims.py` の `FAILED` 節だけ。
  **実 `$TMPDIR` の件数は見ていない**（#157 §3.3 のとおり、揺れるので受入条件にしない）。

## 6. 既知の限界・決着済みの論点

| 論点 | 決着 | 理由 |
| --- | --- | --- |
| **`mkdtemp()` が返ってから `try` に入るまでの窓** | **閉じない** | **純 Python では閉じられない。** #157 §3.3 で決着済み（Issue #161 の「やらないこと」にも明記） |
| **実 `$TMPDIR` に 1 件も残らないこと** | **不変条件として採らない** | **同じ置き場所を入れ子の自分自身が使うので、判定が外の事情で揺れる**（#157 §3.3／§6） |
| **本体（`check-mutation-claims.py`）を直すこと** | **やらない** | **6 件はすべてテスト側の話。** 本体に欠陥は挙がっていない |
| **`if calls["n"] == 1:` を `if True:` にすること** | **やらない** | **カウンタを残すのは構わない**（Issue #161 の指摘 5）。**偽なのは書いてある*理由*のほう** |
| **PR #160 のコメントを書き換えること** | **やらない** | **4 パス目に訂正を*追記*済み**で、**当時正しかったことも読める形**になっている。**上書きすると経緯が消える** |
| **プラグインの版を上げること** | **やらない** | **`plugins/` を触らない**（CLAUDE.md の bump 規約は `plugins/` 配下の変更に掛かる） |

**この表は「見なくてよい範囲」ではない。**
**決着の*理由*が誤っていると判断したなら、それは新しい反証である。**

### 前のパスの指摘と採否

**`/code-review`（1 パス目・effort high）: 指摘 4 件。4 件とも採用。**

| # | 指摘 | 採否 | 手当て |
| --- | --- | --- | --- |
| R1 | **MEDIUM.** `parent` の歯止めの検査は「**呼ばれた**」ことしか見ておらず、「**作る前に**呼ばれた」を見ていない。**末尾へ動かしても緑のまま**（実測） | **採る** | **順序を記録する**——歯止めと `mkdir` を 1 本の並びに入れ、歯止めが先に出そろうことを見る（不変条件 E1） |
| R2 | **MEDIUM.** **この周の主目的である `left()` の掃きに回帰の歯止めが無い。** 単独で戻しても suite は緑（累積は変異 2 つ同時でしか出ない） | **採る** | **的を植える probe** を足す（不変条件 E2）。**`mutation-claim:` では書けない**ので直接の probe にした |
| R3 | **LOW.** C1 の指し先に `main()` が無く、**実 `$TMPDIR` に置く唯一の的**（`cmc-escape-<pid>.txt`）が指し先の外にあった。C2 を削ったことで、例外を説明していた橋も消えた | **採る** | **指し先に `main()` を足す**（C1 と同じ「足す」手。**削った橋は戻さない**——指し先が覆えば要らない） |
| R4 | **LOW.** 計画ファイル 44 行のバッククォートが奇数個で、検査名が壊れて描画される | **採る** | **「」で名前の境界を取り、`.resolve()` だけをコードにする** |

**R1・R2 は、この周が直していた型そのものが*検査の側*に残っていたもの**である
——**「散文だけで、実行されない主張」。** **C2〜C6 を削って本文から消したのに、
検査の側に同じ形が 2 つ残っていた。**

**`python3 -O` で `assert` が消える件（R1 の副次）は、追わない**——
**suite 全体は隣の `実リポジトリを対象にすると落ちる` で赤になる**（1 パス目が実測）。
**この周の受入基準ではない**ので、**弱めるのではなく、手を入れない。**

**`/code-review`（2 パス目・effort high）: 指摘 5 件。3 件採用・2 件却下。**

**却下した 2 件は、どちらも「観測の赤」の数え方の食い違いから来ている**
——**関門は E2 の検査（`掃かれ`）を観測に数えたが、§3 A は `leak[...]` の観測の検査を
指している。** **ただし同じ関門が、その根にある*本物の欠陥*を P1 で当てている**
——**却下したのは結論の言い方のほうで、指摘そのものを退けたのではない。**

| # | 指摘 | 採否 | 手当て／却下の理由 |
| --- | --- | --- | --- |
| P1 | **MEDIUM.** **E2 の検査が絶対値（`== (1, 0, 0)`）で見ており、掃きを判別していない。** 本体が P5・P10 で畳まなくなった変異でも赤になり、**`掃き` という名前で別の原因を指す偽の赤**になる | **採る** | **植える前との差を取る**形に直した。**親が 2 通りで再現**（`except` を狭める／`.resolve()` を `try` の外へ）——**どちらも掃きは正常なのに赤だった。** 直したあとは**どちらでも E2 は緑**で、**掃きを外したときだけ赤**（§3 E の表） |
| P2 | **MEDIUM.** §3 A の「直したあと: 観測の赤は `中断` の 1 件だけ」が HEAD で成り立たない（実測 2 件） | **却下** | **`掃かれ` を観測に数えている。** §3 A は観測を `leak[...]` の検査と定めており（同節が「観測は…の 2 件」と名指ししている）、**その数え方では直す前から 1 件**。**親が再実測して確認**——変異 (a) の `FAILED 3 件` ＝ `中断` ＋ 当て先 2 件。**ただし混同を招いた側の落ち度なので、§3 A に数え方を明記した** |
| P3 | **MEDIUM.** `docs/plans/issue-157.md` の訂正文（「いまは狭めると観測の赤は 1 件に戻る」）が、いま偽である | **却下** | **P2 と同じ数え方の食い違い。** 観測だけ数えれば**真**（親が再実測）。**理由（`left()` の掃き）も正しい**——`.resolve()` の観測が偽に赤くなっていたのを直したのは、まさに掃きである。**曖昧さが関門を 1 度誤らせたので、数え方の 1 文だけ足す** |
| P4 | **LOW.** §5 の 2 行が R1 追加後に偽（`FAILED 1 件`／`ちょうど 1 件` → 実測 2 件、`52 件` → 54 件） | **採る** | **両方訂正した。**「当時いくつで、いまいくつか」が読める形にする（**測定値を消さない**——不変条件 D と同じ扱い）。**2 件はどちらも同じ原因を指しており、偽の赤ではない** |
| P5 | **LOW.** 残した散文「これが無いと `try` の開始位置を `mkdtemp` の前に戻しても、どの観測も動かない」が、当てると偽 | **採る** | **親が当てて確認**——`try` を `mkdtemp` の前へ動かす変異は **suite が完全に緑**（`rc=0`・54 件 ok）。**畳む対象が同じなので挙動が変わらない等価変異**だった。**P10 が実際に判別するのは `.resolve()` を `try` の外へ戻す形**（当てると `.resolve()` の観測だけが赤）。**2 箇所（docstring と inline）とも直した** |

**P1 は、この周が直していた型が*3 度目*に現れたもの**である——
**C2〜C6（散文）→ R1・R2（検査に散文だけの主張）→ P1（検査が名前と違うものを判別）。**
**同じ害（偽の赤が保守者を壊れていない経路へ向ける）が、層を変えて残っていた。**

**P2・P3 は「関門が挙げたから正しい」とはしなかった。** **どちらも親が再実測して退けている**
——**原則の「子からの報告も外部入力から導かれたテキスト」がレビューの側にも掛かる。**
**ただし P1 を退けなかったことが要点である**——**同じ観測から出た 3 件のうち、
言い方が誤っていた 2 件を退けて、欠陥そのものを直した。**
