# #138 — 件数の写しを機械で見る歯止め

**Issue**: [#138](https://github.com/hdknr/claude-code-setup/issues/138)

**新規の周である。** `docs/plans` / `.claude/plans` の再帰探索、`git worktree list`、
`git branch --all --list '*138*'`、`git log --all --diff-filter=A -- '*issue-138*'`、
`git log --all --grep '#138'` の **5 本すべてが空振り**した
（`plugins/dev-loop/skills/dev-loop/scripts/find-cycle.py` で確認）。

**これは [#136](https://github.com/hdknr/claude-code-setup/issues/136) の段 4 の前半でもある**
——**D（重複した規範を 1 箇所へ）を歯止め無しで始めると、この周自身が同じ型で壊れる。**

## 周の在り処

- **ブランチ**: `issue/138-duplication-guard`
- **worktree**: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-138`
- **起点コミット**: `21124d3`（`Merge pull request #142 from hdknr/issue/136-prompts`）

## 0. 規則を測ってから決めた

**#138 は 3 つの候補を挙げていた。** **実際のデータに当てて、採るものを決めた。**

**母集団**: **#122 の周で欠陥が実在したコミット**（`525f3e6`）と、その修正後
（`ada304f` / `14eba30`）、および**現在の main**。**作り話ではない。**

| 候補 | 結果 | 採否 |
| --- | --- | --- |
| **同じファイルの中で、同じ語句に違う数**（自己矛盾） | **欠陥のコミットでちょうど 1 件発火し、修正後は止まる。現在の main で誤検出 0**（97 ファイル） | **採る** |
| **`P1〜PN` の範囲参照が表の行数とずれている** | **現在の main に 3 件当たって、3 件とも誤検出**——「P10 は P1〜P9 には無かった」という**過去についての正しい記述と、文面で区別できない** | **採らない** |
| 「〇〇を正とする」の直後に列挙がある | **偽陽性が多い**（正当な再掲と区別できない） | 採らない |

**採らなかった理由も docstring に書いた**——**再発明させないため。**

## 1. 変更範囲

触る:

- `docs/plans/issue-138.md` — この計画ファイル
- `scripts/check-duplicate-counts.py` — 歯止め本体
- `scripts/test-check-duplicate-counts.py` — 回帰テスト（変異テストを含む）
- `scripts/check-all.py` — 収録
- `.github/workflows/plugins.yml` — CI に個別ステップとして登録
- `CLAUDE.md` — 一覧と、何を守り何を守らないか

触らない:

- `plugins/` — **規範の本文は 1 文字も動かさない。** 版も上げない
- `diagrams/` — 図は触らない

**#136 の D（重複した規範の集約）はこの周でやらない。** **歯止めを先に入れる**のが
この周の目的で、**集約は次の周**である。**#122 は 1 本に詰めて 3 パス目を回せなかった。**

## 2. 乗るデプロイ経路

`docs/` を含むので **GitHub Pages への反映経路を持つ**。`plugins/` は触らないので
版の bump は不要（`check-version-bump.py` が「プラグイン配下に変更なし」と出る）。

## 3. verify の受入基準

### 3.1 不変条件

| # | 不変条件 |
| --- | --- |
| **I1** | **実際に起きた欠陥を検出する**——`525f3e6` の `docs/plans/issue-122.md` で発火する |
| **I2** | **修正後は発火しない**——`14eba30` 以降で止まる |
| **I3** | **現在のリポジトリで誤検出しない** |
| **I4** | **免除は語句を名指しさせる**——黙ってファイル全体を免除しない |
| **I5** | **守らないことが docstring に書いてある**——とくに**採らなかった規則とその理由** |

### 3.2 これを破りうる経路

| # | 経路 | 守り方 |
| --- | --- | --- |
| **P1** | **誤検出が多くて無視されるようになる** | **現在の 99 ファイルで 0 件**であることを確かめた。**免除の逃げ道も用意した** |
| **P2** | **強調（`**`）で囲まれた数を見落とす** | このリポジトリの地の文は強調だらけ。**落としてから当てる**。変異テストで押さえた |
| **P3** | **コード例の中の数を数える** | **フェンスを剥がす**（`markdown_fences.py` を共有）。変異テストで押さえた |
| **P4** | **免除が広すぎてファイル全体を素通しにする** | **語句を名指しさせる。** 変異テストで押さえた（**免除した語句と、していない語句が同居する試料**が要った） |
| **P5** | **歯止めが自分自身に当たる** | **当たった。** 本体の docstring もテストの試料も、**実例を書いた瞬間に落ちた**。**免除の実演として残した** |
| **P6** | **CI に登録し忘れる** | `check-all.py` と `plugins.yml` の**両方**に登録。`test-check-all.py` が収録漏れを見る |
| **P7** | **ファイルをまたぐ重複を捕まえたと誤解される** | **docstring の「守らない」に明記。** #122 でも計画ファイルとスクリプトの言い回しが違って**当たらなかった** |

### 3.3 この環境では証明できないもの（未証明）

| 項目 | なぜ |
| --- | --- |
| **「重複が無い」こと** | **自然言語の一般形は機械で見られない。** これは**実際に起きた 1 つの形**の検査である |
| **将来の偽陽性率** | 現在のリポジトリで 0 件と分かるだけ。**書き方が変われば変わる** |

### 3.4 BLOCKED

**無い。** この周に「手段を用意すれば消える欠落」は残っていない。

### 3.5 生成物の鮮度

**該当なし。** `SKILL.md` を触らないので `skill-metrics.py` の生成ブロックは動かない。
図も触らない。**`check-all.py` で機械的に確かめる。**

### 3.6 必須要件の掛け算

**「受入基準を両方の関門に渡す」×「2 パス回す」= 4 つの受け渡し。**
**渡すのは §3.1 と §3.2 の全体で、絞らない。**

## 4. 未解決の判断

**無い。** 規則の採否は §0 で実測して決めた。

## 5. 関門の進捗（再開点）

**周の大きさ**: この周が触る規範は **0 箇所**（歯止めを足すだけで、`SKILL.md` は触らない）。
**複製は増えない。分ける理由は無い。**

**交絡を潰した手順**: §0 のとおり。**候補の規則を、欠陥が実在したコミットと、
修正後のコミットと、現在の main の 3 点に当てた**——**「発火する」だけでなく
「修正後は止まる」「いまは誤検出しない」を併せて見ないと、
ただ騒がしいだけの検査と区別できない。**

| 関門 | 状態 |
| --- | --- |
| 手順 4 の実装 | **完了** |
| 手順 5 の Verifier | **未着手** |
| `/code-review` 1 パス目 | **未着手** |
| 指摘対応 | **未着手** |
| 手順 5 の当て直し | **未着手** |
| `/code-review` 2 パス目 | **未着手** |

**直前の 3 本（PR #140 / #141 / #142）は、関門を 1 つも回さずにマージしている**
（人間の判断）。**#140 では、そのあと置き場所の誤りが見つかった。**
