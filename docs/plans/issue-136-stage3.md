# #136 段 3 — 入場の機械部分をスクリプトにし、条件つきの塊を `references/` へ

**Issue**: [#136](https://github.com/hdknr/claude-code-setup/issues/136)（**4 段のうち段 3**。
段 1・2 は完了、段 4 の前半は [#138](https://github.com/hdknr/claude-code-setup/issues/138) で入れた）

**新規の周である。** `find-cycle.py 136` は段 1・2 の跡に当たるが、
**この周のブランチ・worktree・計画ファイルはいずれも無い**ことを確認した。

## 周の在り処

- **ブランチ**: `issue/136-worktree-entry`
- **worktree**: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-136c`
- **起点コミット**: `eee5543`（`Merge pull request #143 from hdknr/issue/138-duplication-guard`）

## 0. 測ってから決めた

**手順 4 は 454 行 / 24,169 文字**で、`SKILL.md`（76,632 文字）の **31%** を占める。
**塊ごとに分けると、偏りが極端だった:**

| 塊 | 行 | 文字 | 性質 |
| --- | --- | --- | --- |
| **「既に worktree がある場合は、作らずにそこへ入る」** | **213** | **12,018** | **丸ごと再開の周でしか要らない** |
| 「いま worktree の中にいることは、正しい worktree にいることではない」 | 48 | 2,279 | 新規・再開の両方 |
| 「変更範囲の外へ黙って出ない」 | 53 | 2,562 | 毎周 |
| 「`EnterWorktree` で新規に作れないことがある」 | 28 | 1,451 | 道具の意味論 |
| 「最初の一手は作ることではなく見ること」 | 24 | 1,258 | 毎周 |
| 「実装を終えたら割り目を置く」 | 34 | 1,861 | 毎周 |
| その他 | — | 残り | — |

**1 つの塊が `SKILL.md` の 16% を占めており、それが丸ごと条件つきである。**
**段 1 と同じ手が当たる。**

## 1. 変更範囲

触る:

- `docs/plans/issue-136-stage3.md` — この計画ファイル
- `plugins/dev-loop/skills/dev-loop/references/resume.md` — 移設先（**再開の周の話をここに寄せる**）
- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 当該塊を指し先に置き換える。版を上げる
- `plugins/dev-loop/skills/dev-loop/scripts/prepare-worktree.sh` — **新規の周**の入場の機械部分
- `plugins/dev-loop/skills/dev-loop/scripts/check-pr-preconditions.sh` — 手順 6 の前提条件
- `scripts/test-worktree-scripts.py` — **2 本まとめた回帰テスト**（変異 8 件）。
  **分けなかった**——`prepare-worktree.sh` が作った worktree を
  `check-pr-preconditions.sh` が judge する形なので、**同じ偽リポジトリを共有するほうが自然**
- `scripts/check-all.py` — 収録
- `.github/workflows/plugins.yml` — CI に登録
- `plugins/dev-loop/.claude-plugin/plugin.json` / `.claude-plugin/marketplace.json` — 版を揃える
- `docs/plugins/dev-loop-design.md` — `skill-metrics.py` の生成ブロック（**手で書かない**）
- `CLAUDE.md` — 新しいスクリプトを一覧に載せる

触らない:

- `diagrams/` — 図は触らない
- **手順 5・手順 3・原則** — **判断しかない。スクリプトに翻訳できる形を持っていない**

## 2. 乗るデプロイ経路

`docs/` を含むので **GitHub Pages への反映経路を持つ**。`plugins/` を触るので
版の bump が要る。

## 3. verify の受入基準

### 3.1 不変条件

| # | 不変条件 |
| --- | --- |
| **I1** | **規範が 1 つも消えていない**——移設の前後で、移した本体が**バイト一致**で入っている |
| **I2** | **起点が実際に減っている**——`SKILL.md` の文字数が減る |
| **I3** | **再開する周が、移設後も入場できる**——`SKILL.md` だけを読んだ状態から指し先に到達できる |
| **I4** | **スクリプトが、散文が述べていた分岐をすべて持つ**——存在確認・命名規約・detached の分岐・肯定的な確認 |
| **I5** | **スクリプトが対象リポジトリに依らない**——`dev-loop` はどのリポジトリでも使う（**段 1 で置き場所を間違えた**） |

### 3.2 これを破りうる経路

| # | 経路 | 守り方 |
| --- | --- | --- |
| **P1** | **移設のときに規範が落ちる** | **移した本体のバイト一致を機械でアサートする。** 目視しない |
| **P2** | **スクリプトが `EnterWorktree` を呼べないことを忘れる** | **呼べない。** スクリプトは**素の git で済む部分だけ**を行い、**入場そのものは道具に残す** |
| **P3** | **スクリプトを対象リポジトリの `scripts/` に置く** | **段 1 で実際にやった。** プラグインの中（スキルの base ディレクトリの下）に置く |
| **P4** | **新規の周と再開の周で、当てる時点が違う**ことを崩す | 肯定的な確認は**入場を済ませてから**当てる。**作る前に当てると必ず一致しない側に倒れる** |
| **P5** | **detached HEAD で `git branch -m` を呼んで fatal になる** | **`git switch -c` に分岐する。** #110 で実測済み |
| **P6** | **移設先が歯止めの外に出る** | `check-norm-markers.py` の原本は**スキルのディレクトリ**に一般化済み（段 1）。**`references/` は原本の一部** |
| **P7** | **スクリプトがテストで実環境を触る** | **テンポラリの偽リポジトリだけを対象にすることをアサートする** |

### 3.3 この環境では証明できないもの（未証明）

| 項目 | なぜ |
| --- | --- |
| **`EnterWorktree` / `ExitWorktree` の挙動** | **スクリプトから呼べない。** 道具の意味論は散文に残すしかない |
| **移設で「読まれなくなる」ことが起きないか** | **モデルが指し先を読むかは実行時の判断**で、静的には決まらない |

### 3.4 BLOCKED

| 項目 | 何が無いか |
| --- | --- |
| **周あたりの起点の前後比較** | **同じ Issue の周を移設前後で 1 本ずつ回さないと比べられない**（段 1 と同じ） |

### 3.5 生成物の鮮度

**該当あり。** `SKILL.md` を編集するので `skill-metrics.py` の生成ブロックが古くなる。
**再生成して `--check` が緑になること**を受入基準に含める。

### 3.6 必須要件の掛け算

**「受入基準を両方の関門に渡す」×「2 パス回す」= 4 つの受け渡し。**

**ただしこの周では、関門は段 4 のあとに全体へ掛ける**——**人間の判断**（2026-09-20）。
**だから「この PR は関門を通った」とは書かない。**

## 4. 未解決の判断

1. **`references/worktree-entry.md` を別に作るか、`resume.md` に寄せるか。**
   **寄せる**——**どちらも再開の周でしか読まない**ので、分けると読む条件が 2 つになる。

## 5. 関門の進捗（再開点）

**周の大きさ**: 触る規範は **1 箇所**（`SKILL.md` 手順 4 の当該塊）。**移設であって
書き換えではない**ので複製は増えない。

**交絡を潰した手順**: **この周に実験は無い。** 段 1 で測った「参照は起点に載らない」の
実測に乗っている（`CLAUDE.md` に前提として記録済み）。

| 関門 | 状態 |
| --- | --- |
| 手順 4 の実装 | **完了** |
| 手順 5 の Verifier | **段 4 のあとに全体へ掛ける**（人間の判断） |
| `/code-review` | **同上** |

**移設前の実測**: `SKILL.md` **1,610 行 / 77,043 文字**。

**段 3 後の実測**:

| | 段 2 後 | 段 3 後 |
| --- | --- | --- |
| 起点に載る分（`SKILL.md`） | 77,043 文字 / 1,610 行 | **66,011 文字 / 1,423 行**（**−11,032 / −14.3%**） |
| 起点に載らない分 | 3 本・339 行 | **3 本・552 行** |

**規範が落ちていないことは機械で確かめた**——**移した 213 行の本体が、
`references/resume.md` に 1 行残らず入っている**ことをアサートした
（見つからなかった 1 行は、**節見出しに変換した元の行そのもの**）。

**スクリプトは実際に走らせて確かめた**——新規で作れる / 2 回目は断る / 数字以外を断る /
**別の worktree から走らせても入れ子で生えない** / worktree でブランチ一致なら通す /
不一致で止まる / メインの作業ツリーで止まる / detached を見分ける。

**書いている途中で変異が 3 件生き残り、どれも実装かテストの欠陥だった**:

1. **「相対パスで作る」がメインの作業ツリーからの観測では殺せなかった**
   ——**別の worktree から走らせる観測**を足して殺した。
2. **「肯定的な確認」の比較が到達不能だった**——`-b` で切った直後なので必ず一致する。
   **死んだコードを残さず、実際の成果物である*出力*を守る対象に変えた。**
3. **「守る」8 行に対して変異が 5 件しか無かった**——**テスト自身が数えて落ちた。**
   **3 件足して 1 対 1 にした。**
