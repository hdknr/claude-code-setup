# #171 — 関門の指摘を「実物で失敗を示せるか」で分け、示せないものは TODO に回す

## 周の在り処

- ブランチ: `issue/171-triage-by-demonstrable-failure`
- worktree: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-171`
- 起点: `8a34511`（Merge pull request #170）
- base: 参照 `origin/main`（`refs/remotes/origin/HEAD` から解決）→ `8a345114b0a79baf3077f208e0e6cc1556dfd4e9`
- 入場: `prepare-worktree.sh` で作成 → `EnterWorktree(path)`。**`ExitWorktree` は畳まない**（path で入ったため）

## 1. 変更範囲

触る:

- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 手順 6 に採否の表の規定・止まって相談する箇所の選択肢の順・手順 4 に「Issue に無い機能」・手順 3 の 6 番目から指し先・版のバナー
- `plugins/dev-loop/.claude-plugin/plugin.json` — 版 1.33.0 → 1.34.0（minor: 手順の追加）
- `.claude-plugin/marketplace.json` — 同上
- `docs/plugins/dev-loop-design.md` — **`skill-metrics.py` の生成ブロックだけ**
- `docs/plans/issue-171.md` — この計画ファイル

触らない:

- `prompts/verifier.md` / `prompts/review-handoff.md` / `agents/dev-loop-verifier.md` — 関門は指摘を挙げる側で、採否は親の仕事。関門に分類させると著者の分類の牽制にならない
- `references/*.md` — `resume.md` の「採否」は手順 3 の 6 番目を指しているだけ（指し先は変えない）
- `README.md`・設計ドキュメントの本文 — 指すだけ（Issue の受入基準）
- 分類を機械で検査する歯止め（新しいスクリプト）— Issue の「この周でやらないこと」

## 2. デプロイ経路

main へのマージで GitHub Pages が再デプロイされる（`docs/` の生成ブロックが変わる）。
プラグインは version bump で配布（利用者側は `plugin update`）。

## 3. verify の受入基準

### 不変条件

| # | 不変条件 |
| --- | --- |
| A | 関門の指摘の**採否の表**に「**実物で失敗を示せるか**」の列が**必須**で、**既定は TODO**。示せないものは直さず **PR の TODO に書く**——`SKILL.md`（原本）に入っている |
| A2 | A の既定 TODO が、**受入基準を緩める出口にならない**（受入基準に対する反証・未証明の指摘は、手順 5 の分類に従い、TODO に回して閉じられない） |
| B | 止まって相談するとき、「**TODO に回して閉じる**」が**最初の選択肢**として置かれている |
| C | **Issue に無い機能を足したくなったら、その時点で計画の「触らない」に書く**——手順 4 の「変更範囲の外へ黙って出ない」の具体化として入っている |
| D | A・B・C のどれも**禁止の形（「〜しない」だけ）で書いていない**——「表に書く」「選択肢に置く」「計画に書く」の、跡が見られる形 |
| E | 設計ドキュメント・README に規範を**再掲していない**（指すだけ） |
| F | 版が 3 箇所で 1.34.0、`skill-metrics.py --check` が緑、`check-all.py` が緑、PR の CI が緑 |
| G | 文面が主張する事実（#161・#168 の件数など）が、計画ファイル・PR の記録と一致する |

### 破りうる経路（指摘が生まれ、採否が決まる入口）

| 経路 | 守り方 |
| --- | --- |
| 手順 5 の Verifier の反証 | A の規定がこの経路も覆うと書く。ただし A2 で受入基準への反証は TODO に落とせない |
| 手順 6 の `/code-review` 1 パス目・2 パス目 | A の本体 |
| 手順 6 の fan-out／多レンズ化した各体の指摘 | 「関門の指摘」と一般化して書く（体の数に依らない） |
| 3 パス目以降の止まって相談 | B |
| 計画ファイル（手順 3 の 6 番目）に採否を畳む | 6 番目から A を指す（複製しない） |
| 再開の周（`references/resume.md`） | 手順 3 の 6 番目を指しているので、A に届く（変更不要を確認する） |
| 指摘対応の中で機能を足す | C（手順 4 の項は手順 6 の指摘対応にも掛かると既に書いてある——それを確かめる） |
| PR コメントの「残課題」 | A の TODO の着地点と食い違わないか |

### 生成物

- **該当あり**: 設計ドキュメント §8.2 の `skill-metrics.py` 生成ブロック（`SKILL.md` の節構造に依存）。再生成して `--check` 緑。
- 図（drawio）は触らない——`check-diagram-freshness.py` 緑で確認。

### この環境では証明できないもの（未証明）

- **これで実際に周が小さくなるか**（1 周では母集団にならない）
- **分類が甘くならないか**（分類者は著者自身。表に跡が残るので関門・人間が指摘できる、まで）

### 必須要件の掛け算

受入基準 × {Verifier, `/code-review`} × {1 パス目, 2 パス目} = 4 つの受け渡し。

## 4. 未解決の判断

- なし（着手時点）

## 5. 関門の進捗（再開点）

- 実装: 完了（1 コミット目）。`check-all.py` 28/28 緑
- Verifier: 未
- `/code-review`: 未
- 反証・差し戻し: なし
- 交絡: 実験なし

## 6. 既知の限界・決着済みの論点

- **分類の歯止め（スクリプト）は作らない** — 決着: 採らない。理由: Issue の「この周でやらないこと」。歯止めの新設は周をいちばん重くする型
- **関門（委譲文）に分類をさせない** — 決着: 採らない。理由: 採否は親の仕事で、関門に分類させると同じ判断を 2 箇所に置くことになる
