# Issue #148 — 周の base をどこから取るかを 1 つに決める

> 周の base が**切る側・関門に渡す側・子への指示**の 3 つでばらばらに決まっていて、
> どれも閉じていない。[#145](https://github.com/hdknr/claude-code-setup/issues/145) の周で
> **2 度規定し、2 度とも関門に「その手順では塞がらない」と反証された**ので、人間の判断で分割した。

## 0. 周の在り処

- ブランチ: `issue/148-base-selection`
- worktree: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-148`
- **起点コミット: `fdca142`**（切った当時の `issue/145-gate-claims` の先端）
- **base の参照: `origin/main`** ／ **base の SHA: `fdca142f43d0ab747ab1b99dc0a61477654e7b55`**
  ——**参照と SHA の両方を書くのは、この周が決めた規定そのものである**（`SKILL.md` 手順 4）。
- **`#145` が周の途中で main にマージされたので、参照が `issue/145-gate-claims` から
  `origin/main` に変わった。SHA は変わらない**——
  **`#149` は merge であって squash ではない**ので `fdca142` は `origin/main` の祖先のままで、
  **`git merge-base origin/main HEAD` が同じ SHA を返す。**
  **これが「記録した SHA は値であって定義ではない」の実例である**（指摘 5）。
  **rebase はしない**——**しなくても PR の差分はこの周の分だけになる**（GitHub も merge-base を使う）。

### この周は `main` ではなく #145 のブランチに積んでいる（stacked）

**人間の判断**（`AskUserQuestion` で 3 択を出し、「#145 のブランチから積む」が選ばれた）。
理由: **#145 が未マージのまま、この周が埋めるべき空欄をブランチ側に持っている**
——`SKILL.md:734` / `:1041` の「**この周では規定しない**（#148）」と、
委譲文 2 本の `{{その周の取り方を、base まで含めて書く}}` スロット。
`main` から切ると、その空欄もスロットも存在しない。

- **着手時の PR の base は `issue/145-gate-claims` の予定だった。**
- **周の途中で #145 が main にマージされた**（PR #149、**2026-09-21 07:20:24Z**）。
  **`issue/145-gate-claims` は origin から消えている。**
  **したがって PR は `main` に出す。**
- **この周の着手中に #145 は `a8ab67c` → `fdca142` → マージ、と 2 度動いた。**
  **base の参照は動いたが、SHA は動かなかった**——**式で定義しているから追随できた。**

### `prepare-worktree.sh` を使ったが、起点は手で直した（申告）

**`SKILL.md` 手順 4 の「新規の周は、スクリプトで作る `（必須）`」は満たした**が、
**スクリプトは start-point を取れない**——**それがこの Issue が直す欠陥そのもの**なので、
**この周だけは切ったあとに `git reset --hard issue/145-gate-claims` で起点を乗せ替えた。**
スクリプトが与える守り（置き場所・命名規約・先に見る・肯定的な確認）はすべて通っている。
**黙って落としていない**（原則「必須手順を落とすときは、黙って落とさない」）。

**実際に、スクリプトは `main` の HEAD（`1983049`）の上に切った**——
**この Issue の失敗 1 が、この周の 1 手目で再現した。**

## 1. 変更範囲

触る:

- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 手順 4 に規定の原本、手順 5・6 は指し先に置き換え
- `plugins/dev-loop/skills/dev-loop/scripts/prepare-worktree.sh` — start-point の解決と受け渡し
- `plugins/dev-loop/skills/dev-loop/prompts/verifier.md` — スロットを具体の形にする
- `plugins/dev-loop/skills/dev-loop/prompts/review-handoff.md` — 同上（2 本を同じ形に）
- `plugins/dev-loop/agents/dev-loop-verifier.md` — 衝突が残っていないかの点検（**変更しない可能性が高い**）
- `scripts/test-worktree-scripts.py` — 観測と変異を 1 対 1 で足す
- `plugins/dev-loop/.claude-plugin/plugin.json` — version
- `.claude-plugin/marketplace.json` — version
- `docs/plugins/dev-loop-design.md` — `skill-metrics.py` の生成ブロック（手で書かない）
- `docs/plans/issue-148.md` — この計画ファイル
- `CLAUDE.md` — `test-worktree-scripts.py` の**変異の件数が実際と食い違っている**
  （**2 箇所とも「8 件」だが実際は 12 件**。この周で件数が動くので併せて直す）
- `plugins/dev-loop/skills/dev-loop/references/resume.md` — **1 パス目に広げた**。
  **`/code-review` の指摘 6**——**手で `git worktree add` する復旧の道が、
  start-point 無し・相対パスのまま**だった。**経路 3 を「手順 4 が守る」と書いたのに、
  実物がその形になっていなかった**（**規定を置くのではなく、例を直す**）

触らない:

- `diagrams/` 一切（drawio を編集しないので書き出しの鮮度は動かない）
- `docs/` の `plans/` `plugins/dev-loop-design.md` 以外
- 他のプラグイン（`cmux` / `workspace-setup`）以外は上の「触る」を正とする
- 他のプラグイン（`cmux` / `workspace-setup`）

## 2. 乗るデプロイ経路

- **`docs/` を触るので、`main` への push が GitHub Pages のデプロイになる**
  （`CLAUDE.md`「デプロイ」／`.github/workflows/docs.yml`）。**「ドキュメントだから経路が無い」ではない。**
- プラグインの配布は `.claude-plugin/marketplace.json` 経由。**version を 3 箇所で揃える。**
- **ただし PR の base が `issue/145-gate-claims` なので、この PR のマージだけでは本番に出ない**
  ——**#145 がマージされて初めて `main` に乗る。**

## 3. verify の受入基準

### 決めた形（これが規定の本体。1 箇所に書く）

> **周の base は `git merge-base <既定ブランチのリモート追跡参照> HEAD` が返す SHA である。**
> **親が解決し、SHA のまま関門に渡す。関門は `git diff <SHA>`（2 点）で取る。**
> **未追跡ファイルは `git status --porcelain` で併せて見る。**
> **切る側（`prepare-worktree.sh`）は、同じ参照を start-point にする。**

**なぜこの形か——Issue が挙げた 4 つの失敗を、1 つずつ潰している:**

| 選択 | 潰れる失敗 |
| --- | --- |
| 名前ではなく **SHA** | 子の手元が古くても動かない。**取り直しが 1 回も要らない**ので、委譲文の「副作用のあるコマンドを使うな」と**衝突しない** |
| 先端ではなく **merge-base** | 他人のマージ済みの作業が、**追加としても削除としても**差分に入らない |
| 3 点ではなく **2 点** | **未コミットの実装が見える**（3 点は作業ツリーを含まない） |
| `git status --porcelain` を併せる | **`git diff` は未追跡を出さない**——2 点にしただけでは新規ファイルが見えない |
| start-point を渡す | **`git worktree add -b` は省略時に HEAD に落ちる**ので、古いローカル既定ブランチの上に切らない |

**SHA が子の手元に必ず在る理由（取り直しが要らない根拠）:**
**merge-base は定義上 HEAD の祖先**なので、**HEAD を持つ作業ツリーには必ず在る**。
これが「子への指示」の衝突（Issue の 3 番目）を**規定を足すのではなく、要らなくすることで**閉じる。

### 不変条件と、それを破りうる経路

**経路は差分ではなく「base が決まる／渡される入口」の一覧から数えた。**

不変条件:

- **A**: base は周を切った分岐点を指す（他人のマージ済みの作業が、**どちらの向きにも**差分に入らない）
- **B**: 関門が見る差分に、**未コミット・未追跡の実装が含まれる**
- **C**: base の**決め方**の原本は 1 箇所しかない（指し先は列挙を持たない）
- **D**: 委譲文の指示が、同梱 agent 定義の禁止（副作用のあるコマンドを使うな）と衝突しない
- **E**: 子の手元の状態（古いリモート追跡参照・fetch の有無）に結果が依存しない
- **F**: 切る側が、古いローカル既定ブランチの上に切らない

**base が「決まる」入口:**

| # | 入口 | 守る不変条件 | どう守るか |
| --- | --- | --- | --- |
| 1 | `prepare-worktree.sh` | F | start-point を解決して `git worktree add -b … <start-point>` に渡す。**解決結果を出力する** |
| 2 | `EnterWorktree` の `name` 経路（道具が切る） | F | **道具の `worktree.baseRef` 既定が `fresh` = `origin/<既定>`** なので既に満たす。**規定は当てられない**（道具の設定）——**満たしていることを書くに留める** |
| 3 | 手で `git worktree add`（復旧の道。`references/resume.md`） | F | 手順 4 の規定が start-point を要求する |
| 4 | 再開した周 | A | **計画ファイルの「周の在り処」に記録した base** を使う（作り直さない） |

**base が「渡される」入口:**

| # | 入口 | 守る不変条件 | どう守るか |
| --- | --- | --- | --- |
| 5 | `prompts/verifier.md` | A B E | 親が解決した SHA をスロットに入れる。取り方は `git diff <SHA>` ＋ `git status --porcelain` |
| 6 | `prompts/review-handoff.md` | A B E | **5 と同じ形**（#59 の「片側だけ直す」型を作らない） |
| 7 | 手順 5 の多レンズ化（各レンズ） | A B E | 委譲文 2 本を使うので自動的に掛かる |
| 8 | 手順 6 のレビュー fan-out（各体） | A B E | 同上 |
| 9 | `Workflow` のノード | A B E | 同上 |
| 10 | 同梱 agent 定義 `dev-loop-verifier.md` | D | **何も書かない。** 規定を足さないことが守り方である |
| 11 | `check-plan-scope.py <base-ref>`（CI と手元） | A | **この周では `origin/main` ではなく base SHA を渡す**——stacked なので `origin/main` だと #145 の差分が全部この周のものに見える |
| 12 | `check-version-bump.py` / `check-description-sync.py` | A | 同上（同じ base SHA を渡す） |

**11・12 は差分を読んでいたら出てこない経路である。** リポジトリ自身の歯止めが base を引数に取る。

### 証明できるもの（この環境で verify する）

| # | 受入基準 | 証明手段 |
| --- | --- | --- |
| V1 | **origin が周を切った時点より進んでいても、他人の作業が差分に混ざらない**（A） | 合成リポジトリで、切った後に既定ブランチを進めてから `git diff <merge-base>` を取り、**他人のコミットのファイルが出ないこと**を見る。**先端の SHA を使った場合は「削除」として出ること**（陽性対照）も併せて見る |
| V2 | **未コミットの実装が関門に見える**（B） | 同じ合成リポジトリで、コミットせずに編集して `git diff <SHA>` に出ること。**3 点では出ないこと**（陽性対照）も見る |
| V3 | **未追跡の新規ファイルが見える**（B の穴） | `git status --porcelain` に `??` で出ること。**`git diff <SHA>` には出ないこと**も見る |
| V4 | **`prepare-worktree.sh` が start-point の上に切る**（F） | `test-worktree-scripts.py` に観測と変異を足す。**古い既定ブランチを作った合成リポジトリで、新しい側の上に切ること** |
| V5 | **`origin` が無いリポジトリでも作れて、そのことを黙らない**（F の限界） | 合成リポジトリ（`make_repo` は `origin` を持たない）で rc=0 かつ**出力に「HEAD から切った」と出る**こと |
| V6 | **base の決め方の原本が 1 箇所**（C） | `SKILL.md` を横断して `merge-base` の**規定**が 1 箇所か数える。手順 5・6 は**指し先だけ**で列挙を持たないこと |
| V7 | **取り直しを求める文がどこにも無い**（D） | **機械で見ているのは同梱 agent 定義だけ**（`git fetch` ／「取り直して」）。**`SKILL.md` と委譲文 2 本は、いまはどちらも「取り直しは要りません」と*書いている*ので、語の在不在では見られない**——**目視である。**（**当初「3 つとも grep する」と書いたが、実装はそうなっていなかった**。`/code-review` の 2 パス目が指摘） |
| V8 | **委譲文 2 本が同じ形**（D の鏡像） | 2 本の該当行を並べて突き合わせる |
| V9 | **既存の 12 変異が全部生きている** | `python3 scripts/test-worktree-scripts.py` |
| V10 | **歯止めが全部緑** | `python3 scripts/check-all.py`（**1 本ずつ選ばない**） |
| V11 | **version が 3 箇所で揃っている** | `check-plugin-versions.py` |
| V12 | **`skill-metrics.py` の生成ブロックが現在** | `python3 scripts/skill-metrics.py --check` |
| V13 | **切ったブランチに upstream が付かない**（指摘 1） | 合成リポジトリで切って `git rev-parse --abbrev-ref HEAD@{upstream}` が失敗すること。**変異で当てる** |
| V14 | **`origin/HEAD` が壊れていても、作るか・作らない理由を言うかのどちらか**（指摘 2） | dangling な `refs/remotes/origin/HEAD` を作って rc と出力を見る。**rc=128 で死なないこと** |
| V15 | **断る周（rc=3）ではネットワークに出ない**（指摘 3・12） | fetch を「先に見る」より後に置く。**呼ばれた回数を数える**（偽の `git` で観測） |
| V16 | **base の式が、積んだ周でも成り立つ**（指摘 4） | `DEV_LOOP_BASE_REF` が `SKILL.md` に書かれていること。**この周自身が stacked なので、自分が反例になっていないこと** |
| V17 | **記録した SHA は値であって定義ではない**（指摘 5） | **関門に渡す前に `git merge-base --is-ancestor` と、式の当て直しを要求する**規定が在ること。**rebase 後に記録値が古くなる形を、テストで再現する** |
| V18 | **復旧の道（`resume.md`）の例が、start-point と絶対パスを持つ**（指摘 6） | 当該行を読む。**`check-skill-pointers.py` は形までは見ない**ので、目視＋テスト |
| V19 | **2 つの変異が互いに区別できることを、テスト自身がアサートする**（指摘 9） | **主張に対応する検査を置く**——いまは `correct` としか比べていない |
| V20 | **委譲文の形が壊れたら赤くなる**（指摘 10） | `prompts/` を 3 点に戻す・`status --porcelain` の行を消すと**落ちる**こと |
| V21 | **clone の失敗が「変異を殺せた」に化けない**（指摘 11） | rc を見る。`except Exception` の握り潰しを狭める |

### この環境では証明できないもの（未証明。人間レビューに回す）

| # | 未証明の項目 | なぜ原理的に証明できないか |
| --- | --- | --- |
| U1 | **`EnterWorktree` の `worktree.baseRef` 既定が本当に `origin/<既定>` か** | **道具の説明文が出所**で、**動かして確かめていない**。**この周では測らない**——測るには実際に `name` で worktree を作る必要があり、**この周は既に worktree セッションの中にいるので新規作成が拒否される**（手順 4）。**「道具の説明文にそう書いてある」までしか言わない** |
| U2 | **実際に他人の PR が並行してマージされた状況での挙動** | 合成リポジトリは**他人のマージを模したもの**であって、実際の GitHub の並行マージではない。**模せる範囲は「分岐して先に進む」まで** |
| U3 | **この規定が次の周で実際に守られるか** | **規範が著者に届くかは、この周では測れない**（`norms-dont-reach-their-author`）。**#145 は 3 度書き直して 4 度反証された**ので、**「今度は閉じた」は主張しない** |

### 生成物の鮮度（該当判定）

**該当あり。** 数え上げた先: `CLAUDE.md` の生成手順・`scripts/` の生成タスク・CI の鮮度チェック。

| 生成物 | 該当 | 対応 |
| --- | --- | --- |
| `skill-metrics.py` → 設計 §8.2 の生成ブロック | **該当する**（`SKILL.md` を編集する） | `python3 scripts/skill-metrics.py` で再生成し、`--check` で差分ゼロを確認。**手で書かない** |
| `export-diagrams.py` → `docs/images/` | **該当しない**（drawio を触らない） | `check-diagram-freshness.py` が緑であることだけ見る |
| version（3 箇所） | **該当する** | `check-plugin-versions.py` |

## 4. 未解決の判断・確認待ち

- **PR の base を `issue/145-gate-claims` にする**。#145 が先にマージされたら `main` へ付け替える。
  **#145 が動き続けているので、着地の直前に base を再解決する。**
- **決着: `prepare-worktree.sh` は `fetch` を打つ**（最善努力）。
  **オフラインでも止めない**が、**取り直せなかったことは出力する。**
  `GIT_TERMINAL_PROMPT=0` を付けて、認証を訊かれて固まるのを防いでいる。
  **「fetch すべきでない」という判断もありうるので、関門に問う。**
- **決着: 既定ブランチの解決順序**は
  `DEV_LOOP_BASE_REF` → `refs/remotes/origin/HEAD` → `origin/main` → `origin/master` → **HEAD**。
  **推測したことも、HEAD に落ちたことも出力する。**
- **決着: 実証の置き場所は `test-worktree-scripts.py` の中**（別ファイルにしない）。
  **別ファイルにすると `check-all.py` と CI の yaml に二重登録が要り、
  このリポジトリが 2 度踏んだ「登録し忘れ」の型**（`test_ci_runs_every_registered_script`
  の docstring）**に当たる。**
- **残った問い: `check-all.py` が 120 秒を超えるようになった。**
  **`observe()` が 17 回走り、そのたびに 2 つのクローンを作る**ため。
  **この周では分けない**——**測ってから決める**（手順 3 の「閾値は決めない」と同じ扱い）。

## 5. 関門の進捗（再開点）

**ここに書くのは、この周で実際に起きたことだけである**（規定の引用ではない）。

- 周の在り処: 上記「0. 周の在り処」
- **実装: 完了**（`82bf3a2`）。**歯止めは 26/26 緑**
  （`python3 scripts/check-all.py --base $(git merge-base issue/145-gate-claims HEAD)`)。
  **`origin/main` を base にしてはならない**——stacked なので #145 の 610 行が全部この周のものに見える。
- **交絡を潰した手順: 実測した。**
  1. **陽性対照を 2 つ取った**（`demonstrate_base_rule`）——
     **先端の SHA では他人の作業が削除として出る**／**3 点では未コミットの実装が出ない**。
     **「混ざらない」「見える」だけでは、もともと出ない値と区別がつかない**ため。
     **`assert mb != tip`** をテストに入れて、**#145 の周で区別できなかった配置
     （tip == merge-base）に落ちていないこと**を毎回確かめている。
  2. **2 つの変異が見分けられることを、実際に測った。**
     「start-point を渡さない」と「取り直さない」は、
     **未取得の場面だけを見るとどちらも `A（古い）`** で**区別できない**。
     **取得済みの場面を足すと `A` / `B` に分かれる**。
     食い違う観測キーは**取得済みの場面の 2 つだけ**だった——
     **つまりこの場面が、2 変異を分けている唯一の観測である。**
  3. **観測窓**: `prepare-worktree.sh` の 1 回の実行と、その worktree の HEAD。
     **ネットワーク越しの `origin` は測っていない**（合成リポジトリの `origin` は
     ローカルの bare リポジトリである）。
- **手順 4 の割り目: `AskUserQuestion` で訊き、「このまま続ける」が選ばれた**（割らなかった）。
- Verifier 1 パス目: **通した。反証 0 件**（2026-09-21、`sonnet`。実装は Opus）。
  **運用リスク 1 件（#145 のマージ）と観察 1 件（fetch の位置・ssh）を併せて報告してきた。**
  **どちらも親の側で独立に確かめた**——**#145 のマージは `ls-remote` / `gh pr view` /
  `--is-ancestor` で、fetch の位置はコード順で。**
- `/code-review` 1 パス目: **通した。指摘 12 件**（high 1・medium 6・low 5）。
  **12 件とも親の側で再現を確かめ、12 件とも採った**（下の「1 パス目の指摘と採否」）。
  **ただし指摘 1 の「a bare `git pull` がマージする」は、実測では git が拒否した**
  ——**設定次第**である。**断定のほうは採らなかった。**
- 指摘対応: **完了。** 歯止めは **26/26 緑**。
- **手順 6 の 2 つ目の割り目: 訊いていない**——**人間が「2 パス目を回して」と
  直接指示したので、割り目の問いはその指示が先に答えている。**
  **黙って飛ばしたのではなく、指示に置き換わった。**
- 手順 5 の当て直し（Verifier 2 パス目）: **通した。反証 0 件**（2026-09-21、`sonnet`）。
  **恒真チェックでないことまで裏取りしてきた**——**`base` 時点の `SKILL.md` に
  `git merge-base` が 0 件**だったことを確認したうえで、
  **リポジトリ外の独立コピーで手動変異**を当て、
  **委譲文を旧形（3 点許可・`--porcelain` 無し）に戻すと `check_norm_shape` が 6 件落ちる**／
  **`--no-track` を外すと `upstream が付いていない` が転じる**／
  **`fetch` を「先に見る」より前に戻すと順序の検査が落ちる**／
  **`rev-parse --verify` のガードを潰すと dangling の場面で rc が変わる**ことを確認。
  **指摘 9・10 で足した検査が実際に効いている、という裏付けが取れた。**
- `/code-review` 2 パス目: **通した。指摘 9 件**（high 1・medium 3・low 5）。
  **9 件とも親の側で再現を確かめ、9 件とも採った**（下の「2 パス目の指摘と採否」）。
- **3 パス目は回さないと決めた（人間の判断、2026-09-21）。**
  **2 パス目を回収したら、機械的な指摘だけ直し、残りは未解決点として明記して
  人間レビューへ出す。**
  **根拠**——**1 パス目の 12 件のうち 4 件が、この周が書いた規定そのものの誤り**だった。
  **規定を書いた当人が最初の違反者になる型**は、**追加のパスでは解けない**
  （#145 は 10 パス回して分割に至った）。
  **U3（この規定が次の周で守られるか）は原理的に未証明**なので、
  **人間の判断でしか閉じられない項目が残っている。**
  **成果物が規範そのもの**であり、`CLAUDE.md` が「**規範の書き換えは最も危険な操作**」と
  呼んでいる対象である。
- **進捗欄が一度 2 重になっていた**（同じ 4 行が 2 度）。**この計画ファイル自身が、
  この周が直している「同じことを 2 箇所に書く」型を踏んだ。**

## 1 パス目の指摘と採否

**Verifier（`sonnet`）: 反証 0 件。** 運用リスク 1・観察 1。
**`/code-review`（high）: 指摘 12 件。** **12 件とも親の側で再現を確かめ、12 件とも採った。**

| # | 指摘 | 採否と対応 |
| --- | --- | --- |
| V-A | **#145 が周の途中で main にマージされた**（PR #149、07:20:24Z） | **採る。** 独立に確認（`ls-remote` / `gh pr view` / `--is-ancestor`）。**merge であって squash ではない**ので `fdca142` は `origin/main` の祖先。**PR は main に出す。** |
| V-B | `fetch` が「先に見る」より前 ／ `GIT_TERMINAL_PROMPT=0` は ssh を止めない | **採る**（review 3・12 と同じ）。 |
| 1 | **start-point にリモート追跡参照を渡すと upstream が付く**。`git pull` が既定ブランチを周に取り込みうる | **採る。`--no-track`。** **ただし報告は「a bare `git pull` merges」と書いていたが、実測では git が divergent branches で*拒否*した**——**設定次第**である。**断定を写さない。** |
| 2 | **dangling な `origin/HEAD`** でも `symbolic-ref` は成功し、`worktree add` が rc=128 で死ぬ | **採る。** 解決後に `rev-parse --verify` で検証し、**死なせず HEAD に落ちて言う。** |
| 3 | ssh の passphrase / 未知のホスト鍵で固まりうる | **採る。** `BatchMode=yes` ＋ `ConnectTimeout=5`。**ssh では測っていないと明記した。** |
| 4 | **原本の式が stacked な周に当たらない。`DEV_LOOP_BASE_REF` が `SKILL.md` に無い** | **採る。** 式を「**その周を切った元の参照**」に直し、**stacked と環境変数を原本に書いた**。**この周自身が反例になっていた。** |
| 5 | 「切った直後の HEAD がそのまま base」「選び直さない」が、計画の「着地の直前に再解決する」と矛盾 | **採る。** **記録した SHA は値であって定義ではない**——**系譜が動いたら式を当て直す**、**禁じているのは*違う形*を選ぶこと**、と書き分けた。**`--is-ancestor` の確認も足した。** |
| 6 | `resume.md` の手で切る例が **start-point 無し・相対パス** | **採る。変更範囲を広げた**（§1）。**`--no-track` も足した**（1 と同じ穴が手の道にもある）。 |
| 7 | `worktree.baseRef` は設定可能で、`head` なら F を破る | **採る。**「既定だから満たしている」と書かない、に直した。 |
| 8 | `resume.md` の「upstream なし」が 1 の副作用で偽になる | **採る（1 の修正で解消）。** `--no-track` なので upstream は付かず、**元の記述は真のまま。** |
| 9 | **2 つ目のクローンの目的に、対応する検査が無かった**（`correct` としか比べていない） | **採る。** **変異どうしを比べる検査を足した**——**主張が検査に載った。** |
| 10 | `demonstrate_base_rule` は **git の性質**を見ているだけで、**規定が壊れても緑** | **採る。** `check_norm_shape()` を足し、**委譲文を 3 点に戻す・`--porcelain` の行を消すと赤くなる**ようにした。 |
| 11 | `git clone` の rc 未検査 → 失敗が `except Exception` で「変異を殺せた」に化ける | **採る。** rc を assert。 |
| 12 | `DEV_LOOP_BASE_REF` がローカル参照でも `fetch` する | **採る。** リモート追跡参照のときだけ打つ。 |

**指摘 4・5・9・10 は、この周の中核を直している**——
**4 は原本の式そのもの、5 は「記録と定義」の取り違え、9・10 は「主張に対応する検査が無い」型。**
**9 と 10 は同じ型が 2 箇所に出たもの**で、**どちらも「書いたことを検査が見ていない」。**

## 7. 着地（人間レビュー待ち）

- **PR: https://github.com/hdknr/claude-code-setup/pull/153**（base は `main`）
- **PR 側で緑**——`build` pass / `check` pass / `deploy` skipping / `MERGEABLE` `CLEAN`。
  **手元の緑ではなく、`gh pr checks 153` でリモートを見た。**
- **手順 7 のマージは人間の判断。** **`CLAUDE.md`・Skill の変更は PR で人間レビューを通す。**
- **手順 8 の焼き戻し**: **規範側はこの PR に入っている**（`CLAUDE.md` の件数、
  `SKILL.md` / `references/resume.md` の規定）。
  **永続メモリは PR を通らない**ので、**観測した事実だけ**を
  `checks-add-their-own-bugs` に畳んだ——
  **「直すために書いた検査が同じ型で壊れる」**と**「前の観測が後の前提を汚す」**の 2 つ。
  **どちらもこの周で実際に起きたことで、外部テキストの主張ではない。**

## 2 パス目の指摘と採否

**Verifier（`sonnet`）: 反証 0 件。** **恒真チェックでないことまで裏取りしてきた**
（リポジトリ外の独立コピーで手動変異）。
**`/code-review`（high）: 指摘 9 件。9 件とも再現を確かめ、9 件とも採った。**

| # | 指摘 | 採否と対応 |
| --- | --- | --- |
| 1 | **HIGH。指摘 12 の修正が、#148 の欠陥を stacked 経路に作り直していた。** 解決できない参照を「ローカル参照だから取り直し不要」と読み、取り直しを飛ばして HEAD に落ちる | **採る。** **最初の再現は空振りした**——`origin/main` は手元で解決できるので落ちない。**クローンより後に origin へ生えたブランチ**で再現した。 |
| 2 | **名指しの base が解決しないのに rc=0 で HEAD に落ちる。** 呼ぶ側は「0 以外はすべて作っていない」と読む | **採る。終了コード 7 で断る。** |
| 3 | **`--no-track` が HEAD 経路に無い。** `branch.autoSetupMerge=always` で upstream が付く | **採る。** **`git worktree add` の呼び出しを 1 箇所にまとめた**——**2 箇所あったから片方だけになった。** |
| 4 | **MEDIUM。指摘 10 を直すために足した `check_norm_shape` が、その退行を捕まえない。** 委譲文を 3 点に戻しても 71 ok のまま | **採る。形を当てる正規表現にした。** **「検査が主張を見ていない」型の 3 度目。** |
| 5 | **3 点の陽性対照が、点の数と base の 2 つを同時に変えていた** | **採る。** `tip` → `mb`。**この周が他所で掲げている交絡の規定を、自分の対照で破っていた。** |
| 6 | 不変条件 D の検査が生の部分文字列（`WebFetch` を足すだけで赤くなる） | **採る。** `git fetch` / `取り直して` に狭めた。**V7 の申告も実装に合わせて直す。** |
| 7 | 壊れた参照の経路で出力が自己矛盾（「解決できなかった」と「解決したが壊れていた」を同時に主張） | **採る。** |
| 8 | 取り直しを後ろに回したので、「先に見る」が古い参照で見る | **採る（「何を守らないか」に書いた）。** **直さずに、守らない範囲として明示した。** |
| 9 | `except Exception` が、足場の失敗まで「変異を殺せた」に丸める | **採る。** 例外の型を検査名に出す。 |

**指摘 1 の修正は、1 度目が誤っていた。**
**`git rev-parse --symbolic-full-name` は、解決できないとき*引数をそのまま返す***
（エラーは stderr）。**空文字と比べる書き方では、この場合が `*)` に落ちる。**
**捕まえたのはテストのほう**——「変異を殺せない」で落ちた。
**`--verify` で直接見る形に直した。**

**この周は、同じ型を 3 度踏んでいる**——**「書いたことを検査が見ていない」**。
**1 パス目の指摘 9・10、2 パス目の指摘 4。**
**3 度目は、2 度目を直すために書いた検査そのものだった。**

## 6. 既知の限界・決着済みの論点（両方の関門に毎パス渡す）

- **U1・U2・U3 は未証明**（上の表）。**再指摘は決着済みとして扱う。**
- **`EnterWorktree` / `ExitWorktree` の意味論には規定を当てない**——**スクリプトから呼べない**
  ので、**この周が変えられるのは素の git で済む部分だけ**（`SKILL.md` 手順 4 の既定）。
- **#145 の周で採らなかった 4 形は、再提案しない**——
  **(a) 委譲文に「取り直してから」／(b) 名前（`origin/<既定>`）で渡す／(c) 先端の SHA で渡す／
  (d) 3 点 `<base>...HEAD`**。**4 形とも関門に反証済み**で、理由は上の「なぜこの形か」の表にある。
- **この周は #145 の上に積んで始まり、途中で #145 が main にマージされた。**
  **base の SHA は `fdca142` のまま**（式が同じ値を返す）。**`origin/main` を base にしない。**
- **1 パス目の 12 件は全部採った。** **再指摘は要らない**——
  **ただし直した先がまた誤っている可能性は残る**ので、そこは見てほしい。
- **指摘 1 の「a bare `git pull` がマージする」は、実測では再現しなかった**
  （git が divergent branches で拒否した）。**`--no-track` は採ったが、断定は採っていない。**
- **ssh リモートでの `fetch` の挙動は測っていない**（このリポジトリは https）。
  **`BatchMode=yes` / `ConnectTimeout=5` は渡しているが、「固まらない」とは書いていない。**
