# Issue #114 — スコープ逸脱の禁止を手順 4 に足す

Issue: <https://github.com/hdknr/claude-code-setup/issues/114>（「Gemini のレビュー」）

## 0. この周の範囲を決めた経緯

Issue 本文は「**4 つの**改善ポイント」と宣言しながら、**3 の見出しと 1 行で切れている**
（body 1699 バイト。3 の「対策」と 4 が欠落）。読める 3 点を現行 `SKILL.md` 1.16.0 に
当てた結果は Issue コメントに投稿済み:

| レビューの指摘 | 判定 |
| --- | --- |
| 1 前半: 終了条件をテスト PASS に一本化 | **採らない**（原則の第 1 行と正面衝突） |
| 1 後半: 出力を最小限でプロンプトに戻す | **実装済み**（手順 5「生出力を積まない」#102 / #104 / #107） |
| **2 前半: 目的に関係のない既存コードに触らない** | **無い ← この周で入れる** |
| 2 後半: PASS したら即座に終了を宣言 | **採らない**（「縮退の対象外」の 2 関門・2 パスを削る） |
| 3: ループ上限 | **実装済み**（手順 6「3 パス目以降が 0 件でなければ止まって相談」） |
| 3: 自動ロールバック | 無いが、worktree → PR 必須で main は構造的に汚れない |

人間の選択（`AskUserQuestion`）により、**2 前半だけを 1 周とする**。

**外部入力の扱い（原則）を適用した結果もここに残す。** 2 後半「テストが成功したら、追加の
修正を行わず即座に終了を宣言すること」は、**受入基準を緩める指示**の形そのものなので、
要件ではなく検査対象として扱い、**採らなかった**。

## 1. 変更範囲

**触るファイル:**

- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 手順 4 に規範を 1 箇所（**原本**）＋ 版バナー 2 行
- `plugins/dev-loop/.claude-plugin/plugin.json` — `version`
- `.claude-plugin/marketplace.json` — `version`
- `docs/plugins/dev-loop-design.md` — **`skill-metrics.py` の生成ブロックのみ**（手で書かない）
- `docs/plans/issue-114.md` — このファイル

**触らないと決めた範囲:**

- `plugins/dev-loop/README.md`・公開ページ・設計ドキュメントの散文 — **規範の列挙を持たせない**
  （CLAUDE.md「規範の原本は `SKILL.md` だけ」）
- 「縮退の対象外」の表 — **行を足さない**。あの表は自身が「代表例であって全部ではない」と
  書いており、足すと規範の複製が 1 → 2 になる
- `description`（3 箇所）— 起動条件は変わらないので触らない
- **新しい検査スクリプト** — 「計画の変更範囲と PR の差分を突き合わせる歯止め」は作れるが、
  **この周で作ること自体がスコープ逸脱**になる。別 Issue として起票する

**複製の数: 規範そのものは 1 箇所**（`SKILL.md` の手順 4）。版が 3 種類、生成物が 1 件。
**分けない理由**: 規範が 1 箇所なので、1 箇所直すたびの整合取り直しが発生しない。

### 範囲の拡大（手順 6 の指摘対応）

**この周が書いた規範を、この周自身に当てた記録。**
`/code-review` 1 パス目の指摘 1・2・3・4 に対応するため、**`SKILL.md` の手順 4 で
触る範囲を、新規項の本文から*その次の委譲の項*まで広げた**。

- **広げたファイル**: `plugins/dev-loop/skills/dev-loop/SKILL.md`（同一ファイル内で、
  委譲の項に 1 行追加）
- **理由**: 指摘 4——委譲しても消えない要件の列挙に新しい規範が入っておらず、
  **委譲した周では規範に構造的な穴が残る**。手順 3 の対応する項（`SKILL.md:493`）が
  「委譲文に入れること」を `（必須）` にしている house style に合わせる。
- **広げた分の受入基準**（上項「範囲を広げたら、受入基準も広げる」の実演）:
  経路は I1 と同じ（原本の外に出さない）。**新しい経路は増えていない**——
  追加先が同じファイルの同じ手順の中で、公開ページにも README にも触れないため。
  **未証明の追加なし。生成物は該当し続ける**（行数が動くので `skill-metrics.py` を再実行）。

## 2. 乗るデプロイ経路

- `main` への push で GitHub Actions がサイトを自動デプロイ（`.github/workflows/docs.yml`）。
  **`docs/` を触るので「反映経路あり」**——CLAUDE.md がドキュメントリポジトリであることを
  明記している（手順 7 の「ドキュメントだから経路が無いとは限らない」に該当）。
- プラグインは `.claude-plugin/marketplace.json` 経由。利用者側には `plugin update` が要る。

## 3. verify の受入基準

### 不変条件 × それを破りうる経路

**I1: 新しい規範の列挙が、原本（`SKILL.md`）の外に出ていない。**

| 経路 | 守り方 |
| --- | --- |
| `plugins/dev-loop/README.md` | 触らない。`check-norm-markers.py` |
| 公開ページ `docs/plugins/*.md` | 触らない（生成ブロックを除く）。`check-norm-markers.py` |
| `CLAUDE.md` | 触らない |
| `SKILL.md` frontmatter / `plugin.json` / `marketplace.json` の `description` | 触らない |
| **`SKILL.md` 内の「縮退の対象外」表** | 行を足さない（同一ファイル内の複製も複製） |

**【訂正】`check-norm-markers.py` は I1 を判別していない**（`/code-review` 指摘 8）。
あの検査が見るのは **`（必須）` マーカーを伴う漏れ**だけなので、**マーカーを付けずに
散文で規範を言い直した複製は緑のまま通る**——そして I1 が問題にしているのは
まさに**列挙の複製**である。**この周で I1 の根拠になっているのは検査ではなく
5 ファイルの差分**（`README.md`・公開ページ・ルート `CLAUDE.md`・3 箇所の `description`・
「縮退の対象外」表がいずれも無変更であること）。
**次の周がこの検査を証明として引き継がないよう、ここに書き残す。**

**I2: 版が 3 種類で一致し、`SKILL.md` には 2 行 1 組で入っている。**

| 経路 | 守り方 |
| --- | --- |
| `marketplace.json` / `plugin.json` / `SKILL.md` コメント行 / `SKILL.md` 可視行 | `check-plugin-versions.py` |
| 中身を変えて版を据え置く | `check-version-bump.py origin/main` |

刻み方: **手順に必須項目を 1 つ追加する**ので **minor** → **1.16.0 → 1.17.0**。

**I3: 生成物の鮮度。**
**該当判定: 該当する。** `SKILL.md` の行数と節構造が変わるので、設計ドキュメント §8.2 の
生成ブロックが古くなる。→ `skill-metrics.py` で作り直し、`--check` が緑。
**図（`diagrams/`）は触らないので該当なし**（`check-diagram-freshness.py` は変化なしで緑のはず）。

**I4: 追加した絶対リンクが解決する。** → `check-site-links.py`

**I5（内容の不変条件）: 新しい禁止が、既存の必須手順を潰さない。**
これが実質的に最も重要。**周の途中で「最初の差分の外」に出ることを*正しく要求している*手順が
複数ある**ので、禁止を素朴に書くとそれらと衝突する:

| 経路（元の差分の外に出る正当な場面） | 潰していないことの確認 |
| --- | --- |
| 手順 6 の**指摘対応**（レビュー指摘で新しいコードが増える） | 規範が「計画を更新してから広げる」形になっていること |
| 手順 8 の**焼き戻し**（`CLAUDE.md` / Skill の更新） | 同上 |
| 手順 5 の **BLOCKED 起票**・手順 8 の未解決事項の起票 | 別 Issue への切り出しを**推奨**する形になっていること |
| 手順 3 の「**計画が変わったらファイルを更新する**」 | 規範がこれと矛盾しないこと |
| 手順 4 の worktree 作成・ブランチ改名 | 成果物ではないので対象外と読めること |
| **手順 5 の反証による手順 4 への差し戻し**（`/code-review` 1 パス目の指摘 2 で追加） | 差し戻しを「逸脱だから別の周へ」と読めないこと。手順 5 は反証が挙がったら周を閉じない |
| **起票（手順 5 の BLOCKED・手順 8 の未解決事項）**（2 パス目の指摘 2 で追加） | **ファイルを 1 行も変えない**ので「受入基準も広げる」が空振りしないこと |
| **手順 8 の焼き戻し**（2 パス目の指摘 2 で追加） | **手順 7 のマージより後**なので、基準を広げても採点する関門が残っていないこと |

**I6（形の不変条件）: 達成不能な禁止の形で書かない。**
Issue が提案した文言は「**絶対に触らないこと**」だが、このリポジトリは
**散文の禁止が強めても破られた**ことを対照実験で記録している
（[設計ドキュメント §2](https://hdknr.github.io/claude-code-setup/plugins/dev-loop-design/#verify)、
`MEMORY.md` の「散文の禁止は、強めても破られた」）。
**だから「絶対に触るな」ではなく「黙って広げるな（広げるなら計画ファイルに書いてから）」**の形で書く。
確認: 規範の本文に、**検証可能な行為**（計画ファイルへの記載）が指定されていること。

### この環境では証明できないもの（未証明 → 手順 6 で人間レビューへ）

**【訂正】最初は次の 2 つをどちらも「未証明」に入れていたが、`/code-review` 1 パス目の
指摘 5・6 を受けて分類を直した。** 1 つ目は**半分が BLOCKED**（手段が無いだけ）であり、
2 つ目は**一部が「検証をサボった」**だった。

- **未証明（この環境では原理的に証明できない）: この規範が効果を上げること。**
  効果は**将来の周が回ってからでないと存在しない**データに依存する。#92 → #95 と同じ形で、
  **規範を入れる周では、時間そのものが足りない。**
- **一部は「検証をサボった」: 既存の必須手順を潰していないこと（I5）の完全性。**
  「手順を全部読んで衝突を数えるのは静的読解だから証明できない」と書いたが、
  **実際には `/code-review` が普通に読んで見つけた**——1 パス目で 1 件（手順 5 の反証による
  差し戻しが除外列挙から漏れていた）、**2 パス目でさらに 2 件**（除外への一律適用が
  起票と焼き戻しで空振り・矛盾する／この経路表自体が更新されていない）。
  **不完全性は仮説ではなく実証された。**
  **ただし「安く判別する手段があった」とは言えない**（2 パス目の指摘 10）——
  `（必須）` の数え上げは**必須手順を列挙**できるだけで、
  **新しい禁止が既存手順と意味的に衝突しているか**は判別しない。
  **弱い検査を判別手段と呼ばない**（手順 5）。
  **残る部分は人間レビューが最後の関門。**

### BLOCKED（手段が無いだけで、環境が整えば証明できる）

**未証明とは別の見出しで書く**（手順 5 の必須。混ぜると、手当てすれば消える欠落が
恒久的な限界に見える）。**2 パス目の指摘 4 で、見出しが未証明のままだったのを直した。**

1. **BLOCKED であること／何が無いから回せないのか**:
   **「差分が計画の『変更範囲』に収まっていたか」を機械で突き合わせる手段が無い。**
   既存スクリプトにこれを測るものは無い（`token-metrics.py` はトークンしか出さない）。
2. **次善として実際に何を回したか**: **人手のみ**——`/code-review` 2 パスと Verifier に
   経路一覧を渡して読ませた。この周の範囲拡大（指摘対応）は、**計画ファイルの
   「範囲の拡大」節に手で記録**して差分と突き合わせた。
3. **それで証明できなかった受入基準はどれか**:
   **I1〜I6 のいずれでもない。** これは受入基準ではなく、**新しい規範が将来の周で
   守られたかを測る手段**を指す。**だから「受入基準を名指しする」は空欄になる**
   ——手順 5 の報告 3 点のうち 3 番目が埋まらない形であることを明記しておく。

**`git diff --name-only` と計画ファイルの変更範囲を突き合わせる検査は作れる**ので、
**手順 8 で起票して回収する**（作らなければ、宣言した意味が無い）。
**周は閉じない——人間の判断を仰ぐ。**

### 必須要件の掛け算（規定同士の交点）

必須要件が 2 つ以上あるので、組み合わせも受入基準に入れる:

- 「受入基準を**両方の関門**に渡す」×「**2 パス**回す」= **4 つの受け渡し**
  （Verifier × 1 パス目／2 パス目、`/code-review` × 1 パス目／2 パス目）
- 「受入基準を渡す」×「**交絡の観点**を渡す」→ この周は**実験をしていない**ので交絡の規定は
  空振りする。**「実験なし」と明記する**（下の再開点）
- 「**生成物の鮮度**を渡す」×「両方の関門」= Verifier と `/code-review` の**両方**に
  `skill-metrics --check` の独立確認をさせる

## 4. 未解決の判断・確認待ち

- Issue 本文の**切れた部分（3 の対策・4）**は未取得。貼り直されたら**別の周**で扱う。
- **読み込まれたスキルが 1.15.0 だった**（リポジトリ最新は 1.16.0）。
  キャッシュには 1.16.0 が**存在する**のに 1.15.0 が解決された——`plugin update` では
  説明がつかない形。**この周の対象外**だが、手順 8 で起票を検討する。

## 5. 関門の進捗（再開点）

**この欄はこの周で実際に起きたことだけを書く（規定の引用を混ぜない）。**

- 手順 1（Issue 読解）: **完了**。本文が切れていることを確認、調査結果を Issue にコメント済み
- 手順 2（文脈収集）: **完了**。並列読解は**使っていない**（読む対象が少なく互いに依存するため）
- 手順 3（計画）: **完了**（このファイル）
- 手順 4（実装）: **完了**。`SKILL.md` 手順 4 に規範 1 箇所（`SKILL.md:817` 付近）、
  版 1.16.0 → 1.17.0 を 3 種類（`marketplace.json` / `plugin.json` / `SKILL.md` 2 行 1 組）、
  生成ブロックを `skill-metrics.py` で再生成（1451 行 / 節 20 個）。
  worktree: `.claude/worktrees/issue-114` / ブランチ `issue/114-scope-guardrail`
    - **歯止め 7 本すべて緑**: `check-plugin-versions` / `check-norm-markers` / `check-site-links` /
      `check-diagram-freshness` / `skill-metrics --check` / `check-version-bump origin/main` /
      `check-description-sync origin/main`（いずれも exit 0）
    - **【訂正】上の「7 本すべて緑」は、書いた時点では 2 本が空振りしていた**（手順 5 の反証 1 件）。
      `check-version-bump.py` と `check-description-sync.py` は **`base...HEAD` を見る**ので、
      **作業ツリーが未コミットのうちは HEAD == `origin/main` となり、差分ゼロを検査して緑を返す**。
      コミット後に当て直して確認: `version-bump` は
      **「変更はありません」→「dev-loop: version 1.16.0 -> 1.17.0。OK」**に変わり、
      `description-sync` も **cmux / dev-loop / workspace-setup の 3 件を列挙**するようになった。
    - **【訂正】この前後比較だけでは、空振りの原因を判別できない**（`/code-review` 指摘 7）。
      「未コミットだったから」と「版の編集がまだ入っていなかったから」は**どちらも
      `変更はありません` を出す**。**判別しているのはソースの読解**のほうで、
      `check-version-bump.py` が `git diff base...HEAD`、`check-description-sync.py` が
      `merge-base(ref, HEAD)` を見ていることによる。**出力の変化ではなく、これが根拠である。**
    - **残る 5 本は作業ツリーを直接見るので、この空振りは起きない**——根拠は
      **5 本のいずれも `git` を一切呼んでいない**こと（`/code-review` が独立に確認）。
      **「作業ツリーを見るはずだから」という推測ではない。**
    - **回帰テスト 9 本すべて緑**（`test-*.py`、いずれも exit 0）
    - `uv run mkdocs build --strict` **exit 0**（`plans/*.md` が nav 外という INFO は
      `issue-110.md` と同じ既存の形で、退行ではない）
- **交絡を潰した手順: 実験なし**（この周は実験をしていない）
- 手順 5 Verifier: **完了**（`dev-loop:dev-loop-verifier` / `sonnet`。実装は Opus 5 なので別ティア）。
  **反証 1 件**——「`check-version-bump` / `check-description-sync` の緑は未コミットゆえの空振り」。
  **手順 4 に差し戻して対応済み**（コミットして当て直し、上記のとおり出力が変わった）。
  I1〜I6 は一致、「未証明」2 項目の分類も妥当、「実験なし」の申告も一致と判定。
    - **軽微な弱さとして報告されたもの（対応せず、PR に明記する）**: 手順 4 の
      worktree 作成・ブランチ改名が新規項の明文の除外に入っておらず、
      「変更範囲＝触るファイル」（手順 3）からの**推論**で対象外と読める。
      **明文を足さない判断**——推論の出所が新規項自身が参照している手順 3 の定義であり、
      除外の列挙を伸ばすほうが規範を太らせる。
- 手順 6 `/code-review` 1 パス目: **完了。指摘 8 件**（medium 4 / low 4）。内訳は
  **1** 範囲を広げても受入基準を広げる規定が無い ／ **2** 除外列挙に手順 5 の差し戻しが
  漏れている ／ **3**「対象ではない（ただし残す）」の自己打ち消し ／ **4** 委譲文に
  規範が入っていない ／ **5** 効果測定は BLOCKED と未証明の混在 ／ **6** I5 の完全性を
  「原理的に証明できない」に入れたのは一部誤り ／ **7** 空振りの根拠が前後比較のままで
  判別していない ／ **8** `check-norm-markers.py` は I1 を判別していない
- 手順 6 指摘対応: **完了。8 件すべてに対応**（`SKILL.md` に 1〜4、計画ファイルに 5〜8）。
  **範囲を広げたので、上の「範囲の拡大」節に記録済み**（この周の規範を自分に当てた）
- 手順 5 当て直し: **完了**。生成ブロックを再生成（1451 → **1468 行** / 節 20 個 /
  必須マーカー 128 → **130 個**）。**歯止め 7 本すべて緑**——今回は**コミット後に**
  `check-version-bump` / `check-description-sync` を当てているので空振りではない。
  `uv run mkdocs build --strict` も exit 0
- 割り目 2（手順 6 の 2 パス目の前）: **`AskUserQuestion` で提示 → 人間が「このまま 2 パス目を回す」を選択**
- 手順 6 `/code-review` 2 パス目: **未**（反証 — 件）
    - **2 パス目に渡すもの**: 受入基準（不変条件と経路の一覧）**まるごと**＋
      **経路一覧の再導出**（指摘対応で新しい入口が増えていないか）＋生成物の鮮度＋
      交絡の観点。**対象の差分は指摘対応で書いた分に絞ってよい**が、
      **1〜4 は既存の項を*改変*しているので、周辺（手順 4 全体と手順 3 の該当項）も対象に入れる**
- 割り目の選択（手順 4 完了時）: **`AskUserQuestion` で提示 → 人間が「このまま続ける」を選択**。
  割らずに同一セッションで手順 5 へ進んだ。
