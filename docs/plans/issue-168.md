# #168 — Verifier が残したバックグラウンドジョブで報告が届かない（委譲文の禁止と、止まったときの診断）

## 周の在り処

- ブランチ: `issue/168-verifier-background-jobs`
- worktree: `/Users/hdknr/Projects/hdknr/claude-code-setup/.claude/worktrees/issue-168`
- 起点: `e686445`（Merge pull request #167）
- base: 参照 `origin/main`（`refs/remotes/origin/HEAD` から解決）→ `e686445280f40ff3e7a7a839cecb85ede9d1f022`
- 入場: `prepare-worktree.sh` で作成 → `EnterWorktree(path)`。**`ExitWorktree` は畳まない**（path で入ったため）

## 0. 着手前に確かめたこと（Issue コメントの「実装の最初に確かめる」）

実物の transcript（`~/.claude/projects/*/*/subagents/agent-*.jsonl`、`backgroundTaskId` を含む 65 ファイル）で数えた。

| 観測 | 結果 |
| --- | --- |
| Bash の結果に `toolUseResult.backgroundTaskId` が付く形 | 入力に `run_in_background` あり: 11 件 ／ **入力に無い（自動で回った）: 88 件** ／ 前景: 3735 件（ID なし） |
| 自動で回った場合の印 | **`timedOutAfterMs: 120000` が併せて付く**。結果の本文は `Command did not complete within its 120s timeout and was moved to the background (ID: …)` |
| ジョブの完了は記録されるか | **`<task-notification>` を含む `attachment`（`queued_command`）として、`<task-id>` と `<status>` 付きで記録される**。開始 99 件のうち **70 件に完了の記録があり、29 件には無い** |
| 自動で回って完了の記録が無い実物 | `…issue-10856-costcustomer-sheet/80069888-…/subagents/agent-averifier-pass2-939bb468f553f7e1.jsonl` の `find / …`。**ただしこれは遅延の事例ではない**——teammate として `SendMessage` で報告を渡しており、**ジョブが残ったまま親に届いていた**（`/code-review` 1 パス目 #3。当初「事故の実物」と書いたのは誤り） |

**完了の記録が無い 29 件が「いまも走っている」かは分からない**——記録されない理由を特定していない。
だからスクリプトは**「完了の記録が無い」までしか言わない**（§6）。

**Issue 本文の時刻（26 分で書き終え 97 分後に届いた）は、上の実物ではなく別の実物と一致した**——
`…issue-10974/6c4131ab-…/subagents/agent-ae3210dfe0bcdbb88.jsonl`（全 97 分）。
スクリプトを当てると: **自動で回った 5 本**（`until ! kill -0` 系 4 本は +24 分で完了、
残る 1 本は **+96:13 に完了**）、**`end_turn` の text が +25:41（3751 文字）と +97:00（48 文字 = 引き渡しの定型文）**。
**+25:41 のあと、ジョブの完了通知（user の SYSTEM NOTIFICATION）で子が起こされ、
さらに道具を呼んでから +97 分に引き渡した。** → **「最後の text」で判定すると書き終えた時刻が消える**ので、
判定を **`end_turn` の text** に変えた。**その判定も 1 パス目で反証された**（§5）——
全 1026 件で最後の text の `stop_reason` は `end_turn` 935 ／ `None` 46 ／ `stop_sequence` 29、
`SubagentHandback` で報告を渡すものが 62 件、`end_turn` の text が「待ちます」だけの例もある。
→ **「書き終えたか」の判定をやめ、発言を並べて親が読む**形に弱めた（不変条件 C の改訂）。

| 追加の実測（全 transcript の自動で回った Bash） | 結果 |
| --- | --- |
| `timeout` 引数なし | 回った時点 120000 ms: 240 件（**600000 ms: 2 件**——既定が環境で違う） |
| `timeout` を明示（≦ 600000） | **明示した値で回る**（300000 → 300000 など） |
| `timeout` を 600000 より大きく明示 | **600000 で回る**（1800000 → 600000: 8 件 など）——**上限は 10 分** |

→ 委譲文は「既定は多くの環境で 120 秒」「上限は 10 分」と書いた。

## 1. 変更範囲

触る:

- `plugins/dev-loop/skills/dev-loop/scripts/summarize-subagent.py` — 新規。サブエージェント 1 体分の transcript を集計する
- `plugins/dev-loop/skills/dev-loop/SKILL.md` — 手順 5「Verifier が報告を返さなかったら」／版のバナー
- `plugins/dev-loop/skills/dev-loop/prompts/verifier.md` — 「実環境を壊さないこと」に 4 つ・「報告の形」に経過時間
- `plugins/dev-loop/skills/dev-loop/prompts/review-handoff.md` — 「実環境を壊さないこと」に 4 つ
- `plugins/dev-loop/agents/dev-loop-verifier.md` — 「守ること」に 4 つ
- `plugins/dev-loop/.claude-plugin/plugin.json` — 版 1.32.1 → 1.33.0
- `.claude-plugin/marketplace.json` — 同上
- `docs/plugins/dev-loop-design.md` — `skill-metrics.py` の生成ブロック（手で書かない）**と、
  「欠票と遅延は外から区別できない」の注記**（1 パス目 #7 で範囲を広げた。`SKILL.md` と食い違っていた。
  **規範は再掲せず、`SKILL.md` 手順 5 を正とする**と指すだけ）
- `scripts/test-summarize-subagent.py` — 新規。回帰テスト（変異テストを含む）
- `scripts/check-all.py` — `TESTS` に登録
- `.github/workflows/plugins.yml` — テストのステップを足す
- `CLAUDE.md` — `scripts/` 一覧に 1 行
- `docs/plans/issue-168.md` — この計画ファイル

触らない:

- `plugins/dev-loop/README.md`・公開ページの本文 — 手順の列挙を持たない（指すだけ）
- `plugins/dev-loop/skills/dev-loop/references/` — 条件つきの節を出す先だが、今回の追加は手順 5 の既存項の中に収まる
- `SKILL.md` の frontmatter と 2 つの JSON の `description` — 能力の要約は変わらない
- `diagrams/` — 図は触らない

**複製の数**: 4 つの禁止は **3 箇所**（`verifier.md` / `review-handoff.md` / agent 定義）に同じ趣旨で入る。
**分けない理由**: 3 つとも**単体で子に届く文面**で、指し先を書いても子はそれを開かない
（既存の「実環境を壊さないこと」も 3 箇所に同じ形で置いてある）。**`SKILL.md` には禁止を再掲しない**
——手順 5 の追加は診断のほうだけで、禁止は「`prompts/verifier.md` を正とする」で指す。

## 2. 乗るデプロイ経路

main へのマージ → GitHub Pages の自動デプロイ（`docs/plugins/dev-loop-design.md` の生成ブロックが変わる）。
プラグインはマーケットプレイス経由で利用者側に届く（`plugin update` で記録が進む）。

## 3. verify の受入基準

### 3.1 不変条件

| # | 不変条件 |
| --- | --- |
| A | スクリプトは**前景 / 指定してバックグラウンド / 自動でバックグラウンド**の 3 つを判別して出す。判定は**結果の `backgroundTaskId`** で行い、指定か自動かは入力の `run_in_background` で分ける |
| B | バックグラウンドに回った各呼び出しについて、**完了の記録（`<task-notification>` の `<task-id>`）の有無**を突き合わせて出す。**記録が無いものを「完了」と数えない**。**記録が無いことを「まだ走っている」とも言わない** |
| C | **「報告を書き終えたか」を判定しない**（1 パス目で改訂）。子の発言（assistant の text と `SubagentHandback`・`SendMessage` の引き渡し）を **`stop_reason` で絞らずに**、経過・文字数・冒頭つきで並べる。`--text N` で指定した 1 件だけを全文で出す |
| C2 | 完了の記録は**通知の行（`attachment` と、道具の結果を含まない user の発言）からだけ**拾い、task-id と status は **1 つの通知の中でだけ**組にする |
| D | ID・**名前（`名前@チーム`）**から探して 2 件以上当たったら**選ばずに並べて非ゼロ**で終わる。0 件・読めない入力・範囲外の `--text` も非ゼロ |
| E | 出力は**集計だけ**で、ツールの生出力を貼らない。**コマンドは頭と尾だけ**出す（**印は 3 パス目の相談で外した**） |
| L | 委譲文の「残っているジョブが報告を止める」は**「渡らないことがある」**の強さで書く（teammate 経路では渡った実物がある） |
| M | `SKILL.md` は**ジョブを止めることを当てにしない**（止められるかは確かめていない） |
| N | 設計ドキュメントの「外から区別できない」が `SKILL.md` と食い違わない |
| F | テストは実環境（`~/.claude`）を読まない（アサートで担保）。変異テストは docstring の「守る」と 1 対 1 |
| G | 3 つの委譲文面に 4 つの禁止（ディスク全体の走査・バックグラウンドのジョブ・頼まれていない全体スイート・コンテナ／サービスの起動）と、それぞれの代わりにすることが入っている |
| H | `SKILL.md` 手順 5 に、「background work 待ち」で止まったときは**再実行の前にスクリプトで集計する**が入り、**既存の「区別できないなら再実行」は条件付きで残る**（消さない・緩めない） |
| I | 版が 3 箇所で 1.33.0 に揃い、生成ブロックが最新（`skill-metrics.py --check`）、`check-all.py` が全部緑 |
| J | `SKILL.md` からスクリプトへの指し先が実在する（`check-skill-pointers.py`） |
| K | 書いた事実（自動で回ると `backgroundTaskId` と `timedOutAfterMs` が付く・完了は `<task-notification>` で記録される・`timeout` の上限は 10 分・既定は多くの環境で 120 秒・+25 分の報告が +96 分まで渡らなかった）が、実物の transcript と一致する |

### 3.2 これを破りうる経路

| 不変条件 | 経路 | どう守るか |
| --- | --- | --- |
| A | 入力フラグだけで判定する実装 | 合成 transcript の「自動」ケース ＋ 変異 |
| A | `timedOutAfterMs` が無いのに ID が付く形（手で `run_in_background` を指定しない別の経路） | 「自動」の判定は「ID あり かつ 指定なし」で行い、`timedOutAfterMs` は補助の表示に留める |
| A | Bash 以外の道具（`Agent` の background など）が `backgroundTaskId` を返す | 道具名に依らず `toolUseResult.backgroundTaskId` を見る |
| B | 同じ task-id の通知が 2 回ある（実物で観測） | 最初の 1 件で足りる（有無を見る） |
| C2 | 道具の結果・入力に通知の文面が写る（子が出力ファイルを `cat` する。実物で 2 件） | 通知の行だけを見る ＋ 合成 ＋ 変異 |
| C2 | 1 つの行に通知が 2 つ（status が違う） | 通知のブロックごとに組にする ＋ 合成 ＋ 変異 |
| C2 | 通知が `attachment` ではなく user の発言（SYSTEM NOTIFICATION）で届く | 両方を見る（`ae3210…` の +96:13 が後者で拾えることを実物で確認） |
| C | 最後の報告の `stop_reason` が `None`（実物 46 件） | 絞らずに並べる ＋ 合成 ＋ 変異 |
| C | 報告が `SubagentHandback` ／ `SendMessage` で渡る | 引き渡しとして並べる ＋ 合成（`SendMessage`）＋ 実物（`a01d62…` の `SubagentHandback`） |
| C | `--text` が複数を出す（文脈に積む） | 合成 ＋ 変異 |
| D | 名前で探して 2 件（同名の teammate が別セッションにいる） | `meta.json` の `name`／`teamName` で当て、2 件なら rc=2 ＋ 合成 ＋ 変異 |
| E | 頭だけに切って本体（`… ; find /`）が隠れる | 頭と尾 ＋ 合成 ＋ 変異（印は外した） |
| F | 既定の `~/.claude/projects` をテストが読む | `--projects-dir` を必ず渡し、tmp の中であることをアサート |
| G | 3 箇所のうち 1 箇所だけ直す | 3 ファイルを grep で突き合わせる（関門にも当てさせる） |
| H | 「区別できない」を削って「集計すれば区別できる」に置き換える（基準を緩める） | 差分で既存行が残っていることを見る |
| I | 版の 3 箇所・生成ブロック・CI 登録 | `check-all.py` |
| K | 実物と合わない記述 | 実物の transcript 3 件にスクリプトを当てる（陽性対照） |
| L | 3 箇所のうち 1 箇所だけ弱める | grep で 3 ファイルを突き合わせる |
| N | 公開ページだけ古い規定が残る | 設計ドキュメントは `SKILL.md` を指すだけにする |

### 3.3 この環境では証明できないもの（未証明）

- **禁止を足したあとで、Verifier が実際にジョブを残さなくなるか。** CI はモデルを起こせないし、1 回走らせても母集団にならない。**散文の禁止は強めても破られた前例がある**ので、診断（スクリプト）を同じ周に入れた。人間レビューに回す。

### 3.4 BLOCKED

なし。

### 3.5 生成物の鮮度

該当あり: `docs/plugins/dev-loop-design.md` の §8.2 生成ブロック（`skill-metrics.py`）。`--check` で確かめる。
図（`diagrams/`）は触らないので対象外。

### 3.6 必須要件の掛け算

受入基準 × {Verifier, `/code-review`} × {1 パス目, 2 パス目} = 4 つの受け渡し。
既知の限界（§6）も同じ 4 つに渡す。

### 3.7 陽性対照

- `939bb468…`（§0）で、`find /` の行が「自動・完了の記録なし」と `[find /]` の印つきで出ること。名前@チーム（`verifier-pass2@session-96899c86`）で解決できること。
- `ae3210…`（§0）で、+25:41 の報告（3751 文字）と +96:58 の引き渡しが並び、`find /` のジョブが +96:13 に完了と出ること。
- 完了の記録がある実物（`…issue-10974/176e9ffb-…/subagents/agent-a01d62bfcf46c9eff.jsonl`）で「完了（completed）」と出ること。

## 4. 未解決の判断

- なし（Issue コメントの計画に沿う。コンテナの項は「ネットワーク越しのプロセス」を「常駐するサービス」に言い換えた——**`gh` の読み取りは委譲文が許しているので、「ネットワーク越し」と書くと衝突する**）。

## 5. 関門の進捗（再開点）

- 実装: 完了（手順 4）。`check-all.py` 29/29 緑（ローカル）
- 陽性対照: §3.7 の 2 件 ＋ `ae3210…`（§0）で期待どおりに出ることを確認済み
- Verifier: 1 パス目 **完了・反証 1 件**（`dev-loop:dev-loop-verifier`、sonnet。起動した agent は報告の宛先・道具の範囲から同梱の定義と判断）。
  反証は「生成ブロックが古い（1472 行のまま）」——**著者の編集中の状態を見たもの**で、`f4c52da` の前に再生成済み
  （`skill-metrics.py --check` 緑を再確認）。**Verifier は `f4c52da` 相当の現行コードで A〜N を当て、他は反証 0 件**。
  **注意**: Verifier の `check-all.py` 1 回は著者の編集と時間が重なっており、その回の緑は最終状態の証明にならない（Verifier 自身の申告）。
  副産物: Verifier 自身の Bash が 120 秒で自動的に回った（`bpdneip4g`）が、出力を読んで完了を確かめ、残さなかった
- `/code-review`: 2 パス目 **完了・指摘 10 件**（high）。採否は下表。**10 件とも採った**（未コミットで対応）
- Verifier: 2 パス目 **完了・内容の反証 0 件**（`0eedc6c` に対して。§0 の数値を独立に再計算して一致）。
  **手続きの指摘 1 件**: 検証中に作業ツリーが編集された（下の申告どおり著者の編集）。**その回の check-all 緑は証明にならない**。
- 2 パス目の対応は `3677f77`。`check-all.py` 29/29 緑（著者。**10 分を超えて自動でバックグラウンドに回った**——
  `bddr4q5sl`、完了の通知を待って読んだ）
- 3 パス目: **Verifier と `/code-review` の両方**に `3677f77` を当てる。**0 件でなければ止まって相談する**（手順 6）
  **著者の申告**: Verifier に「検証中は誰も編集しない」と伝えたのに、`/code-review` 2 パス目の対応で**検証中に編集した**
  （1 パス目と同じ重なり）。**Verifier の報告は `0eedc6c` の時点について読み、編集後の分は 3 パス目で当て直す**
- 2 パス目で当て直すもの: **手順 5（Verifier）と手順 6（`/code-review`）の両方**。対象は `e686445..f4c52da`（指摘対応は既存コードの改変を含むので絞らない）
- `/code-review`: 1 パス目 **完了・指摘 9 件**（high）。採否は下表。**9 件とも採った**
- 反証・差し戻し: `/code-review` 1 パス目の 9 件で手順 4 に差し戻し → 対応済み（未コミット）。
  **不変条件 C を「判定する」から「判定しない」に弱めた**（主張を測った範囲まで弱める。PR コメントで名指しする）
- 交絡を潰した手順: §0 の実測は陽性側（自動で回った 88 件）と陰性側（前景 3735 件に ID なし）の両方を取った。
  `timeout` の上限は「明示した値で回る」陽性側（≦ 600000）と「上限で頭打ち」側の両方を見た
- 割り目（手順 4）: AskUserQuestion で訊いた → **「このまま続ける」**
- 割り目（手順 6・2 パス目の前）: AskUserQuestion で訊いた → **「このまま続ける」**

## 6. 既知の限界・決着済みの論点

| 論点 | 決着 | 理由 |
| --- | --- | --- |
| 完了の記録が無いジョブを「走っている」と言うか | 言わない | 記録が無い 29 件の理由を特定していない |
| `timedOutAfterMs` で自動を判定するか | しない（表示だけ） | 判定を 2 つの印に依らせると、片方が消えたとき黙って落ちる。「ID あり・指定なし」で決まる |
| 4 つの禁止を `SKILL.md` にも書くか | 書かない | 子に届くのは委譲文面と agent 定義。`SKILL.md` は指す |
| 子が自分のジョブを止められるか | 主張しない | Verifier の道具（Read/Grep/Glob/Bash）でジョブを列挙・停止する手段を確かめていない。だから禁止は「残さない（起こさない）」側に置く |
| Issue 本文の時刻（26 分／97 分） | `ae3210…` と一致した | §0 |
| `SKILL.md` の「外から区別できない」を書き換えたか | 「親の画面からは区別できない」に絞り、「区別できないなら再実行」は残した。**集計で区別できる場合だけ再実行しない** | Issue コメントの方針どおり。基準を緩めていない（区別できないときの規定は不変） |
| スクリプトが「報告を書き終えたか」を判定するか | しない（1 パス目で改訂） | `stop_reason` は `end_turn`・`None`・`stop_sequence` に割れ、`end_turn` でも報告とは限らず、報告が引き渡しの道具で渡ることもある。**機械で判定できる印が見つからなかった**ので、並べて親が読む |
| 印（`[find /]` など）の網羅 | 主張しない | 文字列から見える形だけ。docstring の「守らない」に書いた |
| 「ネットワーク越しのプロセス」を禁じるか | 「コンテナ・常駐するサービス」に言い換えた | 委譲文は `gh` の読み取りを許しており、「ネットワーク越し」だと衝突する |

### `/code-review` 1 パス目の指摘と、その採否

| # | 指摘 | 採否 | 対応 |
| --- | --- | --- | --- |
| 1 | `end_turn` だけで判定すると、`stop_reason: None` の報告（実物 41 件）を「書き終えていない」と出す | 採る | 判定をやめ、絞らずに並べる（C） |
| 2 | `end_turn` の text が報告とは限らない（「待ちます」、引き渡しの定型文） | 採る | 同上。`SKILL.md` に「迷ったら再実行」 |
| 3 | 「1 本でも残っていると渡らない」は teammate の実物（`939bb468…`）で偽。§0 の「事故の実物」も誤り | 採る | 「渡らないことがある」に弱めた（L）。§0 を訂正 |
| 4 | 名前（`verifier-pass2@session-…`）で解決できない | 採る | `meta.json` の `name`／`teamName` で当てる（D） |
| 5 | 変異の 1 対 1 が数だけで、観測の差で「殺せた」と数えていた。部分主張の変異が生き残る | 採る | 落ちた check で数える。`timedOutAfterMs` の変異・通知のブロックの変異を足した |
| 6 | 道具の結果に写った通知を完了と数える。§3.2 の約束（生テキストとパース済みの両方）が未実装 | 採る | 通知の行だけを見る（C2）。§3.2 の行は書き直した |
| 7 | 設計ドキュメントに「外から区別できない」が残り、`SKILL.md` と食い違う | 採る | 範囲を広げて注記し、`SKILL.md` を指す（N） |
| 8 | 「待つか止めるか親が決める」は止められるか確かめていない。書き終えた報告を読む手段も無い | 採る | 「止める」を外した（M）。`--text N` を足した |
| 9 | 頭 60 文字で切ると `find /` が隠れる | 採る | 頭と尾 ＋ 印（E） |

### `/code-review` 2 パス目の指摘と、その採否

| # | 指摘 | 採否 | 対応 |
| --- | --- | --- | --- |
| 1 | `Monitor` の結果は `taskId` で、`backgroundTaskId` だけでは監視が見えない（実物 19 件） | 採る | `Monitor` は `taskId` で「監視」として数える（守る 2） |
| 2 | 試料に user の発言で届く通知が無く、守る 3 の半分が無検査（変異が生き残る） | 採る | 試料を足し、守るを 1 行 1 主張に割った（守る 4・5） |
| 3 | 引き渡しのテストが `SendMessage` だけ | 採る | `SubagentHandback` の試料と check（守る 9） |
| 4 | `find ~` の印が狭い探索にも、`&` の印が URL にも付く（実物 49 件・114 件） | 採る | 境界を足した（守る 14）。陰性の試料を足した |
| 5 | 委譲文の禁止に `Monitor` が無い | 採る | 3 ファイルに `Monitor` の監視を名指し |
| 6 | `<status>` の無い途中経過を完了に数え、先に来た「?」が後の `completed` を隠す（実物 12 件） | 採る | status の無い通知は数えない（守る 7）。「completed は子の仕事の完了ではない」を守らないに書いた |
| 7 | 新しい 6 件の窓から報告が押し出される | 採る | 引き渡しと最長の text は必ず出す（守る 10・11） |
| 8 | `--text 0` と `stop_sequence` が無検査 | 採る | check と変異（守る 8・16） |
| 9 | `CLAUDE.md` が docstring の守るを写している | 採る | 写しを消して docstring を指すだけにした |
| 10 | docstring に「外から区別できない」が残る | 採る | 「親の画面からは」に直した |

### 3 パス目の結果（ここで止まって相談する——手順 6）

- `/code-review` 3 パス目: **指摘 9 件**。Verifier 3 パス目: **反証 1 件**（`[&]` の印が実物 64 件中 60 件で誤検出。`/code-review` #1 と同じ）。
  **作業ツリーは両関門の検証中に動いていない**（Verifier が開始・終了で確認）。
- パスごとの件数: `/code-review` **9 → 10 → 9**、Verifier **1（編集中の状態を見た）→ 0 → 1**。

| # | 指摘（`/code-review` 3 パス目） | 型 |
| --- | --- | --- |
| 1 | `[&]` の印が Python の演算子・英文の `&` に付く（実物 63 件中、行末の `&` は 21 件） | 実物の形 × 推測（印） |
| 2 | docstring の「前者なら待つ」が `SKILL.md` の「迷ったら再実行」と食い違う | 宣言と内容の食い違い |
| 3 | 守る 12 の ID の半分が無検査（重複 ID の試料が無い） | 1 行 2 主張 |
| 4 | 守る 16 の負の数が無検査 | 1 行 2 主張 |
| 5 | 守る 13 が 3 主張。`nohup`・`docker` の印・`timedOutAfterMs` の表示が無検査 | 1 行 2 主張 |
| 6 | `SendMessage` の `message: null`（本体は `content`）を「None」と出す（実物 1 件） | 実物の形 |
| 7 | パスで渡すテストが `--projects-dir` を渡さず、既定の実環境が生きている | 歯止めの構造 |
| 8 | `review-handoff.md` の `run_in_background` が `Agent` の fan-out まで禁じて読める | 宣言の範囲 |
| 9 | 文字列の user 行を全部通知の運び手として読む（実物 0 件だが歯止めなし） | 推測の範囲 |

**型**: 18 件中ほとんどが **`summarize-subagent.py` の推測（印・通知の運び手・引き渡しの形）と実物の形の突き合わせ**、
および **「守る」1 行に 2 つ以上の主張を載せて変異が当たらない**の 2 型で、**パスをまたいで同じ型が続いている**。
禁止の文面・`SKILL.md` の側は 3 パス目で 2 件（#2・#8）まで減った。

### 相談の結果: **「削る」**（AskUserQuestion で訊いた → 人間が選んだ）

- **印（`[find /]` `[&]` など）の機能を丸ごと外した**——3 パス目の 18 件のうち、印に起因するものが最大の塊で、
  **推測を足すたびに別の誤検出が見つかった**（2 パスで 49 件・114 件 → 境界を足して 64 件中 60 件）。
  docstring の「守らない」に、外したことと理由を書いた。**不変条件 E を「頭と尾を出す」だけに弱めた**（PR コメントで名指しする）。
- **user の発言に引用された通知は区別しない**（3 パス目 #9）——推測を足さず、「守らない」に書いて弱めた。
- **残りの 3 パス目の指摘は直した**: #2（docstring の「前者なら待つ」を削除）・#3（重複 ID の試料と変異）・
  #4（`--text -1`）・#5（守るを 1 行 1 主張に割り、`timedOutAfterMs` の表示に変異。`nohup`・`docker` は印ごと削除）・
  #6（`message: null` → `content`）・#7（パスで渡すテストにも空の `--projects-dir` を渡す）・
  #8（`run_in_background` を「Bash の」に限定し、`Agent` の並列起動は対象外と明記）。
- **「守る」は 20 行・変異 20 件**。**1 行に 1 主張**を docstring の冒頭に規則として書いた。
- 4 パス目: **Verifier と `/code-review` の両方**。**0 件でなければ、また止まって相談する。**
